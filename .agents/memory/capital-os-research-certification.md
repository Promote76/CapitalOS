---
name: Approved research browser certification
description: Durable constraints for authenticated research-isolation browser gates.
---

Authenticated browser certification for advisory research should delay and intercept the API path, not the page URL, when proving loading states. Compare stable financial and execution invariants while excluding request-generated snapshot identifiers.

**Why:** Routed page URLs can share suffixes with APIs, causing a test to pass for the wrong reason or miss a fast-loading view; Capital Governor snapshot IDs are regenerated on reads even when decisions are unchanged.

**How to apply:** Use separate page and API paths in loading/error helpers, compare persisted decision fields, and remove fixture-owned audit dependencies before deleting disposable households.