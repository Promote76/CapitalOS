Replit Prompt — Capital OS Final 2 P0 Closure Sprint

Continue working on the existing Capital OS repository.

Use:

docs/CAPITAL_OS_FINAL_COMPLETION_REPORT_2026-09-02.md

as the single authoritative completion record.

Current state:

RELEASE DECISION:
NOT READY

P0 START:
2

P0 OPEN:
2

The ONLY remaining P0 gates are:

P0-04 Managed Backup / Restore
P0-05 Authenticated Browser Journey

The following P0 gates are already certified and must remain regression-only:

P0-01 Tenant / IDOR
P0-02 Origin / CSRF
P0-03 Existing-Schema Upgrade
P0-06 Roles / Effective Permissions
P0-07 Concurrent Idempotency
P0-08 Actor Attribution

Do NOT add new product functionality.

Do NOT add:

Simply Wall St
Plaid
Banking
ACH
Brokerage
Live Trading
Blockchain
Debt/Credit
Autonomous AI
New Strategy Features
New Property Features

This sprint exists only to reach:

P0 OPEN = 0

or to leave exactly one externally controlled restore gate with precise user action.

---

1. RECONCILE STALE COMPLETION-REPORT LANGUAGE FIRST

Before changing application code, reconcile stale statements in:

docs/CAPITAL_OS_FINAL_COMPLETION_REPORT_2026-09-02.md

The authoritative P0 truth is now:

P0-01 Tenant / IDOR = PASS
P0-02 Origin / CSRF = PASS
P0-03 Existing Schema Upgrade = PASS
P0-04 Backup / Restore = BLOCKED
P0-05 Browser E2E = FAIL
P0-06 Role / Permission = PASS
P0-07 Idempotency = PASS
P0-08 Actor Attribution = PASS

Update every table and narrative to match.

---

2. FIX SECURITY TABLE STATUS DRIFT

The Security Certification section still contains stale language for:

Origin / CSRF
IDOR

Update them to:

Origin / CSRF:
PASS for P0-02

IDOR:
PASS for P0-01

Mass Assignment:
PASS for tested P0-01/P0-06 boundary

Do not downgrade these because Browser E2E is separately open under P0-05.

---

3. FIX AUTHENTICATION WORDING

The Browser E2E journey has now actually executed.

Therefore remove stale claims such as:

No real signed-in browser journey

where no longer accurate.

Replace with the actual state:

Authenticated Clerk browser journey executed.

Authentication, persistence, and isolation assertions passed.

Visible onboarding and signed-out UI assertions failed.

Keep:

P0-05 = FAIL

until those visible lifecycle failures are corrected and rerun.

---

4. P0-05 IS NOW THE PRIMARY ENGINEERING TASK

Do not redesign the authentication architecture.

The authenticated journey already proved major backend boundaries.

Identify the exact failed Playwright/browser assertions.

Separate them into:

ONBOARDING FAILURE
SIGNED-OUT UI FAILURE

Record:

Test name
Page
Expected state
Observed state
Screenshot if available
Console error if any
Network error if any
Relevant component
Relevant route/API

---

5. DO NOT CHANGE TESTS TO MAKE THEM PASS

The objective is:

FIX APPLICATION

not:

WEAKEN ASSERTION

Do not:

remove assertion
skip test
increase timeout without evidence
change expected UI to match a defect

unless the test itself is demonstrably incorrect relative to approved product behavior.

---

6. ONBOARDING FAILURE — DETERMINE THE ROOT CAUSE

The required onboarding lifecycle is:

Authenticated Clerk User
↓
GET /api/auth/me or equivalent
↓
No household detected
↓
Visible onboarding experience
↓
Create first household
↓
Owner membership created
↓
Conservative defaults created
↓
Dashboard

Determine where the current flow diverges.

---

7. ONBOARDING MUST BE USER-VISIBLE AND DETERMINISTIC

A brand-new authenticated user without a household must not land in:

blank dashboard
loading loop
404
generic error
Morgan demo account
partially initialized state

They must receive the intended onboarding state.

---

8. AUTHENTICATION STATE MACHINE

Make the frontend authentication lifecycle explicit.

Conceptually:

AUTH LOADING
     ↓
SIGNED OUT
     ↓
SIGN-IN UI

AUTHENTICATED
     ↓
FETCH INTERNAL USER / MEMBERSHIP
     ↓

NO HOUSEHOLD
     ↓
ONBOARDING

HAS HOUSEHOLD
     ↓
APPLICATION

Do not allow:

AUTHENTICATED
+
NO HOUSEHOLD

to fall through into ordinary dashboard rendering.

---

9. FIRST-HOUSEHOLD CREATION

Verify onboarding creates:

Internal User
Household
Owner Membership
Conservative Risk Defaults
Conservative Capital Defaults
Audit Event

transactionally according to existing implementation.

Do not add demo capital.

---

10. CLEAN HOUSEHOLD ASSERTION

After onboarding verify:

Morgan balances = absent
Morgan goals = absent
Morgan businesses = absent
Morgan properties = absent
Morgan strategies = absent
Morgan activity = absent

unless explicitly created by the new user.

---

11. ONBOARDING IDEMPOTENCY

Repeated:

reload
double submit
navigation retry

must not create:

duplicate household
duplicate owner membership
duplicate defaults

Add browser regression coverage if not already present.

---

12. SIGNED-OUT UI FAILURE — IDENTIFY EXACT OBSERVED DEFECT

The expected signed-out state should be deterministic.

When no Clerk session exists:

Protected Capital OS UI

must not render as though the user is authenticated.

Determine whether the failing browser assertion currently sees:

dashboard flash
protected sidebar
stale household content
incorrect redirect
loading state that never resolves
wrong sign-in surface

Fix the actual defect.

---

13. NO PROTECTED-UI FLASH

A signed-out visitor must not briefly see protected financial content before redirect/auth resolution.

Do not render:

Balances
Duplex Goal
Business data
Property data
Strategies
Household navigation

until authentication is resolved and authorized.

---

14. SIGNED-OUT ROUTING

Required behavior:

SIGNED OUT
+
PROTECTED ROUTE
=
SIGN-IN / AUTH BOUNDARY

No backend protected data request should succeed.

The backend already rejects signed-out access; align the browser UX with that boundary.

---

15. SIGN-OUT STATE CLEARING

On sign-out:

clear or invalidate frontend:

household cache
React Query protected data
local protected state
derived financial state
selected household state

as appropriate.

Do not retain protected household state visibly after session destruction.

---

16. SIGN-BACK-IN

After the same user signs back in:

Internal user resolves
Existing household resolves
Existing owner membership resolves
Existing financial state loads

Do not create a duplicate household.

---

17. COMPLETE P0-05 BROWSER JOURNEY

After fixes, rerun the complete authenticated journey:

Sign Up / Sign In
↓
Onboarding visible
↓
Create Household
↓
Owner Membership
↓
Dashboard
↓
Create Contribution
↓
Verify allocation
↓
Reload
↓
Fresh API read
↓
Verify persistence
↓
Sign Out
↓
Verify signed-out UI
↓
Protected route denied
↓
Sign In
↓
Same Household
↓
Same State

---

18. CONTRIBUTION BROWSER ASSERTIONS

Verify:

$250 total contribution

$200 Duplex Reserve
$25 Capital OS
$25 Opportunity Reserve

according to configured weekly allocation.

Then reload and verify durable state.

---

19. BROWSER PERSISTENCE ASSERTIONS

After reload compare:

Contribution
Allocation Metadata
Ledger
Goal Progress
Treasury Relationship
Audit Actor

through fresh server reads.

---

20. VIEWER REGRESSION

Keep:

Viewer UI write denied
Viewer direct HTTP write denied

as regression evidence.

---

21. CROSS-HOUSEHOLD REGRESSION

Keep authenticated browser/API attempts against a second household.

Require:

DENIED

No tenant leakage.

---

22. FAILED-WRITE REGRESSION

Force a backend rejection.

UI must:

Show error
Not claim success
Rollback/remove invalid optimistic state
Fresh server read unchanged

---

23. P0-05 CLOSURE RULE

Only if the complete authenticated lifecycle passes:

P0-05 = PASS

A partial browser run does not count.

---

24. UPDATE TEST INVENTORY AFTER P0-05

If passed:

Browser E2E:
PASS

and move the browser P0 issue to:

Resolved During Hardening

Include exact test evidence.

---

25. P0-04 — MANAGED BACKUP / RESTORE

Do not simulate this.

Inspect the actual Replit Database interface/capability one final time.

Determine whether an isolated restore can be initiated safely by the agent.

---

26. NO PRODUCTION OVERWRITE

Never restore over:

Production Database
Shared Development Database
Certification Database

Restore only into:

NEW ISOLATED NON-PRODUCTION TARGET

---

27. IF REPLIT CAN INITIATE SAFE ISOLATED RESTORE

Execute:

Approved Point-in-Time Backup
↓
New Isolated Restore Target
↓
Connect Read-Only Verification Process
↓
Run Recovery Certification

---

28. RESTORE VERIFICATION MATRIX

Verify restored:

Users
Households
Memberships
Accounts
Goals
Contributions
Ledger
Protected Capital
Treasury
Business
Property
Strategies
Accounting
Operations
Audit
Micro-Live state

---

29. RESTORE TENANT BOUNDARY

Run at least representative:

Household A cannot access Household B

checks against restored data.

---

30. RESTORE FINANCIAL INVARIANTS

Require:

Debits = Credits

Protected Duplex Reserve intact

Emergency Reserve intact

No invalid negative balance

No business-to-household leakage

Safe-to-Deploy remains conservative

Micro-Live real execution remains disabled

---

31. RESTORE APPLICATION READINESS

Point a test process at restored PostgreSQL.

Require:

Liveness = PASS
Readiness = PASS
Expected schema = compatible

---

32. RPO / RTO

Record:

Source backup timestamp
Restore requested at
Restore completed at
Application verification completed at
Observed RPO
Observed RTO

Use actual measurements.

---

33. RESTORE AUDIT EVIDENCE

Record:

Backup reference
Restore target identifier
Schema version
Release version
Ledger check result
Tenant check result
Protected-capital check result
Audit-count comparison

No credentials.

---

34. P0-04 CLOSURE RULE

Only after a real managed restore and verification:

P0-04 = PASS

---

35. IF RESTORE REQUIRES USER ACTION

Do not call it an engineering failure.

Leave:

P0-04 = BLOCKED

and put exact instructions in the Final Completion Report.

Use the actual Replit UI wording if known.

Required structure:

USER ACTION REQUIRED — MANAGED BACKUP / RESTORE

1. Open the Replit Database tool.
2. Select the production database.
3. Choose an approved point-in-time restore.
4. Restore into a NEW isolated non-production database.
5. Do NOT restore over production.
6. Make the isolated restore target available to the Capital OS certification environment.
7. Record backup timestamp and restore timestamps.
8. Rerun the recovery verification and production-candidate certification.

Adapt these steps to the actual provider controls.

---

36. IF ONLY P0-04 REMAINS

If browser E2E passes but provider restore is still blocked, the completion document must state clearly:

ALL REPLIT-CONTROLLABLE P0 GATES = CERTIFIED

ONLY REMAINING P0:
P0-04 MANAGED BACKUP / RESTORE

RELEASE:
NOT READY

Do not promote early.

---

37. IF P0-04 AND P0-05 BOTH PASS

Then:

P0 OPEN = 0

Set:

PRODUCTION CANDIDATE

for:

CURRENT NON-EXECUTING FAMILY-CAPITAL SCOPE

only.

---

38. P1 REMAINS AFTER P0

Do NOT erase:

Provider-Supported Clerk Step-Up
Durable Scheduler / Queue
Metrics / Alerts
Shared Rate Limiting
Cross-View Accounting
Micro-Live Recovery Durability

These remain P1.

---

39. IMPORTANT DEPLOYMENT-SCOPE RULE

Even if:

P0 OPEN = 0

Capital OS still must NOT enable:

Bank transfers
ACH
Live brokerage
Live venue execution
External investor capital
Blockchain transactions
Autonomous AI execution

---

40. CORRECT MODULE INVENTORY STALE TEXT

The current module inventory still references:

Transactions — full identifier matrix remains open
Settings — complete HTTP role remains open

Those statements are now stale because P0-01 and P0-06 pass.

Update module maturity language accordingly.

Do not change maturity beyond what evidence supports.

---

41. CORRECT UI PERSISTENCE STALE TEXT AFTER BROWSER PASS

If P0-05 passes, update:

browser reload proof is open

where the browser suite now proves reload persistence.

Keep open only workflows not exercised.

---

42. CORRECT FINAL TEST INVENTORY

After rerun, ensure:

Tenant / IDOR = PASS
Role / Permission = PASS
Origin / CSRF = PASS
Idempotency = PASS
Actor Attribution = PASS
Migration = PASS for P0 scope
Browser E2E = PASS or FAIL
Restore = PASS or BLOCKED

Do not keep stale generic "PARTIAL" entries for certified P0 controls.

---

43. FINAL CERTIFICATION RUNNER

Run:

pnpm run certify:production-candidate

It must output all eight P0s:

P0-01 Tenant / IDOR ........ PASS
P0-02 Origin / CSRF ........ PASS
P0-03 Schema Upgrade ....... PASS
P0-04 Backup / Restore ..... PASS/BLOCKED
P0-05 Browser E2E .......... PASS/FAIL
P0-06 Role / Permission .... PASS
P0-07 Idempotency .......... PASS
P0-08 Actor Attribution .... PASS

---

44. P0 COUNT MUST BE GENERATED FROM GATE RESULTS

Do not hardcode:

P0 OPEN = 2

Calculate from current gate status.

---

45. RELEASE DECISION

If:

P0 OPEN > 0

then:

NOT READY

If:

P0 OPEN = 0

then:

PRODUCTION CANDIDATE

for current non-executing scope.

---

46. UPDATE SINGLE AUTHORITATIVE COMPLETION REPORT

Update only:

docs/CAPITAL_OS_FINAL_COMPLETION_REPORT_2026-09-02.md

as the overarching report.

Also update existing supporting matrices/release gate as necessary.

Do not create another executive/final report.

---

47. FINAL REPORT MUST STATE EXPLICITLY

P0 START
P0 CLOSED THIS RUN
P0 OPEN
P1 OPEN

and all eight gate statuses.

---

48. FINAL REPORT CONSISTENCY

The following must agree:

P0 Mapping
Security Certification
Test Inventory
Release Gate
Remaining Risks
Resolved During Hardening
Final Determination
Certification Runner

---

49. FINAL RESPONSE TO ME

Return only:

FINAL DECISION:
<NOT READY / PRODUCTION CANDIDATE>

P0 START:
2

P0 CLOSED THIS RUN:
<number>

P0 OPEN:
<number>

P0-01 TENANT / IDOR:
PASS

P0-02 ORIGIN / CSRF:
PASS

P0-03 EXISTING-SCHEMA UPGRADE:
PASS

P0-04 BACKUP / RESTORE:
<PASS / BLOCKED / FAIL>

P0-05 AUTHENTICATED BROWSER:
<PASS / FAIL>

P0-06 ROLE / PERMISSION:
PASS

P0-07 IDEMPOTENCY:
PASS

P0-08 ACTOR ATTRIBUTION:
PASS

BROWSER FAILURE ROOT CAUSE:
<resolved summary or remaining defect>

BACKUP / RESTORE:
<status>

P1 OPEN:
<number>

CERTIFICATION EXIT:
<number>

ALL REPLIT-CONTROLLABLE P0:
<CERTIFIED / NOT CERTIFIED>

USER ACTION REQUIRED:
<exact restore action or NONE>

FINAL DOCUMENT:
docs/CAPITAL_OS_FINAL_COMPLETION_REPORT_2026-09-02.md

---

FINAL DIRECTIVE

Capital OS no longer needs another generalized hardening sprint.

Six of eight P0 gates are certified.

The remaining engineering task is:

Fix the authenticated browser lifecycle.

The remaining infrastructure task is:

Execute a real managed backup → isolated restore.

Close those two gates.

Do not add anything else.

The target is now:

P0 OPEN:
2 → 1 → 0