Replit Prompt — Capital OS Final Infrastructure Provisioning & Certification Execution

Continue working on the existing Capital OS repository.

Use:

docs/CAPITAL_OS_FINAL_COMPLETION_REPORT_2026-09-02.md

as the authoritative current-state document.

The current release decision is:

NOT READY

with:

10 P0 blockers open

This sprint is NOT for adding more architecture, more speculative tests, or more product features.

The repository already contains substantial certification tooling.

The objective is now:

PROVISION THE REQUIRED CERTIFICATION ENVIRONMENTS AND EXECUTE THE EVIDENCE THAT ALREADY EXISTS

Do not spend this sprint merely writing more tests that remain unexecuted.

---

1. PRIMARY DIRECTIVE

The sequence is now:

PROVISION
↓
CONFIGURE
↓
EXECUTE
↓
VERIFY
↓
RECORD
↓
CLOSE GATE

not:

DESIGN
↓
WRITE MORE SCAFFOLDING
↓
MARK BLOCKED

---

2. DO NOT ADD NEW PRODUCT FEATURES

Do NOT build:

- Debt/Credit Engine
- new Treasury capabilities
- banking
- ACH
- brokerage connectivity
- blockchain integration
- live Micro-Live execution
- autonomous AI capabilities
- additional property modules
- new Strategy Lab strategies

Only work required to execute current certification evidence is allowed.

---

3. DETERMINE WHAT INFRASTRUCTURE REPLIT CAN PROVISION DIRECTLY

Inspect the current Replit project/environment and determine whether Replit can safely provide:

Dedicated certification PostgreSQL database
Disposable PostgreSQL database
Staging deployment
Clerk-compatible test deployment
Browser E2E deployment URL
Managed production/staging backup access
Isolated restore database
Environment-specific secrets

Use existing Replit-native capabilities where available.

Do not assume something is unavailable without checking the actual environment.

---

4. CREATE A CERTIFICATION ENVIRONMENT INVENTORY

Before execution, determine:

CERTIFICATION POSTGRES:
AVAILABLE / MISSING

OLD-SCHEMA TEST POSTGRES:
AVAILABLE / MISSING

RESTORE TARGET:
AVAILABLE / MISSING

CLERK TEST ENVIRONMENT:
AVAILABLE / MISSING

BROWSER TEST URL:
AVAILABLE / MISSING

MANAGED BACKUP ACCESS:
AVAILABLE / MISSING

Do not create another standalone report for this.

Record it in:

docs/CAPITAL_OS_FINAL_COMPLETION_REPORT_2026-09-02.md

---

5. DEDICATED CERTIFICATION DATABASE

The repository already contains guarded migration tooling.

Provide an isolated PostgreSQL URL through:

CAPITAL_OS_CERTIFICATION_DB_URL

It must NOT equal:

DATABASE_URL

The database must be:

Disposable
Non-production
Non-shared-development
Resettable

---

6. EXECUTE MIGRATION CERTIFICATION

Run:

CAPITAL_OS_CERTIFICATION_DB_URL=<dedicated-certification-db>
CAPITAL_OS_CERTIFICATION_ALLOW_RESET=1
pnpm run certify:migrations

Do not merely confirm that the command exists.

Actually execute it.

Record:

Exit code
Applied migration version
Schema verification
Representative tables
Readiness result
Test result

---

7. CLEAN DATABASE PASS CONDITION

The following sequence must execute:

EMPTY POSTGRES
↓
RESET / BASELINE
↓
CURRENT SCHEMA
↓
APPLICATION CONNECT
↓
READINESS PASS
↓
FIXTURES
↓
CERTIFICATION TESTS

Verify at minimum:

users
households
household_members
accounts
goals
ledger
treasury
properties
business
strategies
accounting
operations
audit
micro_live

or their actual current schema names.

---

8. OLD-SCHEMA UPGRADE ENVIRONMENT

Provision another disposable PostgreSQL database or use a separate isolated schema/database.

Construct the representative older schema required by the current certification tooling.

Populate meaningful records for:

Users
Households
Memberships
Accounts
Goals
Contributions
Ledger
Treasury
Property
Business
Strategies
Accounting
Operations
Audit

Then execute the upgrade to current schema.

---

9. OLD-SCHEMA DATA PRESERVATION

Compare before and after:

Row counts
Account balances
Goal progress
Protected balances
Ledger debit totals
Ledger credit totals
Business ownership
Strategy status
Audit event count

Require:

NO UNEXPLAINED DATA LOSS

If there is a mismatch:

FAIL THE GATE

and fix the migration.

---

10. SCHEMA COMPATIBILITY READINESS

If feasible with the existing deployment model, enhance readiness so it can distinguish:

DATABASE REACHABLE

from:

DATABASE SCHEMA COMPATIBLE

Capital OS should not advertise readiness against an unsupported schema.

Do not overengineer this if Replit Publish already provides an equivalent authoritative deployment boundary.

Document the actual control.

---

11. RUN THE DATABASE-BACKED CERTIFICATION SUITE AS A REQUIRED TEST

The current repository has a database-backed fixture that may be skipped without the certification environment.

For this sprint, do not count:

SKIPPED

as success.

Run the real PostgreSQL fixture using the dedicated certification database.

---

12. COMPLETE TENANT / IDOR EXECUTION

The test matrix already exists.

Execute every currently implemented:

A → A
A → B
B → A
Foreign Parent
Malformed ID
Mass Assignment

case.

Expand only where the existing route matrix identifies an uncovered caller-controlled identifier.

Do not add unrelated coverage.

---

13. P0 TENANT GATE

The tenant gate can close only when:

Every high-value caller-controlled identifier
has executable household-isolation evidence.

No high-value entry may remain:

UNEXECUTED
UNKNOWN

---

14. EXECUTE THE COMPLETE ROLE MATRIX

Use real PostgreSQL fixtures for:

Owner
Partner
Advisor
Viewer

Execute the currently implemented role paths.

Verify:

Owner allowed actions
Partner allowed/denied actions
Advisor allowed/denied actions
Viewer denied writes

against the actual route stack.

---

15. EXECUTE EFFECTIVE PERMISSION PRECEDENCE

Prove:

ROLE ALLOWS
+
MEMBERSHIP REVOKES
=
DENY

and any supported explicit grant behavior.

Do not certify based on source inspection.

Query the actual HTTP result.

---

16. EXECUTE ACTOR AUDIT VERIFICATION

For allowed actions by:

Owner
Partner
Advisor

query the resulting audit rows.

Verify:

actorUserId
=
actual authenticated test actor

for all currently implemented representative actions.

At minimum:

Contribution
Transfer
Capital Request
Capital Approval
Business Write
Property Write
Recommendation Decision
Emergency Stop
Strategy Action

where the role is permitted.

---

17. EXECUTE FULL ORIGIN / CSRF SUITE

The repository now has multiple origin/CSRF scenarios.

Execute all currently implemented tests.

Required coverage:

POST
PUT
PATCH
DELETE

against:

same origin
explicit allow
disallowed
malformed
missing
cross-site credentialed request

Any unexpected state-changing success:

FAIL

---

18. EXECUTE HIGH-CONTENTION TRANSFER TEST

The report states the path is implemented but not yet executed.

Run the actual test against dedicated PostgreSQL:

Starting balance = $1,000

100 simultaneous transfer attempts
$25 each

Required:

Successful count <= 40
Final source balance >= $0
No overdraft

---

19. RECONCILE THE CONTENTION LEDGER

After execution query persisted state.

Require:

Successful API transfer count
=
Transfer records
=
Ledger transactions

and:

Total debits
=
Total credits

No:

orphan debit
orphan credit
duplicate transaction

---

20. EXECUTE ALL IMPLEMENTED IDEMPOTENCY TESTS

Execute concurrent duplicate requests for all event types currently implemented.

At minimum:

Contribution
Transfer

Then implement/execute the remaining required P0 types:

Capital Request
Business Distribution Preparation

Use real concurrent requests and separate DB connections.

Expected:

ONE ECONOMIC EVENT

for each idempotency key.

---

21. VERIFY ECONOMIC EVENT ROWS

For every idempotency test query:

Primary record count
Ledger record count where applicable
Audit record count
Idempotency record count

Confirm no duplicate economic effect.

---

22. CLERK TEST ENVIRONMENT

Determine whether the current Clerk account/configuration supports:

development test users
test-mode authentication
provider-supported E2E authentication
reverification / step-up

Use Clerk-supported testing mechanisms.

Do not build custom authentication.

---

23. CONFIGURE AUTHENTICATED E2E ENVIRONMENT

Provision/configure:

Frontend test deployment
API test deployment
Clerk development/test instance
Allowed test origin
Test users

Use environment-specific secrets.

Never commit credentials.

---

24. EXECUTE AUTHENTICATED BROWSER E2E

Use the existing/preferred browser framework.

Run:

Sign Up
↓
Authenticate
↓
Create Household
↓
Dashboard
↓
Contribution
↓
Reload
↓
Sign Out
↓
Sign In
↓
Verify Same State

---

25. VERIFY NEW USER IS CLEAN

A newly authenticated household must NOT receive:

Morgan demo balances
Morgan business records
Morgan goals
Morgan strategies
Morgan property data

unless explicitly created as demo content.

---

26. VIEWER BROWSER E2E

Authenticate as Viewer.

Attempt a prohibited write via:

UI
Direct HTTP request

Both must be denied.

---

27. CROSS-HOUSEHOLD BROWSER E2E

Authenticate Household A user.

Attempt:

URL manipulation
object-ID substitution
direct API call

against Household B.

Require:

DENIED

---

28. BROWSER PERSISTENCE PROOF

For every critical persisted write exercised:

WRITE
↓
RELOAD
↓
FRESH API READ

must return the same authoritative result.

---

29. CLERK STEP-UP

If the configured Clerk environment exposes provider-supported reauthentication/reverification:

configure and test it now.

Replace temporary issued-at-only certification for high-risk actions with the provider-supported mechanism.

---

30. STEP-UP TEST

Prove:

Authorized Owner
+
insufficient/stale verification
→ STEP_UP_REQUIRED

then:

successful provider reverification
→ ALLOW

And:

Viewer
+
fresh reverification
→ STILL DENIED

---

31. IF CLERK STEP-UP CANNOT BE CONFIGURED

Do not fake it.

Record:

STEP-UP = BLOCKED

with the exact provider/configuration action needed from the project owner.

This remains P1 unless it directly blocks a P0 browser route in the current gate.

---

32. MANAGED DATABASE BACKUP

Inspect the actual production/staging PostgreSQL provider available in Replit.

If backup access is available:

create or identify a real managed backup.

Record:

backup reference
timestamp
schema/release
retention

No credentials in documentation.

---

33. ISOLATED RESTORE

Provision an isolated restore target.

Restore the managed backup.

Never overwrite production.

---

34. RESTORE CERTIFICATION

Point a test application instance at the restored database.

Run:

Readiness
Tenant isolation
Membership resolution
Ledger balance
Protected capital checks
Treasury checks
Business ownership
Strategy state
Accounting checks
Audit checks
Micro-Live disabled checks

---

35. RPO / RTO

Record actual observed:

Backup timestamp
Restore start
Restore complete
Verification complete
Observed RPO
Observed RTO

Do not estimate if actual measurements exist.

---

36. IF REPLIT CANNOT EXECUTE MANAGED BACKUP/RESTORE

This is important.

Do not simply say:

blocked

without providing an actionable requirement.

In the Final Completion Report state exactly:

USER ACTION REQUIRED:
<exact action>

Examples:

Create an approved managed Postgres backup
Provide isolated restore database
Enable provider backup retention
Authorize staging restore

Use the actual Replit/provider terminology.

---

37. HUMAN-ACTION ESCALATION FORMAT

For every infrastructure requirement Replit cannot satisfy autonomously, include:

BLOCKER:
<name>

WHY REPLIT CANNOT EXECUTE:
<technical reason>

EXACT USER ACTION REQUIRED:
<specific step>

WHAT TO PROVIDE BACK:
<environment variable / resource / confirmation>

NEXT AUTOMATED COMMAND:
<command Replit will run afterward>

Do not use vague phrases such as:

needs infrastructure

---

38. CONTINUE AUTOMATICALLY WHERE POSSIBLE

If one blocker requires human/provider action:

do NOT stop all other certification work.

Continue executing every independent test that can run.

---

39. OPERATIONS

Do not make durable scheduler work a substitute for finishing P0 infrastructure evidence.

If P0 execution succeeds and time remains, retain P1 status for:

scheduler
queue
retry
dead-letter
metrics
alerting
audit shipping
rate limiter

unless there is actual executable evidence.

---

40. RE-RUN FULL CERTIFICATION COMMAND

At the end run:

pnpm run certify:production-candidate

Record:

exit code
passed tests
failed tests
skipped tests
blocked external gates

A skipped P0 test means its gate remains open.

---

41. UPDATE THE RELEASE GATE

Update:

docs/PRODUCTION_RELEASE_GATE.md

Only check items backed by executed evidence.

---

42. UPDATE THE EXISTING MATRICES

Update:

docs/TENANT_ISOLATION_ROUTE_MATRIX.md
docs/ROLE_CERTIFICATION_MATRIX.md
docs/UI_PERSISTENCE_TRUTH_MATRIX.md

Do not create replacements.

---

43. UPDATE THE SINGLE AUTHORITATIVE COMPLETION DOCUMENT

Update:

docs/CAPITAL_OS_FINAL_COMPLETION_REPORT_2026-09-02.md

Do not create another overarching report.

This remains the single completion record.

---

44. UPDATE THE P0 COUNT

The completion document currently reports ten P0 blockers.

After execution, recalculate the number from actual evidence.

Do not preserve:

P0 = 10

if some items are now fully certified.

Do not reduce the count for tests that only exist but have not run.

---

45. MOVE RESOLVED P0 ITEMS

If an item obtains complete evidence:

move it from:

Remaining Risks → P0

to:

Resolved During Hardening / Certification

Include:

test
command
database/browser environment
result

---

46. FINAL DECISION RULE

If:

P0 OPEN > 0

then:

FINAL DECISION = NOT READY

If:

P0 OPEN = 0

and all current non-executing scope requirements pass:

FINAL DECISION = PRODUCTION CANDIDATE

---

47. PRODUCTION CANDIDATE DOES NOT ENABLE REAL MONEY

Even if promoted:

do NOT enable:

Plaid production
ACH
Bank transfers
Brokerage
Live Micro-Live
Blockchain
Autonomous AI

Production Candidate applies only to the current non-executing family-capital scope.

---

48. FINAL COMPLETION REPORT MUST INCLUDE AN INFRASTRUCTURE SECTION

Add:

Certification Infrastructure

Include:

Certification Database
Old-Schema Database
Clerk Test Environment
Browser E2E Environment
Backup Source
Restore Target

For each:

AVAILABLE
EXECUTED
BLOCKED

---

49. FINAL COMPLETION REPORT MUST DISTINGUISH

IMPLEMENTED

from:

EXECUTED

from:

CERTIFIED

Example:

High-contention test:
IMPLEMENTED = YES
EXECUTED = NO
CERTIFIED = NO

Do not collapse these concepts.

---

50. FINAL RESPONSE TO ME

When finished, respond only:

FINAL DECISION:
<NOT READY / PRODUCTION CANDIDATE>

P0 OPEN:
<number>

P0 CLOSED THIS RUN:
<number>

CERTIFICATION DATABASE:
<EXECUTED / BLOCKED>

CLEAN MIGRATION:
<PASS / FAIL / BLOCKED>

OLD-SCHEMA UPGRADE:
<PASS / FAIL / BLOCKED>

TENANT / IDOR:
<PASS / PARTIAL / BLOCKED>

ROLE / PERMISSION:
<PASS / PARTIAL / BLOCKED>

ORIGIN / CSRF:
<PASS / PARTIAL / BLOCKED>

HIGH-CONTENTION TRANSFER:
<PASS / FAIL / BLOCKED>

IDEMPOTENCY:
<PASS / PARTIAL / BLOCKED>

ACTOR AUDIT:
<PASS / PARTIAL / BLOCKED>

AUTHENTICATED BROWSER:
<PASS / BLOCKED>

CLERK STEP-UP:
<PASS / PARTIAL / BLOCKED>

BACKUP / RESTORE:
<PASS / BLOCKED>

USER ACTION REQUIRED:
<none OR exact actions>

FULL CERTIFICATION:
<exit code / status>

FINAL DOCUMENT:
docs/CAPITAL_OS_FINAL_COMPLETION_REPORT_2026-09-02.md

Do not write another long chat response.

All details belong in the Final Completion Report.

---

FINAL DIRECTIVE

We already know what needs to be tested.

Stop creating proof that cannot run.

Provision the environment.

Execute the proof.

If Replit can create the required environment, do it.

If Replit cannot create it, identify the precise external action required and continue everything else.

The objective is to convert:

IMPLEMENTED BUT UNEXECUTED

into:

EXECUTED AND CERTIFIED

until:

P0 OPEN = 0

or until the remaining blocker is demonstrably outside Replit's control.