Replit Prompt — Capital OS Observability, Telemetry & Critical Alert Certification

Continue working on the existing Capital OS repository.

Use:

docs/CAPITAL_OS_CURRENT_CERTIFICATION.md

as the authoritative current certification.

Current relevant status:

Worker / Scheduler = PASS
Server Emergency Stop = PASS
Guardian = PASS
OMS = PASS
Reconciliation = PASS

Observability = PARTIAL
Backup / Restore = BLOCKED

The durable operations certification has already passed:

OR-01 through OR-24 = PASS
29 tests passed
0 failed
0 skipped

Do NOT rewrite or destabilize the operations subsystem.

This sprint has ONE objective:

TURN OBSERVABILITY FROM PARTIAL TO PASS WITH REAL TELEMETRY AND NAMED CRITICAL-ALERT DELIVERY

Do NOT:

- activate Micro-Live
- connect Schwab
- send brokerage orders
- enable ACH
- move money
- add autonomous AI execution
- weaken any existing fail-closed boundary

---

1. OBSERVABILITY PRINCIPLE

Capital OS must be able to answer, in real time:

Is the API healthy?
Is the database healthy?
Are workers alive?
Is the scheduler alive?
Is the queue backing up?
Are jobs failing?
Is the ledger balanced?
Is audit persistence healthy?
Is tenant isolation being denied unexpectedly?
Is execution stopped?
Is Guardian healthy?
Is reconciliation failing?
Is a provider failing?
Has a critical safety condition occurred?

Do not rely only on logs.

Implement machine-readable metrics.

---

2. METRICS ENDPOINT

Create an internal, authenticated/authorized metrics surface or provider exporter.

Prefer a standard format such as:

Prometheus/OpenMetrics

if compatible with the stack.

If another existing telemetry stack is already present, use it.

Do not expose private financial values in metrics.

---

3. CORE API METRICS

Expose counters/histograms/gauges for:

http_requests_total
http_request_errors_total
http_request_duration_ms

auth_failures_total
authorization_denials_total
tenant_denials_total

database_readiness
database_errors_total
database_query_duration_ms

Tag carefully.

Do not use:

household name
email
account number
user PII

as metric labels.

---

4. FINANCIAL-INTEGRITY METRICS

Add:

ledger_imbalance_total
idempotency_conflict_total
negative_balance_prevention_total
protected_capital_denial_total
safe_to_deploy_invariant_failure_total
audit_persistence_failure_total

A financial invariant breach should be visible immediately.

---

5. OPERATIONS METRICS

The Worker/Scheduler certification already established runtime metrics.

Export the certified metrics for:

queue_depth
oldest_pending_job_age
retry_queue_depth
dead_letter_count

active_worker_count
stale_worker_count
worker_heartbeat_age

job_execution_duration
job_failure_count
job_retry_count
recovered_job_count

scheduler_heartbeat
scheduler_leader
scheduler_lag
missed_schedule_count
schedule_recovery_count

Do not create a second incompatible metric vocabulary.

Reuse certified operations values.

---

6. EXECUTION-SAFETY METRICS

Expose:

execution_state
guardian_state
risk_governor_state
oms_unknown_orders
open_order_intents
reconciliation_mismatch_total
reconciliation_failure_total
execution_stop_total
execution_lock_total

No real trading is enabled.

These metrics certify the safety infrastructure only.

---

7. MICRO-LIVE METRICS

Even while disabled, expose safety status:

micro_live_enabled = 0
micro_live_eligible
micro_live_guardian_health
micro_live_reconciliation_health

Do not imply active execution.

---

8. PROVIDER METRICS

Create provider-neutral metrics:

provider_health
provider_request_errors_total
provider_request_duration_ms
provider_rate_limit_total
provider_auth_failure_total
provider_reconnect_total

Use fixture/simulated provider for certification.

Do not connect Schwab yet.

---

9. BANKING METRICS

Because banking remains read-only/provider-disabled:

track only lifecycle health:

bank_connection_status
bank_sync_errors_total
bank_sync_age
bank_webhook_failures_total
bank_duplicate_event_total
bank_replay_rejection_total

No money movement metrics are needed because money movement is disabled.

---

10. METRIC CARDINALITY CONTROL

Avoid high-cardinality labels such as:

transactionId
jobId
orderId
email
accountId
raw error message

Use low-cardinality dimensions:

route
status_class
job_type
provider
severity
execution_state

where safe.

---

11. STRUCTURED EVENTS

Retain structured logs and correlation IDs.

Ensure high-severity events include:

event type
severity
timestamp
correlationId
household-scoped internal reference where appropriate
component
error code

without leaking protected data.

---

12. ALERT ENGINE

Implement deterministic alert rules for at least:

Database readiness failure
Ledger imbalance
Audit persistence failure
Dead-letter queue > 0
Worker heartbeat stale
Scheduler heartbeat stale
Queue age above threshold
Repeated reconciliation failures
Guardian STOP / LOCK
Execution Control STOP / LOCK
Unexpected provider authentication failure
Safe-to-Deploy invariant failure

---

13. ALERT SEVERITIES

Use:

INFO
WARNING
HIGH
CRITICAL

Critical should include at minimum:

ledger imbalance
audit persistence failure during financial write
execution safety failure
Guardian unhealthy during execution-capable state
database unavailable for authoritative execution
tenant isolation anomaly

---

14. NAMED ALERT DESTINATION

This is mandatory.

Configure one real named alert destination supported by the environment.

Examples:

Slack channel
email distribution
Replit-supported notification target
approved webhook endpoint

Use whichever is actually available.

Do NOT mark this PASS unless a real destination receives a test alert.

---

15. DO NOT EXPOSE SECRETS

Alert destination credentials/webhooks must use secure secrets.

Never place:

webhook secret
API key
auth token
email credentials

in:

source code
logs
audit
frontend
AI prompt

---

16. SYNTHETIC CRITICAL ALERT

Create a controlled certification-only synthetic event.

Example:

CERTIFICATION_TEST_CRITICAL

Trigger the full alert pipeline.

Required evidence:

event created
rule evaluated
alert generated
delivery attempted
destination received alert
delivery status recorded

---

17. DELIVERY RECEIPT

Persist safe metadata for alert delivery:

alertId
rule
severity
createdAt
destinationName
deliveryAttempt
deliveryStatus
deliveredAt
errorCode if failed
correlationId

Do not store destination secrets.

---

18. RETRY ALERT DELIVERY

Simulate temporary destination failure.

Expected:

delivery failure
→ durable retry
→ later success

Use the now-certified durable operations infrastructure.

No silent alert loss.

---

19. DEAD-LETTER ALERT DELIVERY

If alert delivery repeatedly fails:

→ DEAD_LETTER

with operator visibility.

Do not create infinite retry loops.

---

20. ALERT DEDUPLICATION

Prevent alert storms.

Repeated same condition within configured window should:

aggregate
deduplicate
or suppress

according to policy.

Critical state transitions should still be recorded.

---

21. ALERT RECOVERY

Support resolution notifications where useful.

Example:

DATABASE_NOT_READY
→ CRITICAL

database recovered
→ RESOLVED

This makes incidents operationally usable.

---

22. HEALTH DASHBOARD

Add or enhance internal system-health UI.

Show:

API
Database
Workers
Scheduler
Queue
Audit
Ledger
Guardian
Execution Control
Reconciliation
Provider
Alerts

Use status:

HEALTHY
DEGRADED
UNHEALTHY
UNKNOWN

Do not use financial data in operational status cards.

---

23. QUEUE VISIBILITY

Show:

queue depth
oldest pending
retries
dead letters
active workers
stale workers
scheduler lag

using live metrics.

---

24. EXECUTION HEALTH VIEW

Show:

Execution State
Guardian
OMS
Reconciliation
Risk Governor
Micro-Live status

Micro-Live must still display:

DISABLED

---

25. METRICS PERSISTENCE

Do not require indefinite raw time-series storage inside PostgreSQL if a telemetry provider is available.

But maintain enough durable incident/alert state to explain:

what happened
when
why
what alerted
whether delivery succeeded

---

26. TELEMETRY EXPORTER

Configure actual telemetry export.

Possible destinations:

Prometheus-compatible scraper
OpenTelemetry collector
Replit-supported monitoring
another approved telemetry backend

Pick one that exists in this environment.

Do not mark PASS if metrics exist only in code without export/scrape evidence.

---

27. EXPORT CERTIFICATION

Prove at least these metrics reach the exporter/backend:

API requests
DB readiness
queue depth
worker heartbeat
scheduler heartbeat
dead letters
Guardian state
Execution Control state
reconciliation failures

---

28. NO PII IN TELEMETRY

Scan exported metric labels/log fields.

Fail certification if telemetry contains:

email
full account number
SSN
bank token
Schwab token
secret
raw financial document

---

29. FAILURE INJECTION

Generate controlled failures for certification:

database unavailable
worker stale
scheduler stale
dead-letter present
reconciliation mismatch
Guardian STOP
audit failure simulation

Verify metric changes and alert behavior.

---

30. CRITICAL INCIDENT: LEDGER

Inject a safe test-only ledger invariant violation in isolated certification context.

Expected:

ledger_imbalance metric increments
CRITICAL alert generated
alert delivered
incident recorded

Do not corrupt shared/production data.

---

31. CRITICAL INCIDENT: EXECUTION

Simulate Guardian STOP while execution control is eligible.

Expected:

Guardian metric changes
execution blocked
critical/high alert generated according to policy

No order is sent.

---

32. CRITICAL INCIDENT: WORKER

Stop a worker until heartbeat becomes stale.

Expected:

stale_worker_count increases
alert fires
replacement/recovery proceeds

---

33. CRITICAL INCIDENT: SCHEDULER

Stop scheduler.

Expected:

scheduler heartbeat stale
scheduler lag rises
alert fires

Then restart and verify recovery.

---

34. CRITICAL INCIDENT: AUDIT

Simulate audit persistence failure for a certification-only protected workflow.

Expected:

audit_persistence_failure metric
alert
fail-closed behavior according to policy

---

35. ALERT AUTHORIZATION

Only authorized internal users may:

view alert details
acknowledge
resolve/manual-note
reprocess delivery

Viewer permissions should follow existing centralized role policy.

---

36. ALERT AUDIT

Audit:

acknowledge
resolve
manual replay
rule configuration change
destination configuration change

with real actor attribution.

---

37. OBSERVABILITY CONFIG CHANGES

Critical alert thresholds and destination changes must be server-side persisted.

Frontend cannot silently change operational safety policy.

---

38. CERTIFICATION COMMAND

Create:

pnpm run certify:observability

Required gates:

OB-01 Metrics Endpoint
OB-02 API Metrics
OB-03 Database Metrics
OB-04 Financial Integrity Metrics
OB-05 Operations Metrics
OB-06 Execution Safety Metrics
OB-07 Provider Metrics
OB-08 Telemetry Export
OB-09 Cardinality Safety
OB-10 PII/Secret Safety
OB-11 Alert Rule Engine
OB-12 Named Destination
OB-13 Synthetic Critical Delivery
OB-14 Delivery Retry
OB-15 Delivery Dead Letter
OB-16 Alert Deduplication
OB-17 Recovery Notification
OB-18 Alert Persistence
OB-19 Alert Authorization
OB-20 Alert Audit
OB-21 Failure Injection
OB-22 Ledger Critical Alert
OB-23 Worker Critical Alert
OB-24 Scheduler Critical Alert
OB-25 Execution Safety Alert

No critical skips.

---

39. ISOLATED CERTIFICATION

Use a disposable certification environment where failures can be safely injected.

Do not intentionally break shared production state.

---

40. REGRESSION

After observability certification run:

pnpm run typecheck
PostgreSQL integration suite
pnpm run certify:execution-control
pnpm run certify:operations-recovery
pnpm run certify:micro-live
production-candidate certification

Do not regress:

Worker/Scheduler
Emergency Stop
Guardian
OMS
Reconciliation
Manual Finance
Treasury
Tenant boundaries

---

41. UPDATE CURRENT CERTIFICATION

Update:

docs/CAPITAL_OS_CURRENT_CERTIFICATION.md

Change:

Observability = PASS

only if the exporter and real named alert delivery are actually demonstrated.

Otherwise remain:

PARTIAL

and list the exact unmet OB gates.

---

FINAL RESPONSE

Return:

CURRENT HEAD:
<hash>

OBSERVABILITY:
PASS / PARTIAL / FAIL

OB GATES:
<passed>/25

METRICS ENDPOINT:
PASS / FAIL

TELEMETRY EXPORT:
PASS / FAIL

API METRICS:
PASS / FAIL

DATABASE METRICS:
PASS / FAIL

FINANCIAL INTEGRITY METRICS:
PASS / FAIL

QUEUE METRICS:
PASS / FAIL

WORKER METRICS:
PASS / FAIL

SCHEDULER METRICS:
PASS / FAIL

EXECUTION METRICS:
PASS / FAIL

ALERT ENGINE:
PASS / FAIL

NAMED ALERT DESTINATION:
PASS / FAIL

SYNTHETIC CRITICAL ALERT:
PASS / FAIL

DELIVERY RETRY:
PASS / FAIL

ALERT DEAD LETTER:
PASS / FAIL

ALERT DEDUPLICATION:
PASS / FAIL

RECOVERY NOTIFICATION:
PASS / FAIL

PII / SECRET TELEMETRY SCAN:
PASS / FAIL

LEDGER CRITICAL ALERT:
PASS / FAIL

WORKER CRITICAL ALERT:
PASS / FAIL

SCHEDULER CRITICAL ALERT:
PASS / FAIL

EXECUTION SAFETY ALERT:
PASS / FAIL

REAL ORDERS SENT:
0

MICRO-LIVE:
DISABLED

SCHWAB:
DISABLED

AI EXECUTION:
DISABLED

CURRENT CRITICAL GATE COUNTS:
PASS <n>
PARTIAL <n>
BLOCKED <n>
FAIL <n>

PRODUCTION CANDIDATE:
<actual status>

NEXT HIGHEST-PRIORITY BLOCKER:
<one exact blocker>

UPDATED CERTIFICATION:
docs/CAPITAL_OS_CURRENT_CERTIFICATION.md