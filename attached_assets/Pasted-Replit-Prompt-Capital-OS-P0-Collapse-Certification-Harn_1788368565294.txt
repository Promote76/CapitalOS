Replit Prompt — Capital OS P0 Collapse, Certification Harness Completion & External-Gate Isolation

Continue working on the existing Capital OS repository.

Use only this document as the authoritative current-state record:

docs/CAPITAL_OS_FINAL_COMPLETION_REPORT_2026-09-02.md

Current state:

RELEASE DECISION: NOT READY
P0 OPEN: 8
PRODUCTION CANDIDATE: DENIED

This sprint is NOT for new features.

Do NOT add:

Simply Wall St
Banking
Plaid
ACH
Brokerage
Live trading
Blockchain
Debt/Credit
New Strategy Lab functionality
Autonomous AI
New property modules

The objective is:

COLLAPSE THE EIGHT P0 GATES AS FAR AS POSSIBLE

and make the production-candidate certification command enforce every remaining P0, not merely external infrastructure blockers.

---

1. FIRST FIX: CERTIFICATION HARNESS MUST MATCH THE P0 TABLE

The Final Completion Report currently states:

P0 OPEN = 8

but also states that:

pnpm run certify:production-candidate

exits "2" only for four external gates.

That is not sufficient.

The certification command must represent the actual release gate.

Create a direct mapping:

P0-01 → Caller-Controlled Identifier / IDOR Matrix
P0-02 → Origin / CSRF Certification
P0-03 → Existing-Schema Upgrade
P0-04 → Managed Backup / Restore
P0-05 → Authenticated Browser Journey
P0-06 → Role / Effective-Permission HTTP Certification
P0-07 → Concurrent Idempotency Breadth
P0-08 → Actor Attribution Certification

Every P0 must produce one of:

PASS
FAIL
BLOCKED

Never silently omit one.

---

2. CERTIFICATION EXIT RULE

Change the certification runner so:

ALL P0 = PASS
→ exit 0

Anything else:

P0 FAIL > 0
or
P0 BLOCKED > 0
→ non-zero exit

The output must identify every remaining P0 individually.

Example:

P0 CERTIFICATION

P0-01 Tenant/IDOR ............... PASS
P0-02 Origin/CSRF ............... PASS
P0-03 Existing Schema Upgrade ... BLOCKED
P0-04 Backup/Restore ............ BLOCKED
P0-05 Browser E2E ............... BLOCKED
P0-06 Roles/Permissions ......... PASS
P0-07 Idempotency ............... PASS
P0-08 Audit Attribution ......... PASS

P0 OPEN: 3

RESULT:
NOT READY

---

3. DO NOT COUNT TEST IMPLEMENTATION AS PASS

Required distinction:

IMPLEMENTED
EXECUTED
CERTIFIED

Only:

CERTIFIED = YES

closes a P0.

---

4. P0-01 — COMPLETE CALLER-CONTROLLED IDENTIFIER MATRIX

Finish:

docs/TENANT_ISOLATION_ROUTE_MATRIX.md

Enumerate all 108 route/method pairs.

For every route identify:

path IDs
query IDs
body IDs
parent foreign keys
child foreign keys
household-owned object references

Classify each:

NO CALLER-CONTROLLED ID

HOUSEHOLD-OWNED CALLER ID

GLOBAL / NON-TENANT ID

SERVER-OWNED / INTERNAL

---

5. EXECUTE EVERY HOUSEHOLD-OWNED ID TEST

For each household-owned identifier execute:

Household A → A
Household A → B
Household B → A
Malformed ID
Nonexistent ID
Foreign Parent ID
Mass Assignment
Unauthorized Role

Required cross-household result:

DENIED

and prove:

No object leak
No mutation
No child creation
No ledger activity
No ownership mutation
No partial transaction

---

6. FULL MASS-ASSIGNMENT MATRIX

Attack where relevant:

householdId
userId
ownerId
actorUserId
role
permissions
protected
protectedCapitalLocked
financialAuthority
liveEnabled
businessId
goalId
strategyId
propertyId
parentId

Caller input must not override server identity/security context.

---

7. P0-01 CLOSURE REQUIREMENT

The route matrix may contain:

PASS
NOT APPLICABLE

but not:

UNTESTED
UNKNOWN
UNREVIEWED

for any high-value household resource.

If this condition is met:

P0-01 = PASS

---

8. P0-06 — COMPLETE ROLE / PERMISSION MATRIX

Finish:

docs/ROLE_CERTIFICATION_MATRIX.md

Execute actual HTTP/database-backed tests for:

Owner
Partner
Advisor
Viewer

---

9. ROLE ACTIONS

Cover at minimum:

View finances
Edit budget
Create contribution
Create transfer
Submit capital request
Approve capital request
Treasury policy
Business write
Property write
Strategy create/edit
Strategy promotion
Strategy review
Accounting access
Operations
Emergency Stop
Household management
Membership changes
Permission changes
Micro-Live review
Micro-Live arming boundary

Frontend visibility is not certification.

Use the actual:

auth
→ request context
→ permission check
→ route
→ service
→ PostgreSQL

stack.

---

10. EFFECTIVE PERMISSION PRECEDENCE

Explicitly certify:

ROLE DEFAULT ALLOW
+
MEMBERSHIP REVOCATION
=
DENY

Also test the actual implemented explicit-grant behavior.

Document the exact rule.

Do not leave grant/revoke semantics implicit.

---

11. PERMISSION MASS-ASSIGNMENT ATTACK

Attempt to submit:

role=owner
permissions=[...]
householdId=<foreign>

through unrelated mutations.

Expected:

NO SECURITY CONTEXT CHANGE

---

12. P0-06 CLOSURE

Require complete HTTP proof for every security-significant role/action combination defined in the role matrix.

Then:

P0-06 = PASS

---

13. P0-08 — FULL ACTOR ATTRIBUTION

Existing evidence already covers:

Contribution
Transfer
Capital Request
Business Distribution

Expand persisted audit verification.

Use:

Owner
Partner
Advisor

where permitted.

---

14. ACTOR-AUDIT ACTIONS

Certify applicable actions for:

Budget write
Contribution
Transfer
Capital Request creation
Capital Request approval
Business write
Property write
Strategy write/review
Recommendation decision
Emergency Stop
Operations action
Household/member action

For each query the persisted audit record.

Require:

actorUserId = authenticated actor

not household owner unless that owner actually acted.

---

15. AUDIT RECORD CONSISTENCY

Where fields exist verify:

householdId
actorUserId
actorRole
action
entityType
entityId
correlationId
timestamp

are consistent.

---

16. P0-08 CLOSURE

If every representative security/financial action records the authenticated actor correctly:

P0-08 = PASS

---

17. P0-07 — COMPLETE ECONOMIC IDEMPOTENCY MATRIX

Already certified:

Contribution
Transfer
Capital Request
Business Distribution Preparation

Do not reopen these unless regression occurs.

Now enumerate every remaining economic workflow that accepts or should accept an idempotency key.

Examples to inspect:

Allocation
Deallocation
Capital allocation request
Treasury review/write
Business contribution
Business transfer
Business adjustment
Strategy capital allocation
Financial adjustment
Other ledger-producing mutation

Do not invent idempotency requirements for purely read-only/advisory records.

---

18. SAME-KEY TEST

For each applicable economic workflow:

N concurrent requests
same idempotency key
same payload

must produce:

ONE ECONOMIC EVENT

---

19. IDEMPOTENCY MISMATCH TEST

Critical addition:

same idempotency key
+
different economic payload

must NOT silently reuse or overwrite an unrelated economic event.

Expected behavior should be one of:

409 IDEMPOTENCY_CONFLICT
or equivalent explicit deterministic rejection

Document actual API contract.

---

20. IDEMPOTENCY EVIDENCE

Verify:

Primary record count
Ledger rows
Audit rows
Idempotency rows
Balances

No duplicated financial effect.

---

21. CROSS-PROCESS / CONNECTION PROOF

Use real:

parallel HTTP requests
separate DB connections
PostgreSQL

not only sequential requests.

---

22. P0-07 CLOSURE

Once every economic idempotent workflow has same-key replay and mismatch proof:

P0-07 = PASS

---

23. P0-02 — COMPLETE ORIGIN / CSRF CERTIFICATION

Current middleware coverage exists.

Production allowed origin is configured as:

https://capital-os-fund.replit.app

Republish first if required.

Then run production-like authenticated route tests.

---

24. ORIGIN MATRIX

For representative state-changing protected routes:

POST
PUT
PATCH
DELETE

test:

Correct origin
Explicit allowed origin
Disallowed origin
Malformed origin
Missing origin
Cross-site credentialed request

---

25. TEST MULTIPLE DOMAINS

Do not test only one endpoint.

Exercise representative:

financial write
business write
property write
governance/security write

to prove middleware coverage is global.

---

26. ORIGIN EXPECTATION

Required browser behavior:

Allowed production origin → route proceeds to auth/authorization
Disallowed origin → DENY
Malformed origin → DENY
Cross-site credentialed → DENY
Missing required browser origin → FAIL CLOSED

Document intentional machine/server exception, if any.

---

27. P0-02 CLOSURE

If production-like authenticated HTTP coverage proves the policy across supported state-changing methods:

P0-02 = PASS

Do not keep it P0 merely because every possible API route is not individually repeated if middleware is globally mounted and representative route-level proof establishes enforcement.

---

28. P0-03 — EXISTING-SCHEMA UPGRADE

Before escalating to the user, exhaust Git/repository history.

Search:

git log
git show
git diff
schema.ts history
Drizzle history
deleted migrations
initialization scripts
seed history
old branches/tags if available

---

29. IDENTIFY A REAL HISTORIC SCHEMA

Recover an actual prior Capital OS schema if available.

Document:

Commit
Date
Schema path
Major differences
Why it represents a valid historical release state

---

30. USE EXISTING OLD-SCHEMA CERTIFICATION DATABASE

Use:

old-schema-certification

isolated Neon branch.

Do not touch production or shared development.

---

31. HISTORICAL DATASET

Create representative data using the historical schema.

Capture pre-upgrade:

Household count
User count
Membership count
Account balances
Protected balances
Goal progress
Ledger debit/credit totals
Business ownership
Property state
Strategy state
Audit history

---

32. EXECUTE REAL UPGRADE

Perform:

HISTORIC SCHEMA
↓
CURRENT SCHEMA

without:

drop-and-recreate
manual row copying

unless a documented migration procedure explicitly requires transformation.

---

33. DATA PRESERVATION

Require:

No unexplained row loss
Balances preserved
Ledger preserved
Protected state preserved
Ownership preserved
No cross-household corruption
No duplicate records

---

34. IF NO HISTORICAL SCHEMA EXISTS

Only after exhaustive Git inspection may this remain blocked.

Record exactly what history was searched.

Then request one of:

historical database snapshot
historical SQL schema
approved representative legacy fixture

Do not fabricate release evidence.

---

35. P0-05 — AUTHENTICATED BROWSER E2E

Continue exploring Replit-managed Clerk Development authentication.

Do not request passwords or OTPs in chat.

Use approved secure test-user mechanisms.

---

36. REQUIRED JOURNEY

Execute:

Authenticate
↓
Internal User Resolution
↓
Create Household
↓
Owner Membership
↓
Dashboard
↓
Contribution
↓
Reload
↓
Fresh Read
↓
Sign Out
↓
Protected Route Denied
↓
Sign In
↓
Same Household
↓
Same Financial State

---

37. CLEAN-HOUSEHOLD CHECK

New user must not inherit demo:

balances
businesses
properties
strategies
goals

---

38. VIEWER E2E

Use Viewer account.

Attempt prohibited:

UI write
direct API write

Require denial.

---

39. CROSS-HOUSEHOLD E2E

Attempt:

URL modification
foreign object ID
direct API call

Require denial.

---

40. FALSE-SUCCESS E2E

Force API rejection.

Verify:

No false success toast
No stale optimistic financial state
Fresh read unchanged

---

41. P0-05 CLOSURE

If all authenticated critical journeys pass:

P0-05 = PASS

---

42. CLERK STEP-UP IS P1

Do not confuse:

Authenticated Browser E2E

with:

Provider-Supported Clerk Step-Up

The current report classifies provider step-up as P1, not P0.

Do not count it as one of the eight P0 gates unless the actual release policy has deliberately changed.

Fix any certification script that incorrectly treats P1 step-up as a P0 gate.

---

43. P0-04 — BACKUP / RESTORE

This is likely provider-controlled.

Inspect current Replit Database capabilities once more.

If safe isolated restore can be performed:

execute it.

Never restore over production.

---

44. RESTORE VERIFICATION

Against restored database test:

Identity
Households
Memberships
Accounts
Goals
Ledger
Protected Capital
Treasury
Business
Property
Strategies
Accounting
Audit
Micro-Live disabled state

---

45. RPO / RTO

Record actual:

Backup timestamp
Restore start
Restore completion
Verification completion
Observed RPO
Observed RTO

---

46. IF USER ACTION IS REQUIRED

Give one exact action block:

USER ACTION REQUIRED — DATABASE RESTORE

1. Open Replit Database.
2. Select an approved production point-in-time backup.
3. Restore it into a NEW isolated non-production database.
4. Do NOT overwrite production.
5. Make the isolated target available to the certification environment.

Adjust terminology to the actual Replit UI.

Then continue everything else.

---

47. REMOVE DUPLICATE P0 IF CLOSURE CRITERIA OVERLAP

Review the current eight P0s carefully.

Do not maintain separate P0 blockers that are actually the same missing evidence.

Example:

Full tenant matrix

may already include certain mass-assignment tests.

Do not create:

Tenant Matrix P0
Mass Assignment P0

unless the release gate intentionally defines both independently.

P0 count must correspond to actual independent release gates.

---

48. RECONCILE P0 COUNT WITH RELEASE GATE

At end:

Remaining P0 table
=
PRODUCTION_RELEASE_GATE P0 unchecked items
=
certify:production-candidate failing P0 gates

These three sources MUST agree.

---

49. P0 SOURCE OF TRUTH CONSISTENCY

There must not be:

Final Report says 8
Release Gate says 6
Certification command says 4

They must all report the same P0 count.

This is mandatory.

---

50. P1 SEPARATION

Keep P1 separately:

Provider-supported Clerk step-up
Durable scheduler / queue
Metrics / alerts
Shared rate limit
Cross-view accounting
Micro-Live failure durability

Do not allow a P1 to inflate the P0 count.

---

51. RE-RUN ALL CLOSED REGRESSIONS

Run:

Clean migration
Contribution IDOR
Transfer contention
Contribution idempotency
Transfer idempotency
Capital Request idempotency
Distribution idempotency
Ledger reconciliation
Origin middleware
OpenAPI parity
Micro-Live disabled tests

Do not redesign them.

---

52. FINAL CERTIFICATION COMMAND

Execute:

pnpm run certify:production-candidate

The command must output each P0 individually.

---

53. REQUIRED FINAL COMMAND OUTPUT

Example:

CAPITAL OS PRODUCTION CANDIDATE CERTIFICATION

P0-01 Tenant / IDOR ........ PASS
P0-02 Origin / CSRF ........ PASS
P0-03 Schema Upgrade ....... BLOCKED
P0-04 Backup / Restore ..... BLOCKED
P0-05 Browser E2E .......... BLOCKED
P0-06 Role / Permission .... PASS
P0-07 Idempotency .......... PASS
P0-08 Actor Attribution .... PASS

P0:
5 PASS
0 FAIL
3 BLOCKED

P0 OPEN:
3

P1 OPEN:
6

FINAL:
NOT READY

---

54. IF ALL INTERNAL P0 CLOSE

If the only remaining blockers are genuinely external:

state:

ALL REPLIT-CONTROLLABLE P0 CERTIFIED

Then identify exact external actions.

---

55. UPDATE ONLY EXISTING AUTHORITATIVE FILES

Update:

docs/CAPITAL_OS_FINAL_COMPLETION_REPORT_2026-09-02.md
docs/PRODUCTION_RELEASE_GATE.md
docs/TENANT_ISOLATION_ROUTE_MATRIX.md
docs/ROLE_CERTIFICATION_MATRIX.md
docs/UI_PERSISTENCE_TRUTH_MATRIX.md

Do not create another high-level report.

---

56. FINAL COMPLETION DOCUMENT

Update the single completion document to reflect:

Actual P0 Count
Actual P1 Count
Certification Harness Results
Tenant Matrix
Role Matrix
Permission Precedence
Origin Certification
Schema Upgrade
Restore
Browser E2E
Idempotency Breadth
Actor Attribution
External Actions

---

57. MOVE CLOSED P0s TO RESOLVED

When certified:

Remaining Risks → P0

must shrink.

Do not leave resolved items in both tables.

---

58. PRODUCTION CANDIDATE RULE

Only:

P0 OPEN = 0

permits:

PRODUCTION CANDIDATE

for the current non-executing family-capital scope.

---

59. DO NOT CLAIM FULL PRODUCTION READINESS

Production Candidate does not authorize:

bank transfers
ACH
live trading
blockchain
outside investor capital
autonomous AI

---

60. FINAL RESPONSE

Return only:

FINAL DECISION:
<NOT READY / PRODUCTION CANDIDATE>

P0 START:
8

P0 CLOSED THIS RUN:
<number>

P0 OPEN:
<number>

P0-01 TENANT / IDOR:
<PASS / FAIL / BLOCKED>

P0-02 ORIGIN / CSRF:
<PASS / FAIL / BLOCKED>

P0-03 EXISTING-SCHEMA UPGRADE:
<PASS / FAIL / BLOCKED>

P0-04 BACKUP / RESTORE:
<PASS / FAIL / BLOCKED>

P0-05 AUTHENTICATED BROWSER:
<PASS / FAIL / BLOCKED>

P0-06 ROLE / PERMISSION:
<PASS / FAIL / BLOCKED>

P0-07 IDEMPOTENCY:
<PASS / FAIL / BLOCKED>

P0-08 ACTOR ATTRIBUTION:
<PASS / FAIL / BLOCKED>

P1 OPEN:
<number>

FULL CERTIFICATION EXIT:
<number>

ALL REPLIT-CONTROLLABLE P0:
<CERTIFIED / NOT CERTIFIED>

USER ACTION REQUIRED:
<exact actions or NONE>

FINAL DOCUMENT:
docs/CAPITAL_OS_FINAL_COMPLETION_REPORT_2026-09-02.md

---

FINAL DIRECTIVE

The most important objective in this sprint is not writing more code.

It is making these three things identical:

FINAL COMPLETION REPORT
        =
PRODUCTION RELEASE GATE
        =
CERTIFICATION RUNNER

If the completion report says eight P0 blockers remain, the certification runner must know about eight P0 blockers.

Close every Replit-controllable one.

Use actual execution evidence.

Keep external/provider-controlled gates explicitly blocked.

Do not promote Capital OS until:

P0 OPEN = 0