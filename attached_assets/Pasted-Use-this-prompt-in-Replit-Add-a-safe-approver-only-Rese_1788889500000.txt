Use this prompt in Replit:

> Add a safe, approver-only Reset All Financial Evidence workflow to Capital OS so I can completely clear incorrectly uploaded financial documents and start over.

On /documents, add:

A clearly labeled Reset All Financial Evidence action.

Per-document Delete actions.

Strong destructive confirmation before either action executes.

For the bulk reset, require the approver to type a confirmation phrase such as RESET FINANCIAL EVIDENCE.

Show exactly how many documents and derived records will be affected before confirmation.


The reset must remove the household’s uploaded financial-document evidence and all document-derived artifacts associated with those uploads, including:

financial document records

document-to-business links

content-detection results

type-correction records

parser generations

duplicate/version identity reviews

canonical/duplicate/supersession relationships

settlement source records and settlement-derived lines/reconciliation

P&L source records and P&L-derived lines/reconciliation

other document-derived business-intelligence records created solely from those uploaded documents


The reset must not delete or modify:

household accounts

household ledger transactions

the existing $1,500 and $235 manual household inflows

budget categories or planning periods

Wells Fargo manual account state

the canonical Stevens Transport Independent Contractor BusinessEntity

business reserve configuration

owner-draw proposals unrelated to deleted documents

verified household-income events unrelated to deleted documents

household identity, memberships, permissions, or Clerk authentication

treasury, property, duplex-goal, strategy, or trading records


Preserve an immutable audit record of the destructive action containing the authenticated actor, household, timestamp, reason, document IDs/hashes affected, and counts of removed derived records. Do not silently merge, reclassify, or recreate deleted evidence.

The reset must be household-scoped, server-authorized, idempotent, and require approve permission. A non-approver should see the control disabled with a clear explanation rather than receiving a mutation error after clicking.

After a successful reset, /documents should show an empty financial-evidence state and allow clean re-upload of each file under the correct document type. /business should no longer show settlement/P&L evidence derived from deleted documents. /budget must remain unchanged except for any document-only evidence references that were removed.

Add a post-reset verification summary showing:

financial documents remaining

document detections remaining

type corrections remaining

parser generations remaining

identity reviews remaining

settlement source records remaining

P&L source records remaining

household ledger transactions remaining

canonical BusinessEntity still present


Do not use direct unaudited production database edits. Implement this through the normal authenticated application workflow with fail-closed behavior and tests for household isolation, permissions, partial failure rollback, idempotency, and preservation of non-document financial records.