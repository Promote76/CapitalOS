# Protected Capital Policy

Protected designations are enforced server-side and represented in the Protected Capital Registry. A UI action or scenario cannot bypass a locked designation.

Protected capital may be displayed to an authorized role, but it is not included in household Safe-to-Deploy merely because the physical account is liquid. Manual overrides are not available in the v2 API. Any future policy change must create a new version and preserve historical snapshots.