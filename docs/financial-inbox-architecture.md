# Financial Inbox

`financial_documents` is household-scoped immutable source evidence. Its SHA-256
unique key prevents the same uploaded bytes from creating duplicate evidence.
Settlement and P&L remain their existing domain records; `sourceRecordType` and
`sourceRecordId` link rather than recreate economic events. A document being
parsed never means verified. Every review is actor-attributed and audited.
