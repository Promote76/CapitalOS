openapi: 3.1.0
info:
  # Do not change the title, if the title changes, the import paths will be broken
  title: Api
  version: 0.3.0
  description: Capital OS household capital operating system API
servers:
  - url: /api
    description: Base API path
security:
  - clerkSession: []
tags:
  - name: health
  - name: auth
  - name: household
  - name: capital
  - name: planning
  - name: governance
  - name: blockchain
  - name: household-finance
  - name: treasury
  - name: accounting
  - name: operations
  - name: business
  - name: financing
  - name: observability
  - name: family-office
paths:
  /internal/metrics:
    get:
      operationId: getInternalMetrics
      tags: [observability]
      summary: Get authenticated low-cardinality OpenMetrics telemetry
      responses:
        "200":
          description: OpenMetrics text exposition
          content:
            application/openmetrics-text:
              schema:
                type: string
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /observability/incidents:
    get:
      operationId: listObservabilityIncidents
      tags: [observability]
      summary: List actor-scoped observability incidents
      responses:
        "200": { description: Persisted incidents }
  /observability/health:
    get:
      operationId: getObservabilityHealth
      tags: [observability]
      summary: Get aggregate queue, scheduler, execution, and incident health
      responses:
        "200": { description: Live aggregate observability health }
        "403": { $ref: "#/components/responses/Forbidden" }
  /observability/incidents/{incidentId}/deliveries:
    get:
      operationId: listObservabilityIncidentDeliveries
      tags: [observability]
      summary: List safe persisted delivery receipts for an actor-scoped incident
      parameters:
        - name: incidentId
          in: path
          required: true
          schema: { type: string, pattern: "^[0-9a-fA-F-]{36}$" }
      responses:
        "200": { description: Persisted delivery attempts and receipts }
        "403": { $ref: "#/components/responses/Forbidden" }
  /observability/incidents/{incidentId}/reprocess:
    post:
      operationId: reprocessObservabilityIncident
      tags: [observability]
      summary: Reprocess delivery for an actor-scoped incident
      parameters:
        - name: incidentId
          in: path
          required: true
          schema: { type: string, pattern: "^[0-9a-fA-F-]{36}$" }
      responses:
        "200": { description: Delivery replay result }
        "403": { $ref: "#/components/responses/Forbidden" }
  /observability/incidents/{incidentId}/resolve:
    post:
      operationId: resolveObservabilityIncident
      tags: [observability]
      summary: Resolve an observability incident with an actor-attributed note
      parameters:
        - name: incidentId
          in: path
          required: true
          schema: { type: string, pattern: "^[0-9a-fA-F-]{36}$" }
      responses:
        "200": { description: Resolved incident }
        "403": { $ref: "#/components/responses/Forbidden" }
  /observability/certification/critical:
    post:
      operationId: triggerObservabilityCertificationCritical
      tags: [observability]
      summary: Trigger a non-production, authorized synthetic critical alert
      responses:
        "201": { description: Incident and delivery result }
        "403": { $ref: "#/components/responses/Forbidden" }
  /healthz:
    get:
      security: []
      operationId: healthCheck
      tags: [health]
      summary: Health check
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"
  /health/live:
    get:
      security: []
      operationId: healthLive
      tags: [health]
      summary: Dependency-free liveness check
      responses:
        "200":
          description: Process is alive
          content:
            application/json:
              schema: { $ref: "#/components/schemas/HealthStatus" }
  /health/ready:
    get:
      security: []
      operationId: healthReady
      tags: [health]
      summary: PostgreSQL readiness check
      responses:
        "200":
          description: Dependencies are ready
        "503":
          description: A dependency is unavailable
  /auth/me:
    get:
      operationId: getAuthMe
      tags: [auth]
      summary: Resolve the authenticated Clerk user and household memberships
      responses:
        "200":
          description: Internal identity and memberships
        "401":
          $ref: "#/components/responses/Unauthorized"
  /auth/onboard:
    post:
      operationId: onboardHousehold
      tags: [auth]
      summary: Create the first household for an authenticated user
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/OnboardHouseholdInput" }
      responses:
        "201":
          description: Household created with conservative defaults
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          description: User already belongs to an active household
  /household:
    get:
      operationId: getHousehold
      tags: [household]
      summary: Get the current household
      responses:
        "200":
          description: Household workspace
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HouseholdSummary"
  /household/privacy:
    patch:
      operationId: updatePrivacySettings
      tags: [household]
      summary: Update household finance privacy controls
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/PrivacySettingsInput" }
      responses:
        "200":
          description: Updated household workspace
          content:
            application/json:
              schema: { $ref: "#/components/schemas/HouseholdSummary" }
        "403":
          $ref: "#/components/responses/Forbidden"
  /dashboard:
    get:
      operationId: getDashboard
      tags: [capital]
      summary: Get the household dashboard snapshot
      responses:
        "200":
          description: Dashboard data
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DashboardSnapshot"
  /accounts:
    get:
      operationId: listAccounts
      tags: [capital]
      summary: List internal capital accounts
      responses:
        "200":
          description: Accounts
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AccountSummary"
  /goals:
    get:
      operationId: listGoals
      tags: [capital]
      summary: List capital goals
      responses:
        "200":
          description: Goals
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/GoalSummary"
  /contributions:
    get:
      operationId: listContributions
      tags: [capital]
      summary: List household contributions
      responses:
        "200":
          description: Contributions
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ContributionSummary"
    post:
      operationId: createContribution
      tags: [capital]
      summary: Record a household contribution
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ContributionInput"
      responses:
        "201":
          description: Contribution recorded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ContributionSummary"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/Conflict"
  /transfers:
    post:
      operationId: createTransfer
      tags: [capital]
      summary: Create an internal transfer
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TransferInput"
      responses:
        "201":
          description: Transfer created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TransactionSummary"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/Conflict"
  /allocations/impact:
    post:
      operationId: previewAllocationImpact
      tags: [capital]
      summary: Preview an allocation rule change
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AllocationInput"
      responses:
        "200":
          description: Allocation impact
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AllocationImpact"
        "400":
          $ref: "#/components/responses/BadRequest"
  /allocations:
    put:
      operationId: updateAllocation
      tags: [capital]
      summary: Save an allocation rule after previewing its impact
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AllocationInput"
      responses:
        "200":
          description: Saved allocation rule
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AllocationSummary"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
  /portfolio:
    get:
      operationId: getPortfolio
      tags: [capital]
      summary: Get portfolio composition and ledger health
      responses:
        "200":
          description: Portfolio snapshot
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PortfolioSummary"
  /properties:
    get:
      operationId: getProperties
      tags: [planning]
      summary: Get property planning data
      responses:
        "200":
          description: Property planning
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PropertySummary"
    post:
      operationId: addPropertyNote
      tags: [planning]
      summary: Add a private property research note
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PropertyNoteInput"
      responses:
        "201":
          description: Note added
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PropertyNote"
        "403":
          $ref: "#/components/responses/Forbidden"
  /properties/underwriting:
    get:
      operationId: getPropertyUnderwriting
      tags: [planning]
      summary: Get the acquisition dashboard and underwriting data
      responses:
        "200":
          description: Property underwriting dashboard
          content:
            application/json:
              schema: { $ref: "#/components/schemas/PropertyUnderwriting" }
  /properties/buy-box:
    patch:
      operationId: updateBuyBox
      tags: [planning]
      summary: Update the household duplex buy box
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateBuyBoxInput" }
      responses:
        "200":
          description: Updated buy box
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BuyBox" }
        "403":
          $ref: "#/components/responses/Forbidden"
  /properties/candidates:
    post:
      operationId: createPropertyCandidate
      tags: [planning]
      summary: Add a property candidate to the acquisition pipeline
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreatePropertyCandidateInput" }
      responses:
        "201":
          description: Property candidate
          content:
            application/json:
              schema: { $ref: "#/components/schemas/PropertyCandidate" }
        "403":
          $ref: "#/components/responses/Forbidden"
  /properties/candidates/{candidateId}/analyze:
    post:
      operationId: analyzePropertyCandidate
      tags: [planning]
      summary: Calculate a property candidate underwriting package
      parameters:
        - name: candidateId
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Underwriting analysis
          content:
            application/json:
              schema: { $ref: "#/components/schemas/PropertyAnalysis" }
        "403":
          $ref: "#/components/responses/Forbidden"
  /strategies:
    get:
      operationId: listStrategies
      tags: [planning]
      summary: List strategy lifecycle records
      responses:
        "200":
          description: Strategies
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/StrategySummary"
  /strategy-lab:
    get:
      operationId: getStrategyLab
      tags: [planning]
      summary: Get the research-only Strategy Lab snapshot
      responses:
        "200":
          description: Strategy Lab snapshot
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StrategyLabSnapshot"
  /micro-live:
    get:
      operationId: getMicroLive
      tags: [planning]
      summary: Get the fail-closed Micro-Live execution monitor
      responses:
        "200":
          description: Micro-Live control-plane snapshot
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MicroLiveSnapshot" }
  /execution-control:
    get:
      operationId: getExecutionControl
      tags: [planning]
      summary: Get the household-scoped server execution control state
      responses:
        "200":
          description: Execution control state
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ExecutionControlState" }
  /execution-control/stop:
    post:
      operationId: requestExecutionStop
      tags: [planning]
      summary: Request an authoritative execution stop
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ExecutionControlCommandInput" }
      responses:
        "200":
          description: Confirmed execution state
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ExecutionControlState" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
  /execution-control/safe-mode:
    post:
      operationId: requestExecutionSafeMode
      tags: [planning]
      summary: Request an authoritative safe mode transition
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ExecutionControlCommandInput" }
      responses:
        "200":
          description: Confirmed execution state
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ExecutionControlState" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
  /execution-control/evacuate:
    post:
      operationId: requestExecutionEvacuate
      tags: [planning]
      summary: Request an authoritative evacuation state
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ExecutionControlCommandInput" }
      responses:
        "200":
          description: Confirmed execution state
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ExecutionControlState" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
  /execution-control/recover:
    post:
      operationId: recoverExecutionControl
      tags: [planning]
      summary: Recover a stopped or locked execution control to disabled
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ExecutionControlCommandInput" }
      responses:
        "200":
          description: Confirmed disabled execution state
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ExecutionControlState" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
  /micro-live/rehearsal:
    post:
      operationId: runMicroLiveRehearsal
      tags: [planning]
      summary: Run a no-transmission Micro-Live rehearsal
      responses:
        "201":
          description: Rehearsal result
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MicroLiveRehearsal" }
  /micro-live/enablement-review:
    post:
      operationId: reviewMicroLiveEnablement
      tags: [planning]
      summary: Review Micro-Live gates without enabling execution
      responses:
        "200":
          description: Enablement review
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MicroLiveEnablementReview" }
  /micro-live/venues/{venueId}/approve:
    post:
      operationId: approveMicroLiveVenue
      tags: [planning]
      summary: Record an owner-controlled real venue approval review
      parameters:
        - name: venueId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/MicroLiveVenueApprovalRequest" }
      responses:
        "200":
          description: Venue approval record
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MicroLiveVenueApproval" }
  /micro-live/venues/{venueId}/reviews/{kind}:
    post:
      operationId: recordMicroLiveVenueReview
      tags: [planning]
      summary: Record an independent venue security or jurisdiction review
      parameters:
        - name: venueId
          in: path
          required: true
          schema: { type: string }
        - name: kind
          in: path
          required: true
          schema: { type: string, enum: [security, jurisdiction] }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/MicroLiveVenueReviewRequest" }
      responses:
        "201":
          description: Independent review record
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MicroLiveVenueReview" }
  /micro-live/arm:
    post:
      operationId: armMicroLive
      tags: [planning]
      summary: Arm a Micro-Live session through the human-controlled boundary
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MicroLiveArmRequest"
      responses:
        "200":
          description: Armed session or fail-closed rejection
          content:
            application/json:
              schema:
                type: object
                required: [status, sessionId, authorizationExpiresAt, liveExecutionEnabled]
                properties:
                  status: { type: string }
                  sessionId: { type: string }
                  authorizationExpiresAt: { type: string, format: date-time }
                  liveExecutionEnabled: { type: boolean }
  /micro-live/orders:
    post:
      operationId: submitMicroLiveOrder
      tags: [planning]
      summary: Submit one authenticated Micro-Live order through the durable OMS
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/MicroLiveOrderRequest" }
      responses:
        "201":
          description: Durable order intent, provider acknowledgement, and reconciliation result
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MicroLiveOrderResponse" }
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/Conflict"
  /micro-live/orders/{orderIntentId}/cancel:
    post:
      operationId: cancelMicroLiveOrder
      tags: [planning]
      summary: Cancel one provider order through the durable OMS
      parameters:
        - name: orderIntentId
          in: path
          required: true
          schema: { type: string, pattern: "^[0-9a-fA-F-]{36}$" }
      responses:
        "200":
          description: Durable cancellation acknowledgement and reconciliation result
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MicroLiveOrderResponse" }
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
  /micro-live/first-fill/approve:
    post:
      operationId: approveMicroLiveFirstFillResume
      tags: [planning]
      summary: Approve resuming new orders after clean first-fill reconciliation
      responses:
        "200":
          description: First-fill hold approval
          content:
            application/json:
              schema:
                type: object
                required: [sessionId, approvedAt, approvedBy, allowNewOrders, liveExecutionEnabled]
                properties:
                  sessionId: { type: string }
                  approvedAt: { type: string, format: date-time }
                  approvedBy: { type: string }
                  allowNewOrders: { type: boolean }
                  liveExecutionEnabled: { type: boolean }
  /micro-live/reconciliation-runs:
    get:
      operationId: listMicroLiveReconciliationRuns
      tags: [planning]
      summary: List persisted Micro-Live reconciliation runs
      responses:
        "200":
          description: Reconciliation history
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/MicroLiveReconciliationRun" }
    post:
      operationId: runMicroLiveReconciliation
      tags: [planning]
      summary: Persist a venue-authoritative Micro-Live reconciliation run
      responses:
        "201":
          description: Reconciliation result
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MicroLiveReconciliationRun" }
  /micro-live/position-snapshots:
    get:
      operationId: listMicroLivePositionSnapshots
      tags: [planning]
      summary: List persisted Micro-Live position snapshots
      responses:
        "200":
          description: Position snapshots
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/MicroLivePositionSnapshot" }
  /micro-live/fill-snapshots:
    get:
      operationId: listMicroLiveFillSnapshots
      tags: [planning]
      summary: List persisted Micro-Live fill snapshots
      responses:
        "200":
          description: Fill snapshots
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/MicroLiveFillSnapshot" }
  /micro-live/incidents:
    get:
      operationId: listMicroLiveIncidents
      tags: [planning]
      summary: List open Micro-Live incidents
      responses:
        "200":
          description: Open incidents
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/MicroLiveIncident" }
  /micro-live/incidents/{incidentId}/reviews:
    post:
      operationId: createMicroLiveIncidentReview
      tags: [planning]
      summary: Record a human post-incident review
      parameters:
        - name: incidentId
          in: path
          required: true
          schema: { type: string, pattern: "^[0-9a-fA-F-]{36}$" }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/MicroLiveIncidentReviewInput" }
      responses:
        "201":
          description: Post-incident review and reactivation requirements
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MicroLiveIncidentReview" }
  /micro-live/incident-reviews:
    get:
      operationId: listMicroLiveIncidentReviews
      tags: [planning]
      summary: List persisted human post-incident reviews
      responses:
        "200":
          description: Post-incident reviews
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/MicroLiveIncidentReview" }
  /micro-live/reactivation-requirements:
    get:
      operationId: listMicroLiveReactivationRequirements
      tags: [planning]
      summary: List human reactivation requirements
      responses:
        "200":
          description: Reactivation requirements
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/MicroLiveReactivationRequirement" }
  /micro-live/reactivation-requirements/{requirementId}/complete:
    post:
      operationId: completeMicroLiveReactivationRequirement
      tags: [planning]
      summary: Complete a human reactivation requirement
      parameters:
        - name: requirementId
          in: path
          required: true
          schema: { type: string, pattern: "^[0-9a-fA-F-]{36}$" }
      responses:
        "200":
          description: Updated reactivation requirement
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MicroLiveReactivationRequirement" }
  /strategy-lab/strategies:
    post:
      operationId: createResearchStrategy
      tags: [planning]
      summary: Create an unproven research strategy with a required hypothesis
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateResearchStrategyInput"
      responses:
        "201":
          description: Research strategy created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ResearchStrategyCreated"
  /strategy-lab/strategies/{strategyId}/versions:
    post:
      operationId: createStrategyVersion
      tags: [planning]
      summary: Create an immutable strategy version
      parameters:
        - $ref: "#/components/parameters/StrategyId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateStrategyVersionInput"
      responses:
        "201":
          description: Immutable strategy version
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StrategyVersionCreated"
  /strategy-lab/experiments:
    post:
      operationId: runStrategyExperiment
      tags: [planning]
      summary: Run a deterministic simulated Strategy Lab experiment
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RunStrategyExperimentInput"
      responses:
        "201":
          description: Simulated experiment result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StrategyLabExperiment"
  /strategy-lab/strategies/{strategyId}/graduation:
    post:
      operationId: evaluateStrategyGraduation
      tags: [planning]
      summary: Evaluate strategy evidence gates without enabling live trading
      parameters:
        - $ref: "#/components/parameters/StrategyId"
      responses:
        "200":
          description: Graduation eligibility review
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StrategyGraduationReview"
  /strategy-lab/journal:
    post:
      operationId: createResearchJournalEntry
      tags: [planning]
      summary: Add a Strategy Lab research journal entry
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateResearchJournalEntryInput"
      responses:
        "201":
          description: Research journal entry
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ResearchJournalEntry"
  /strategies/{strategyId}/promote:
    post:
      operationId: promoteStrategy
      tags: [planning]
      summary: Promote a strategy by one validated stage
      parameters:
        - $ref: "#/components/parameters/StrategyId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StrategyPromotionInput"
      responses:
        "200":
          description: Promoted strategy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StrategySummary"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
  /strategies/{strategyId}/allocation:
    post:
      operationId: allocateStrategy
      tags: [planning]
      summary: Allocate internal capital to an approved strategy
      parameters:
        - $ref: "#/components/parameters/StrategyId"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StrategyAllocationInput"
      responses:
        "201":
          description: Strategy allocation recorded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TransactionSummary"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/Conflict"
  /risk:
    get:
      operationId: getRisk
      tags: [governance]
      summary: Get Capital Governor state and safeguards
      responses:
        "200":
          description: Risk state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RiskSummary"
  /risk/emergency-stop:
    post:
      operationId: activateEmergencyStop
      tags: [governance]
      summary: Activate the human-confirmed emergency stop
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EmergencyStopInput"
      responses:
        "200":
          description: Risk state after activation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RiskSummary"
        "403":
          $ref: "#/components/responses/Forbidden"
  /recommendations:
    get:
      operationId: listRecommendations
      tags: [governance]
      summary: List AI CIO advisory recommendations
      responses:
        "200":
          description: Recommendations
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/RecommendationSummary"
  /recommendations/{recommendationId}/decision:
    post:
      operationId: decideRecommendation
      tags: [governance]
      summary: Record a human recommendation decision
      parameters:
        - $ref: "#/components/parameters/RecommendationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RecommendationDecisionInput"
      responses:
        "200":
          description: Updated recommendation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RecommendationSummary"
        "403":
          $ref: "#/components/responses/Forbidden"
  /intelligence:
    get:
      operationId: getIntelligence
      tags: [governance]
      summary: Get the explainable household intelligence snapshot
      responses:
        "200":
          description: Intelligence snapshot
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IntelligenceSnapshot"
  /intelligence/refresh:
    post:
      operationId: refreshIntelligence
      tags: [governance]
      summary: Refresh persisted advisory intelligence
      responses:
        "200":
          description: Refreshed intelligence snapshot
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IntelligenceSnapshot"
  /intelligence/scenario:
    post:
      operationId: runIntelligenceScenario
      tags: [governance]
      summary: Run a non-mutating contribution pace scenario
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IntelligenceScenarioInput"
      responses:
        "200":
          description: Contribution scenario
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ContributionScenario"
  /intelligence/feedback:
    post:
      operationId: recordIntelligenceFeedback
      tags: [governance]
      summary: Record human feedback on an advisory recommendation
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IntelligenceFeedbackInput"
      responses:
        "201":
          description: Recorded recommendation feedback
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RecommendationFeedback"
  /family-office:
    get:
      operationId: getFamilyOffice
      tags: [family-office]
      summary: Get the household-scoped advisory Family Office snapshot
      responses:
        "200":
          description: Family Office snapshot
          content:
            application/json:
              schema: { $ref: "#/components/schemas/FamilyOfficeSnapshot" }
  /family-office/real-estate:
    get:
      operationId: getRealEstateIntelligence
      tags: [family-office]
      summary: Get deterministic real-estate underwriting and Florida tax-lien research
      responses:
        "200":
          description: Household-scoped real-estate intelligence
          content:
            application/json:
              schema: { $ref: "#/components/schemas/RealEstateIntelligence" }
  /family-office/refresh:
    post:
      operationId: createFamilyOfficeRefresh
      tags: [family-office]
      summary: Request a bounded, household-scoped Family Office refresh
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/FamilyOfficeRefreshInput" }
      responses:
        "200":
          description: Refresh record, including a durable skip or block reason
          content:
            application/json:
              schema: { $ref: "#/components/schemas/FamilyOfficeRefreshResult" }
  /family-office/research:
    post:
      operationId: createFamilyOfficeResearch
      tags: [family-office]
      summary: Run advisory Family Office research
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/FamilyOfficeResearchInput" }
      responses:
        "201":
          description: Completed advisory research run and proposal
          content:
            application/json:
              schema: { $ref: "#/components/schemas/FamilyOfficeResearchResult" }
        "503":
          description: Provider unavailable or provider output rejected; the blocked run is retained
          content:
            application/json:
              schema: { $ref: "#/components/schemas/FamilyOfficeResearchFailure" }
  /family-office/proposals/{proposalId}/decision:
    post:
      operationId: decideFamilyOfficeProposal
      tags: [family-office]
      summary: Record a human Shadow-only Family Office proposal decision
      parameters:
        - name: proposalId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/FamilyOfficeProposalDecisionInput" }
      responses:
        "200":
          description: Updated advisory proposal
          content:
            application/json:
              schema: { $ref: "#/components/schemas/FamilyOfficeProposal" }
  /family-office/shadow/portfolios:
    post:
      operationId: createShadowPortfolio
      tags: [family-office]
      summary: Create a hypothetical Shadow portfolio
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ShadowPortfolioInput" }
      responses:
        "201":
          description: Shadow portfolio
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ShadowPortfolio" }
  /family-office/shadow/intents:
    post:
      operationId: createShadowIntent
      tags: [family-office]
      summary: Create a hypothetical, never-transmitted Shadow order intent
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ShadowIntentInput" }
      responses:
        "201":
          description: Shadow order intent
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ShadowIntent" }
  /family-office/tax-liens:
    post:
      operationId: createTaxLienCandidate
      tags: [family-office]
      summary: Record a Florida tax-lien research candidate without authorizing a purchase
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/TaxLienCandidateInput" }
      responses:
        "201":
          description: Tax-lien research candidate
          content:
            application/json:
              schema: { $ref: "#/components/schemas/TaxLienCandidate" }
  /audit:
    get:
      operationId: listAuditEvents
      tags: [governance]
      summary: List append-only audit events
      responses:
        "200":
          description: Audit events
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AuditEventSummary"
  /reports:
    get:
      operationId: listReports
      tags: [governance]
      summary: List private report interfaces
      responses:
        "200":
          description: Available report interfaces
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ReportDescriptor"
  /blockchain/status:
    get:
      operationId: getBlockchainStatus
      tags: [blockchain]
      summary: Get disabled-by-default chain adapter status
      responses:
        "200":
          description: Blockchain status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BlockchainStatus"
  /budget:
    get:
      operationId: getBudget
      tags: [household-finance]
      summary: Get current household budget performance
      responses:
        "200":
          description: Budget performance
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BudgetSummary" }
  /cash-flow:
    get:
      operationId: getCashFlow
      tags: [household-finance]
      summary: Get household cash flow and financial health
      responses:
        "200":
          description: Cash flow snapshot
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CashFlowSummary" }
  /financial-accounts:
    get:
      operationId: listFinancialAccounts
      tags: [household-finance]
      summary: List read-only household financial accounts
      responses:
        "200":
          description: Financial accounts
          content:
            application/json:
              schema: { $ref: "#/components/schemas/FinancialAccountsSummary" }
    post:
      operationId: createManualFinancialAccount
      tags: [household-finance]
      summary: Add a manual financial account
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ManualFinancialAccountInput" }
      responses:
        "201":
          description: Financial account created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/FinancialAccount" }
  /financial-accounts/{accountId}/import-csv:
    post:
      operationId: importFinancialAccountCsv
      tags: [household-finance]
      summary: Import read-only transactions from CSV
      parameters:
        - $ref: "#/components/parameters/FinancialAccountId"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CsvImportInput" }
      responses:
        "200":
          description: Import result
          content:
            application/json:
              schema:
                type: object
                required: [imported, skippedDuplicates, readOnly]
                properties:
                  imported: { type: number }
                  skippedDuplicates: { type: number }
                  readOnly: { type: boolean }
  /financial-transactions/review-queue:
    get:
      operationId: listTransactionReviewQueue
      tags: [household-finance]
      summary: List imported transactions awaiting household review
      parameters:
        - name: periodId
          in: query
          required: false
          description: When supplied, return every transaction excluded from that planning period's weekly guidance.
          schema: { type: string, pattern: "^[0-9a-fA-F-]{36}$" }
      responses:
        "200":
          description: Household-scoped transaction review queue
          content:
            application/json:
              schema: { $ref: "#/components/schemas/TransactionReviewQueue" }
  /financial-transactions/{transactionId}/review:
    post:
      operationId: reviewFinancialTransaction
      tags: [household-finance]
      summary: Categorize or decide the planning treatment of an imported transaction
      parameters:
        - $ref: "#/components/parameters/FinancialTransactionId"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/TransactionReviewInput" }
      responses:
        "200":
          description: Reviewed transaction
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ReviewedFinancialTransaction" }
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
  /financial-accounts/{accountId}/transactions:
    post:
      operationId: createManualFinanceTransaction
      tags: [household-finance]
      summary: Record a manual transaction for household review
      parameters:
        - $ref: "#/components/parameters/FinancialAccountId"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ManualFinanceTransactionInput" }
      responses:
        "201":
          description: Transaction recorded for review
          content:
            application/json:
              schema: { $ref: "#/components/schemas/FinanceTransaction" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /bills:
    get:
      operationId: listBills
      tags: [household-finance]
      summary: List upcoming household bills
      responses:
        "200":
          description: Bills
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/Bill" }
    post:
      operationId: createBill
      tags: [household-finance]
      summary: Add a household bill
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/BillInput" }
      responses:
        "201":
          description: Bill created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Bill" }
  /bills/{billId}:
    patch:
      operationId: updateBill
      tags: [household-finance]
      summary: Update a household bill
      parameters:
        - $ref: "#/components/parameters/BillId"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/BillUpdateInput" }
      responses:
        "200":
          description: Bill updated
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Bill" }
    delete:
      operationId: deleteBill
      tags: [household-finance]
      summary: Delete a household bill
      parameters:
        - $ref: "#/components/parameters/BillId"
      responses:
        "204":
          description: Bill deleted
  /bills/{billId}/pause:
    post:
      operationId: pauseBill
      tags: [household-finance]
      summary: Pause a household bill
      parameters:
        - $ref: "#/components/parameters/BillId"
      responses:
        "200":
          description: Bill paused
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Bill" }
  /bills/{billId}/resume:
    post:
      operationId: resumeBill
      tags: [household-finance]
      summary: Resume a household bill
      parameters:
        - $ref: "#/components/parameters/BillId"
      responses:
        "200":
          description: Bill resumed
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Bill" }
  /upcoming-expenses:
    get:
      operationId: listUpcomingExpenses
      tags: [household-finance]
      summary: List known upcoming expenses
      responses:
        "200":
          description: Upcoming expenses
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/UpcomingExpense" }
    post:
      operationId: createUpcomingExpense
      tags: [household-finance]
      summary: Add a known upcoming expense
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpcomingExpenseInput" }
      responses:
        "201":
          description: Upcoming expense created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/UpcomingExpense" }
  /upcoming-expenses/{expenseId}:
    patch:
      operationId: updateUpcomingExpense
      tags: [household-finance]
      summary: Update a known upcoming expense
      parameters:
        - $ref: "#/components/parameters/UpcomingExpenseId"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpcomingExpenseUpdateInput" }
      responses:
        "200":
          description: Upcoming expense updated
          content:
            application/json:
              schema: { $ref: "#/components/schemas/UpcomingExpense" }
    delete:
      operationId: deleteUpcomingExpense
      tags: [household-finance]
      summary: Delete a known upcoming expense
      parameters:
        - $ref: "#/components/parameters/UpcomingExpenseId"
      responses:
        "204":
          description: Upcoming expense deleted
  /upcoming-expenses/{expenseId}/pause:
    post:
      operationId: pauseUpcomingExpense
      tags: [household-finance]
      summary: Pause a known upcoming expense
      parameters:
        - $ref: "#/components/parameters/UpcomingExpenseId"
      responses:
        "200":
          description: Upcoming expense paused
          content:
            application/json:
              schema: { $ref: "#/components/schemas/UpcomingExpense" }
  /upcoming-expenses/{expenseId}/resume:
    post:
      operationId: resumeUpcomingExpense
      tags: [household-finance]
      summary: Resume a known upcoming expense
      parameters:
        - $ref: "#/components/parameters/UpcomingExpenseId"
      responses:
        "200":
          description: Upcoming expense resumed
          content:
            application/json:
              schema: { $ref: "#/components/schemas/UpcomingExpense" }
  /income:
    get:
      operationId: listIncomeSources
      tags: [household-finance]
      summary: List household income sources
      responses:
        "200":
          description: Income sources
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/IncomeSource" }
    post:
      operationId: createIncomeSource
      tags: [household-finance]
      summary: Add a household income source
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/IncomeSourceInput" }
      responses:
        "201":
          description: Income source created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/IncomeSource" }
  /income/{incomeId}:
    patch:
      operationId: updateIncomeSource
      tags: [household-finance]
      summary: Update a household income source
      parameters:
        - $ref: "#/components/parameters/IncomeSourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/IncomeSourceUpdateInput" }
      responses:
        "200":
          description: Income source updated
          content:
            application/json:
              schema: { $ref: "#/components/schemas/IncomeSource" }
    delete:
      operationId: deleteIncomeSource
      tags: [household-finance]
      summary: Delete a household income source
      parameters:
        - $ref: "#/components/parameters/IncomeSourceId"
      responses:
        "204":
          description: Income source deleted
  /income/{incomeId}/pause:
    post:
      operationId: pauseIncomeSource
      tags: [household-finance]
      summary: Pause a household income source
      parameters:
        - $ref: "#/components/parameters/IncomeSourceId"
      responses:
        "200":
          description: Income source paused
          content:
            application/json:
              schema: { $ref: "#/components/schemas/IncomeSource" }
  /income/{incomeId}/resume:
    post:
      operationId: resumeIncomeSource
      tags: [household-finance]
      summary: Resume a household income source
      parameters:
        - $ref: "#/components/parameters/IncomeSourceId"
      responses:
        "200":
          description: Income source resumed
          content:
            application/json:
              schema: { $ref: "#/components/schemas/IncomeSource" }
  /safe-to-deploy:
    get:
      operationId: getSafeToDeploy
      tags: [household-finance]
      summary: Get Capital Governor safe-to-deploy amount
      responses:
        "200":
          description: Safe-to-deploy result
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SafeToDeploy" }
  /capital-governor/v2:
    get:
      operationId: getCapitalGovernorV2
      tags: [household-finance]
      summary: Get the explainable Safe-to-Deploy 2.0 result
      parameters:
        - name: asOf
          in: query
          schema: { type: string, format: date }
      responses:
        "200":
          description: Capital Governor V2 result
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CapitalGovernorV2" }
  /capital-governor/v2/waterfall:
    post:
      operationId: runCapitalWaterfall
      tags: [household-finance]
      summary: Create an advisory Capital Waterfall run
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CapitalWaterfallInput"
      responses:
        "201":
          description: Advisory waterfall run
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CapitalGovernorV2" }
  /finance-insights:
    get:
      operationId: getFinanceInsights
      tags: [household-finance]
      summary: Get advisory household finance insights
      responses:
        "200":
          description: Finance insights
          content:
            application/json:
              schema: { $ref: "#/components/schemas/FinanceInsights" }
  /finance-snapshots:
    get:
      operationId: listFinanceSnapshots
      tags: [household-finance]
      summary: List historical household finance snapshots
      responses:
        "200":
          description: Historical snapshots
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/FinanceSnapshot" }
  /budget-planning-periods/{month}:
    get:
      operationId: getBudgetPlanningPeriod
      tags: [household-finance]
      summary: Get the current draft or latest finalized household monthly budget planning period
      parameters:
        - name: month
          in: path
          required: true
          schema: { type: string, pattern: "^[0-9]{4}-(0[1-9]|1[0-2])$" }
      responses:
        "200":
          description: Planning period and immutable category snapshots
          content: { application/json: { schema: { oneOf: [{ $ref: "#/components/schemas/BudgetPlanningPeriod" }, { type: "null" }] } } }
    post:
      operationId: createBudgetPlanningPeriod
      tags: [household-finance]
      summary: Create a draft monthly plan from the latest finalized plan or household taxonomy
      parameters:
        - name: month
          in: path
          required: true
          schema: { type: string, pattern: "^[0-9]{4}-(0[1-9]|1[0-2])$" }
      responses:
        "201": { description: Draft planning period, content: { application/json: { schema: { $ref: "#/components/schemas/BudgetPlanningPeriod" } } } }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
  /budget-planning-periods/{periodId}/weekly-guidance:
    get:
      operationId: getWeeklyBudgetGuidance
      tags: [household-finance]
      summary: Get advisory weekly guidance for a planning period
      parameters:
        - { name: periodId, in: path, required: true, schema: { type: string } }
      responses:
        "200": { description: Advisory guidance that does not affect official totals, content: { application/json: { schema: { $ref: "#/components/schemas/WeeklyBudgetGuidance" } } } }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /budget-planning-periods/{periodId}/weekly-guidance/allocations:
    put:
      operationId: updateWeeklyBudgetAllocations
      tags: [household-finance]
      summary: Owner updates the exact 100 percent allocation template for a draft period
      parameters:
        - { name: periodId, in: path, required: true, schema: { type: string } }
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/WeeklyBudgetAllocationTemplateInput" } } }
      responses:
        "200": { description: Versioned allocation template update, content: { application/json: { schema: { $ref: "#/components/schemas/WeeklyBudgetAllocationTemplateResult" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
  /budget-planning-periods/{periodId}/categories:
    post:
      operationId: createBudgetPlanningCategory
      tags: [household-finance]
      summary: Add a category snapshot to a draft planning period
      parameters:
        - { name: periodId, in: path, required: true, schema: { type: string } }
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/BudgetPlanningCategoryInput" } } }
      responses:
        "201": { description: Category snapshot created, content: { application/json: { schema: { $ref: "#/components/schemas/BudgetPlanningCategoryMutation" } } } }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
  /budget-planning-periods/{periodId}/weekly-guidance/accept:
    post:
      operationId: acceptWeeklyBudgetGuidance
      tags: [household-finance]
      summary: Owner-accept selected advisory weekly guidance targets into a draft only
      parameters:
        - { name: periodId, in: path, required: true, schema: { type: string } }
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/AcceptWeeklyBudgetGuidanceInput" } } }
      responses:
        "200": { description: Draft targets updated from current recommendations, content: { application/json: { schema: { $ref: "#/components/schemas/AcceptWeeklyBudgetGuidanceResult" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
  /budget-planning-periods/{periodId}/categories/{categoryId}:
    patch:
      operationId: updateBudgetPlanningCategory
      tags: [household-finance]
      summary: Update or archive a category snapshot in a draft planning period
      parameters:
        - { name: periodId, in: path, required: true, schema: { type: string } }
        - { name: categoryId, in: path, required: true, schema: { type: string } }
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/BudgetPlanningCategoryUpdateInput" } } }
      responses:
        "200": { description: Category snapshot updated, content: { application/json: { schema: { $ref: "#/components/schemas/BudgetPlanningCategoryMutation" } } } }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
  /budget-planning-periods/{periodId}/approve:
    post:
      operationId: approveBudgetPlanningPeriod
      tags: [household-finance]
      summary: Owner-approve and freeze a monthly budget plan
      parameters:
        - { name: periodId, in: path, required: true, schema: { type: string } }
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/BudgetPlanningVersionInput" } } }
      responses:
        "200": { description: Approved immutable period, content: { application/json: { schema: { $ref: "#/components/schemas/BudgetPlanningApproval" } } } }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
  /budget-planning-periods/{periodId}/superseding-draft:
    post:
      operationId: createSupersedingBudgetPlanningPeriod
      tags: [household-finance]
      summary: Create a reviewable correction draft while preserving the approved plan
      parameters:
        - { name: periodId, in: path, required: true, schema: { type: string } }
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "201": { description: Superseding draft, content: { application/json: { schema: { $ref: "#/components/schemas/BudgetPlanningPeriod" } } } }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
  /budget-planning-periods/{month}/copy-forward:
    post:
      operationId: copyBudgetPlanningPeriod
      tags: [household-finance]
      summary: Idempotently copy the latest approved plan into a draft month
      parameters:
        - { name: month, in: path, required: true, schema: { type: string } }
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200": { description: Draft period, content: { application/json: { schema: { $ref: "#/components/schemas/BudgetPlanningPeriod" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
  /budget-planning-periods/{periodId}/reorder:
    post:
      operationId: reorderBudgetPlanningCategories
      tags: [household-finance]
      summary: Atomically reorder all draft category snapshots
      parameters: [{ name: periodId, in: path, required: true, schema: { type: string } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/BudgetPlanningReorderInput" } } } }
      responses:
        "200": { description: Reordered, content: { application/json: { schema: { $ref: "#/components/schemas/BudgetPlanningReorderResult" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
  /budget-planning-periods/{periodId}/close:
    post:
      operationId: closeBudgetPlanningPeriod
      tags: [household-finance]
      summary: Owner-close an approved immutable plan
      parameters:
        - { name: periodId, in: path, required: true, schema: { type: string } }
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/BudgetPlanningVersionInput" } } } }
      responses:
        "200": { description: Closed plan, content: { application/json: { schema: { $ref: "#/components/schemas/BudgetPlanningCloseResult" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
  /budget-planning-periods:
    get:
      operationId: listBudgetPlanningHistory
      tags: [household-finance]
      summary: List household planning-period history
      responses:
        "200": { description: History, content: { application/json: { schema: { $ref: "#/components/schemas/BudgetPlanningHistoryList" } } } }
        "403": { $ref: "#/components/responses/Forbidden" }
  /budget-planning-periods/{periodId}/change-history:
    get:
      operationId: getBudgetPlanningChangeHistory
      tags: [household-finance]
      summary: List actor-attributed audit events for a planning period and its category snapshots
      parameters: [{ name: periodId, in: path, required: true, schema: { type: string, pattern: "^[0-9a-fA-F-]{36}$" } }]
      responses:
        "200": { description: Budget plan audit history, content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/AuditEventSummary" } } } } }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /budget-planning-comparison/{month}:
    get:
      operationId: getBudgetPlanningComparison
      tags: [household-finance]
      summary: Compare approved plan targets across month quarter and year
      parameters: [{ name: month, in: path, required: true, schema: { type: string } }]
      responses:
        "200": { description: Approved-plan comparison, content: { application/json: { schema: { $ref: "#/components/schemas/BudgetPlanningComparison" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /budget-planning-periods/{periodId}/categories/{categoryId}/contributions:
    get:
      operationId: getBudgetPlanningCategoryContributionDetail
      tags: [household-finance]
      summary: Separate included reviewed household activity from excluded and unreviewed rows
      parameters:
        - { name: periodId, in: path, required: true, schema: { type: string } }
        - { name: categoryId, in: path, required: true, schema: { type: string } }
      responses:
        "200": { description: Category contribution detail, content: { application/json: { schema: { $ref: "#/components/schemas/BudgetPlanningContributionDetail" } } } }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /variable-budget/intelligence:
    get:
      operationId: getVariableBudgetIntelligence
      tags: [household-finance]
      summary: Get deterministic variable-income household planning intelligence
      parameters:
        - name: asOf
          in: query
          required: false
          schema: { type: string, pattern: "^[0-9]{4}-[0-9]{2}-[0-9]{2}$" }
      responses:
        "200": { description: Variable-income planning intelligence, content: { application/json: { schema: { $ref: "#/components/schemas/VariableBudgetIntelligence" } } } }
        "403": { $ref: "#/components/responses/Forbidden" }
  /variable-budget/profile/recalculate:
    post:
      operationId: recalculateVariableIncomeProfile
      tags: [household-finance]
      summary: Recalculate a verified-income profile
      requestBody:
        required: false
        content: { application/json: { schema: { $ref: "#/components/schemas/RecalculateVariableIncomeProfileInput" } } }
      responses:
        "200": { description: Persisted income profile, content: { application/json: { schema: { $ref: "#/components/schemas/VariableIncomeProfile" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /variable-budget/vehicle-scenarios:
    post:
      operationId: createVehicleScenario
      tags: [household-finance]
      summary: Create a planning-only vehicle affordability scenario
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/VehicleScenarioInput" } } }
      responses:
        "201": { description: Vehicle scenario, content: { application/json: { schema: { $ref: "#/components/schemas/VehicleScenario" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /banking/status:
    get:
      operationId: getBankingStatus
      tags: [household-finance]
      summary: Get provider-neutral read-only banking adapter status
      responses:
        "200":
          description: Banking status
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BankingStatus" }
  /banking/webhooks/{provider}:
    post:
      operationId: receiveReadOnlyBankWebhook
      tags: [household-finance]
      summary: Receive an authenticated read-only provider webhook
      security: []
      parameters:
        - in: path
          name: provider
          required: true
          schema: { type: string, minLength: 1, maxLength: 80 }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ProviderWebhookPayload" }
      responses:
        "202":
          description: Verified provider event accepted or deduplicated
          content:
            application/json:
              schema:
                type: object
                required: [accepted, duplicate, eventId]
                properties:
                  accepted: { type: boolean }
                  duplicate: { type: boolean }
                  eventId: { type: string }
        "403":
          $ref: "#/components/responses/Forbidden"
  /banking/connections:
    get:
      operationId: listReadOnlyBankConnections
      tags: [household-finance]
      summary: List household bank connections without exposing credentials
      responses:
        "200":
          description: Household-scoped read-only bank connections
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BankConnectionList" }
    post:
      operationId: createReadOnlyBankConnection
      tags: [household-finance]
      summary: Create a consented read-only bank connection
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ReadOnlyBankConnectionInput" }
      responses:
        "201":
          description: Consented bank connection
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BankConnection" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /banking/connections/{connectionId}/link-account:
    post:
      operationId: linkReadOnlyBankAccount
      tags: [household-finance]
      summary: Explicitly match a provider account to an existing planning account
      parameters:
        - $ref: "#/components/parameters/BankingConnectionId"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/BankAccountLinkInput" }
      responses:
        "200":
          description: Linked financial account
          content:
            application/json:
              schema: { $ref: "#/components/schemas/FinancialAccount" }
  /banking/connections/{connectionId}/sync:
    post:
      operationId: syncReadOnlyBankConnection
      tags: [household-finance]
      summary: Poll a consented bank connection through its read-only provider
      parameters:
        - $ref: "#/components/parameters/BankingConnectionId"
      responses:
        "200":
          description: Sync result and reconciliation gate
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BankSyncResult" }
  /banking/connections/{connectionId}/revoke:
    post:
      operationId: revokeReadOnlyBankConnection
      tags: [household-finance]
      summary: Revoke household consent for a read-only bank connection
      parameters:
        - $ref: "#/components/parameters/BankingConnectionId"
      responses:
        "200":
          description: Revoked bank connection
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BankConnection" }
  /banking/connections/{connectionId}/export:
    get:
      operationId: exportReadOnlyBankConnection
      tags: [household-finance]
      summary: Export provider-derived bank data without credentials
      parameters:
        - $ref: "#/components/parameters/BankingConnectionId"
      responses:
        "200":
          description: Provider-derived data export
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BankConnectionExport" }
  /banking/connections/{connectionId}/data:
    delete:
      operationId: deleteReadOnlyBankConnectionData
      tags: [household-finance]
      summary: Delete provider-derived data and credentials for a bank connection
      parameters:
        - $ref: "#/components/parameters/BankingConnectionId"
      responses:
        "200":
          description: Provider-derived data deleted
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BankConnectionDeleteResult" }
  /treasury:
    get:
      operationId: getTreasury
      tags: [treasury]
      summary: Get the household Treasury snapshot
      responses:
        "200":
          description: Treasury overview
          content:
            application/json:
              schema: { $ref: "#/components/schemas/TreasurySnapshot" }
  /treasury/requests:
    post:
      operationId: createCapitalRequest
      tags: [treasury]
      summary: Submit a recommendation-only capital request
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CapitalRequestInput" }
      responses:
        "201":
          description: Capital request submitted
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CapitalRequest" }
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
  /treasury/requests/{requestId}/decision:
    post:
      operationId: decideCapitalRequest
      tags: [treasury]
      summary: Record a human Treasury decision
      parameters:
        - name: requestId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CapitalRequestDecisionInput" }
      responses:
        "200":
          description: Capital request decision
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CapitalRequest" }
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/Conflict"
  /accounting:
    get:
      operationId: getAccountingOverview
      tags: [accounting]
      summary: Get the household accounting and net worth overview
      responses:
        "200":
          description: Authoritative accounting overview
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AccountingOverview" }
  /financing:
    get:
      operationId: getFinancingSnapshot
      tags: [financing]
      summary: Get the household advisory financing workspace
      responses:
        "200":
          description: Financing readiness, liabilities, scenarios, offers, and pipeline
          content:
            application/json:
              schema: { $ref: "#/components/schemas/FinancingSnapshot" }
  /financing/liabilities:
    post:
      operationId: createFinancingLiability
      tags: [financing]
      summary: Record a household or business liability for advisory planning
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/FinancingLiabilityInput" }
      responses:
        "201":
          description: Liability recorded
          content:
            application/json:
              schema: { $ref: "#/components/schemas/FinancingLiability" }
        "409": { $ref: "#/components/responses/Conflict" }
  /financing/credit-profile:
    patch:
      operationId: updateFinancingCreditProfile
      tags: [financing]
      summary: Update manually supplied credit planning inputs without a credit pull
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/FinancingCreditProfileUpdate" }
      responses:
        "200":
          description: Credit planning profile updated
          content:
            application/json:
              schema: { $ref: "#/components/schemas/FinancingCreditProfile" }
  /financing/scenarios:
    post:
      operationId: createFinancingScenario
      tags: [financing]
      summary: Create an illustrative financing scenario
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/FinancingScenarioInput" }
      responses:
        "201":
          description: Illustrative scenario created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/FinancingScenario" }
  /financing/offers:
    post:
      operationId: createFinancingOffer
      tags: [financing]
      summary: Record a non-binding lender or rate indication for comparison
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/FinancingOfferInput" }
      responses:
        "201":
          description: Offer indication recorded
          content:
            application/json:
              schema: { $ref: "#/components/schemas/FinancingOffer" }
  /financing/pipeline/events:
    post:
      operationId: createFinancingPipelineEvent
      tags: [financing]
      summary: Record an advisory financing pipeline stage change
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/FinancingPipelineEventInput" }
      responses:
        "201":
          description: Pipeline event recorded
          content:
            application/json:
              schema: { $ref: "#/components/schemas/FinancingPipelineEvent" }
  /financing/documents/{documentId}:
    patch:
      operationId: updateFinancingDocument
      tags: [financing]
      summary: Update a financing document readiness requirement
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
        - name: documentId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/FinancingDocumentUpdate" }
      responses:
        "200":
          description: Document requirement updated
          content:
            application/json:
              schema: { $ref: "#/components/schemas/FinancingDocument" }
  /operations:
    get:
      operationId: getOperationsOverview
      tags: [operations]
      summary: Get the household Operations Command Center snapshot
      responses:
        "200":
          description: Operations overview
          content:
            application/json:
              schema: { $ref: "#/components/schemas/OperationsOverview" }
  /operations/jobs:
    get:
      operationId: listOperationsJobs
      tags: [operations]
      summary: List the household's durable advisory jobs
      responses:
        "200":
          description: Durable operations jobs
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/OperationsJob" }
  /operations/jobs/metrics:
    get:
      operationId: getOperationsJobMetrics
      tags: [operations]
      summary: Get durable operations queue metrics
      responses:
        "200":
          description: Queue metrics
          content:
            application/json:
              schema: { $ref: "#/components/schemas/OperationsJobMetrics" }
  /operations/jobs/{jobId}/reprocess:
    post:
      operationId: reprocessOperationsJob
      tags: [operations]
      summary: Reprocess an authorized household dead-letter job
      parameters:
        - name: jobId
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Requeued durable job
          content:
            application/json:
              schema: { $ref: "#/components/schemas/OperationsJob" }
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /operations/workers:
    get:
      operationId: listOperationsWorkerHealth
      tags: [operations]
      summary: List internal operations worker health
      responses:
        "200":
          description: Worker health
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/OperationsWorker" }
  /operations/schedulers:
    get:
      operationId: listOperationsSchedulers
      tags: [operations]
      summary: List persistent household scheduler definitions
      responses:
        "200":
          description: Scheduler definitions
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/OperationsScheduler" }
  /operations/schedulers/metrics:
    get:
      operationId: getOperationsSchedulerMetrics
      tags: [operations]
      summary: Get persistent scheduler runtime metrics
      responses:
        "200":
          description: Scheduler metrics
          content:
            application/json:
              schema: { $ref: "#/components/schemas/OperationsSchedulerMetrics" }
  /operations/schedulers/leadership:
    post:
      operationId: acquireOperationsSchedulerLeadership
      tags: [operations]
      summary: Acquire the serialized scheduler leadership lease
      responses:
        "200":
          description: Scheduler leadership lease
          content:
            application/json:
              schema: { $ref: "#/components/schemas/OperationsSchedulerLease" }
        "403":
          $ref: "#/components/responses/Forbidden"
  /operations/schedulers/missed-runs/recover:
    post:
      operationId: recoverMissedOperationsSchedules
      tags: [operations]
      summary: Recover due persistent scheduler definitions
      responses:
        "200":
          description: Number of recovered schedules
          content:
            application/json:
              schema:
                type: object
                required: [recovered]
                properties:
                  recovered: { type: number, minimum: 0 }
  /operations/tasks:
    get:
      operationId: listOperationsTasks
      tags: [operations]
      summary: List household operations tasks
      responses:
        "200":
          description: Operations tasks
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/OperationsTask" }
    post:
      operationId: createOperationsTask
      tags: [operations]
      summary: Create a household operations task
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/OperationsTaskInput" }
      responses:
        "201":
          description: Created task
          content:
            application/json:
              schema: { $ref: "#/components/schemas/OperationsTask" }
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
  /operations/tasks/{taskId}:
    patch:
      operationId: updateOperationsTask
      tags: [operations]
      summary: Update task status or assignment
      parameters:
        - name: taskId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/OperationsTaskUpdate" }
      responses:
        "200":
          description: Updated task
          content:
            application/json:
              schema: { $ref: "#/components/schemas/OperationsTask" }
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"
  /operations/daily-ops:
    get:
      operationId: listDailyOpsHistory
      tags: [operations]
      summary: List household Daily Ops journal and guided-run history
      parameters:
        - name: entryType
          in: query
          description: Limit the review trail to a journal entry type or Guided Run cadence records.
          required: false
          schema: { type: string, enum: [ALL, HANDOFF, CLOSEOUT, DECISION, CADENCE], default: ALL }
        - name: cadence
          in: query
          description: Limit Guided Run records to a cadence.
          required: false
          schema: { type: string, enum: [TODAY, WEEK, MONTH] }
        - name: from
          in: query
          description: Inclusive UTC start date for the review trail.
          required: false
          schema: { type: string, format: date }
        - name: to
          in: query
          description: Inclusive UTC end date for the review trail.
          required: false
          schema: { type: string, format: date }
      responses:
        "200":
          description: Daily Ops review history
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DailyOpsHistory" }
        "400":
          $ref: "#/components/responses/BadRequest"
  /operations/daily-ops/export:
    get:
      operationId: exportDailyOpsHistory
      tags: [operations]
      summary: Export the filtered household Daily Ops review trail
      parameters:
        - name: entryType
          in: query
          description: Limit the review trail to a journal entry type or Guided Run cadence records.
          required: false
          schema: { type: string, enum: [ALL, HANDOFF, CLOSEOUT, DECISION, CADENCE], default: ALL }
        - name: cadence
          in: query
          description: Limit Guided Run records to a cadence.
          required: false
          schema: { type: string, enum: [TODAY, WEEK, MONTH] }
        - name: from
          in: query
          description: Inclusive UTC start date for the review trail.
          required: false
          schema: { type: string, format: date }
        - name: to
          in: query
          description: Inclusive UTC end date for the review trail.
          required: false
          schema: { type: string, format: date }
      responses:
        "200":
          description: CSV export containing only Daily Ops review-history fields
          content:
            text/csv:
              schema: { type: string }
              example: |
                recordType,entryType,cadence,recordId,actorId,timestamp,runDate,status,action,title,context,outcome,evidenceLinks,unresolvedBlockers
                JOURNAL,HANDOFF,,00000000-0000-0000-0000-000000000000,00000000-0000-0000-0000-000000000000,2026-09-07T10:00:00.000Z,,,,"Shift handoff","Review context",,"https://example.com/evidence",
        "400":
          $ref: "#/components/responses/BadRequest"
  /operations/daily-ops/journal:
    post:
      operationId: createDailyOpsJournalEntry
      tags: [operations]
      summary: Record a household Daily Ops decision, handoff, or closeout note
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/DailyOpsJournalEntryInput" }
      responses:
        "201":
          description: Created Daily Ops journal entry
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DailyOpsJournalEntry" }
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
  /operations/daily-ops/guided-run:
    post:
      operationId: recordGuidedRunAction
      tags: [operations]
      summary: Persist a Guided Run the Day action
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/GuidedRunActionInput" }
      responses:
        "201":
          description: Updated guided run and persisted action event
          content:
            application/json:
              schema: { $ref: "#/components/schemas/GuidedRunActionResult" }
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
  /operations/approvals:
    get:
      operationId: listOperationsApprovals
      tags: [operations]
      summary: List pending and recent household approvals
      responses:
        "200":
          description: Approval inbox
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/OperationsApproval" }
  /operations/approvals/{approvalId}/decision:
    post:
      operationId: decideOperationsApproval
      tags: [operations]
      summary: Record a human approval decision
      parameters:
        - name: approvalId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/OperationsApprovalDecision" }
      responses:
        "200":
          description: Updated approval request
          content:
            application/json:
              schema: { $ref: "#/components/schemas/OperationsApproval" }
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /operations/alerts:
    get:
      operationId: listOperationsAlerts
      tags: [operations]
      summary: List active household operations alerts
      responses:
        "200":
          description: Operations alerts
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/OperationsAlert" }
  /operations/alerts/{alertId}:
    patch:
      operationId: updateOperationsAlert
      tags: [operations]
      summary: Acknowledge or resolve an operations alert
      parameters:
        - name: alertId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/OperationsAlertUpdate" }
      responses:
        "200":
          description: Updated alert
          content:
            application/json:
              schema: { $ref: "#/components/schemas/OperationsAlert" }
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"
  /operations/automations:
    get:
      operationId: listOperationsAutomations
      tags: [operations]
      summary: List safe household automation rules
      responses:
        "200":
          description: Automation rules
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/OperationsAutomation" }
  /operations/automations/{automationId}/run:
    post:
      operationId: runOperationsAutomation
      tags: [operations]
      summary: Run a safe automation in prepare-only mode
      parameters:
        - name: automationId
          in: path
          required: true
          schema: { type: string }
      responses:
        "201":
          description: Automation run result
          content:
            application/json:
              schema: { $ref: "#/components/schemas/OperationsAutomationRun" }
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /operations/notifications/preferences:
    get:
      operationId: getOperationsNotificationPreferences
      tags: [operations]
      summary: Get household notification preferences
      responses:
        "200":
          description: Notification preferences
          content:
            application/json:
              schema: { $ref: "#/components/schemas/OperationsNotificationPreferences" }
    patch:
      operationId: updateOperationsNotificationPreferences
      tags: [operations]
      summary: Update household notification preferences
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/OperationsNotificationPreferencesUpdate" }
      responses:
        "200":
          description: Updated notification preferences
          content:
            application/json:
              schema: { $ref: "#/components/schemas/OperationsNotificationPreferences" }
        "403":
          $ref: "#/components/responses/Forbidden"
  /business:
    get:
      operationId: getBusinessOverview
      tags: [business]
      summary: Get the consolidated business and income engine overview
      responses:
        "200":
          description: Business overview
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BusinessOverview" }
  /business/companies:
    post:
      operationId: createBusinessEntity
      tags: [business]
      summary: Create an operating business
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/BusinessEntityInput" }
      responses:
        "201":
          description: Created business
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BusinessEntity" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
    get:
      operationId: listBusinessEntities
      tags: [business]
      summary: List household businesses
      responses:
        "200":
          description: Businesses
          content:
            application/json:
              schema: { type: array, items: { $ref: "#/components/schemas/BusinessEntity" } }
  /business/companies/resolve-compatible:
    post:
      operationId: resolveCompatibleTruckingBusiness
      tags: [business]
      summary: Reuse or create the canonical internal trucking accounting boundary
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CompatibleBusinessSetupInput" }
      responses:
        "200":
          description: Reused or created compatible business
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CompatibleBusinessSetupResult" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
  /business/companies/{businessId}:
    patch:
      operationId: updateBusinessEntity
      tags: [business]
      summary: Update a business profile
      parameters:
        - name: businessId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/BusinessEntityUpdate" }
      responses:
        "200":
          description: Updated business
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BusinessEntity" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /business/revenue:
    post:
      operationId: createBusinessRevenue
      tags: [business]
      summary: Record business revenue
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/BusinessRevenueInput" }
      responses:
        "201":
          description: Recorded business revenue
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BusinessRevenue" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /business/expenses:
    post:
      operationId: createBusinessExpense
      tags: [business]
      summary: Record a business expense
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/BusinessExpenseInput" }
      responses:
        "201":
          description: Recorded business expense
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BusinessExpense" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /business/distributions:
    post:
      operationId: createBusinessDistribution
      tags: [business]
      summary: Prepare an owner distribution for human review
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/BusinessDistributionInput" }
      responses:
        "201":
          description: Proposed owner distribution
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BusinessDistribution" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /business/reserves/{businessId}:
    patch:
      operationId: updateBusinessReserve
      tags: [business]
      summary: Update business reserve planning assumptions
      parameters:
        - name: businessId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/BusinessReserveUpdate" }
      responses:
        "200":
          description: Updated business reserve
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BusinessReserve" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /business/income-intelligence:
    get:
      operationId: getBusinessIncomeIntelligence
      tags: [business]
      summary: Get settlement, reconciliation, cash, and verified-income intelligence
      responses:
        "200":
          description: Business income intelligence
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BusinessIncomeIntelligence" }
  /business/income/documents/upload-url:
    post:
      operationId: requestBusinessIncomeDocumentUploadUrl
      tags: [business]
      summary: Request a managed object-storage upload URL for a business source PDF
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/BusinessIncomeDocumentUploadInput" }
      responses:
        "200":
          description: Presigned document upload
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BusinessIncomeDocumentUpload" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /business/income/documents/ingest:
    post:
      operationId: ingestBusinessIncomeDocument
      tags: [business]
      summary: Parse a stored settlement or P&L PDF into reviewable source records
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/BusinessIncomeDocumentIngestInput" }
      responses:
        "201":
          description: Parsed or review-blocked business source document
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BusinessIncomeDocumentIngestResult" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
  /business/income/documents/{documentId}/review:
    post:
      operationId: reviewBusinessIncomeDocument
      tags: [business]
      summary: Record an authorized review decision for an extracted business document
      parameters:
        - name: documentId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/BusinessIncomeDocumentReviewInput" }
      responses:
        "200":
          description: Document review result
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BusinessIncomeReviewResult" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
  /business/income/documents/{documentId}/lines/{lineId}/review:
    post:
      operationId: reviewBusinessIncomeLine
      tags: [business]
      summary: Record an authorized review decision and optional correction for an extracted line item
      parameters:
        - name: documentId
          in: path
          required: true
          schema: { type: string }
        - name: lineId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/BusinessIncomeLineReviewInput" }
      responses:
        "200":
          description: Line review result
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BusinessIncomeReviewResult" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
  /business/income/settlements:
    post:
      operationId: createBusinessSettlement
      tags: [business]
      summary: Record an immutable settlement source and reconcile its exact-cent math
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SettlementDocumentInput" }
      responses:
        "201":
          description: Settlement document and math reconciliation
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BusinessSettlementDocument" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
  /business/income/profit-loss:
    post:
      operationId: createBusinessProfitLoss
      tags: [business]
      summary: Record a period P&L document for reconciliation
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ProfitLossDocumentInput" }
      responses:
        "201":
          description: P&L document
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BusinessProfitLossDocument" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /business/income/reconcile:
    post:
      operationId: reconcileBusinessIncomePeriod
      tags: [business]
      summary: Reconcile settlement-derived profit to a period P&L
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/BusinessIncomePeriodReconciliationInput" }
      responses:
        "201":
          description: Reconciliation run
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BusinessIncomePeriodReconciliation" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /business/income/settlements/{settlementId}/cash-match:
    post:
      operationId: matchBusinessSettlementCash
      tags: [business]
      summary: Match a settlement to read-only business bank evidence
      parameters:
        - name: settlementId
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Settlement cash match
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BusinessSettlementCashMatch" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /business/income/cash-position:
    post:
      operationId: createBusinessCashPosition
      tags: [business]
      summary: Build a reserve-governed business cash position from read-only evidence
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/BusinessCashPositionInput" }
      responses:
        "201":
          description: Business cash position
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BusinessCashPosition" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /business/income/owner-draws:
    post:
      operationId: createBusinessOwnerDraw
      tags: [business]
      summary: Prepare a reserve-governed owner draw for human approval
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/BusinessOwnerDrawInput" }
      responses:
        "201":
          description: Owner draw proposal
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BusinessOwnerDraw" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /business/income/owner-draws/{proposalId}/approve:
    post:
      operationId: approveBusinessOwnerDraw
      tags: [business]
      summary: Approve an eligible owner draw and create verified household income
      parameters:
        - name: proposalId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/BusinessOwnerDrawApprovalInput" }
      responses:
        "200":
          description: Approved owner draw and verified income event
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BusinessOwnerDrawApprovalResult" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
  /financial-documents:
    get:
      operationId: listFinancialDocuments
      tags: [household-finance]
      summary: List household financial source evidence
      responses:
        "200":
          description: Financial documents
          content: { application/json: { schema: { $ref: "#/components/schemas/FinancialDocumentList" } } }
  /financial-documents/upload-url:
    post:
      operationId: requestFinancialDocumentUploadUrl
      tags: [household-finance]
      summary: Request a private upload URL for financial evidence
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/FinancialDocumentUploadInput" } } }
      responses:
        "200": { description: Upload target, content: { application/json: { schema: { $ref: "#/components/schemas/FinancialDocumentUploadTarget" } } } }
  /financial-documents/ingest:
    post:
      operationId: ingestFinancialDocument
      tags: [household-finance]
      summary: Record uploaded financial evidence without creating ledger entries
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/FinancialDocumentIngestInput" } } }
      responses:
        "201": { description: Recorded financial evidence, content: { application/json: { schema: { $ref: "#/components/schemas/FinancialDocument" } } } }
        "409": { $ref: "#/components/responses/Conflict" }
  /financial-documents/review-queue:
    get:
      operationId: listFinancialReviewQueue
      tags: [household-finance]
      summary: List cross-source financial evidence requiring review
      responses:
        "200": { description: Review queue, content: { application/json: { schema: { $ref: "#/components/schemas/FinancialReviewQueue" } } } }
  /financial-documents/{documentId}:
    get:
      operationId: getFinancialDocument
      tags: [household-finance]
      summary: Retrieve source provenance and evidence detail
      parameters: [{ $ref: "#/components/parameters/FinancialDocumentId" }]
      responses:
        "200": { description: Financial document detail, content: { application/json: { schema: { $ref: "#/components/schemas/FinancialDocument" } } } }
  /financial-documents/{documentId}/review:
    post:
      operationId: reviewFinancialDocument
      tags: [household-finance]
      summary: Record an approver review decision for source evidence
      parameters: [{ $ref: "#/components/parameters/FinancialDocumentId" }]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/FinancialDocumentReviewInput" } } }
      responses:
        "200": { description: Reviewed document, content: { application/json: { schema: { $ref: "#/components/schemas/FinancialDocument" } } } }
  /financial-documents/{documentId}/detect-type:
    post:
      operationId: runFinancialDocumentTypeDetection
      tags: [household-finance]
      summary: Run or re-run content detection against preserved financial evidence
      parameters: [{ $ref: "#/components/parameters/FinancialDocumentId" }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/FinancialDocumentTypeDetectionInput" }
      responses:
        "200": { description: Persisted detection evidence, content: { application/json: { schema: { $ref: "#/components/schemas/FinancialDocument" } } } }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
  /financial-documents/{documentId}/business-link:
    post:
      operationId: linkFinancialDocumentBusiness
      tags: [household-finance]
      summary: Link an existing household business to source evidence
      parameters: [{ $ref: "#/components/parameters/FinancialDocumentId" }]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/FinancialDocumentBusinessLinkInput" } } }
      responses:
        "200": { description: Linked financial document, content: { application/json: { schema: { $ref: "#/components/schemas/FinancialDocument" } } } }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
  /financial-documents/{documentId}/type-decision:
    post:
      operationId: decideFinancialDocumentType
      tags: [household-finance]
      summary: Keep or apply a human-reviewed financial document type decision
      parameters: [{ $ref: "#/components/parameters/FinancialDocumentId" }]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/FinancialDocumentTypeDecisionInput" } } }
      responses:
        "200": { description: Applied or recorded type decision, content: { application/json: { schema: { $ref: "#/components/schemas/FinancialDocument" } } } }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
  /financial-documents/{documentId}/identity-review:
    post:
      operationId: reviewFinancialDocumentIdentity
      tags: [household-finance]
      summary: Record an explicit duplicate or document-version review
      parameters: [{ $ref: "#/components/parameters/FinancialDocumentId" }]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/FinancialDocumentIdentityReviewInput" } } }
      responses:
        "200": { description: Updated identity metadata, content: { application/json: { schema: { $ref: "#/components/schemas/FinancialDocument" } } } }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
  /bank-statement-transactions/{transactionId}/review:
    post:
      operationId: reviewBankStatementTransaction
      tags: [household-finance]
      summary: Review statement evidence only; never posts or writes to a bank
      parameters:
        - name: transactionId
          in: path
          required: true
          schema: { type: string, pattern: "^[0-9a-fA-F-]{36}$" }
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/BankStatementTransactionReviewInput" } } }
      responses:
        "200": { description: Reviewed evidence row, content: { application/json: { schema: { $ref: "#/components/schemas/BankStatementTransactionEvidence" } } } }
  /bank-statement-transactions/{transactionId}/category-decision:
    post:
      operationId: decideBankStatementTransactionCategory
      tags: [household-finance]
      summary: Record a human category decision without posting financial activity
      parameters: [{ $ref: "#/components/parameters/FinancialTransactionId" }]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/StatementCategoryDecisionInput" } } }
      responses:
        "200": { description: Updated statement evidence row, content: { application/json: { schema: { $ref: "#/components/schemas/BankStatementTransactionEvidence" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
  /bank-statement-transactions/{transactionId}/inclusion:
    get:
      operationId: getBankStatementTransactionInclusion
      tags: [household-finance]
      summary: Get the durable current inclusion and reconciliation state for statement evidence
      parameters: [{ $ref: "#/components/parameters/FinancialTransactionId" }]
      responses:
        "200": { description: Current inclusion state, content: { application/json: { schema: { $ref: "#/components/schemas/StatementFinancialInclusion" } } } }
        "404": { $ref: "#/components/responses/NotFound" }
  /bank-statement-transactions/{transactionId}/inclusion/reconcile:
    post:
      operationId: reconcileBankStatementTransactionInclusion
      tags: [household-finance]
      summary: Explicitly resolve a source-to-official mismatch without mutating source or official transaction
      parameters: [{ $ref: "#/components/parameters/FinancialTransactionId" }, { $ref: "#/components/parameters/IdempotencyKey" }]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/StatementFinancialReconciliationInput" } } }
      responses:
        "200": { description: Reconciled inclusion, content: { application/json: { schema: { $ref: "#/components/schemas/StatementFinancialInclusion" } } } }
  /bank-statement-transactions/{transactionId}/match-preview:
    post:
      operationId: previewBankStatementTransactionMatch
      tags: [household-finance]
      summary: Preview household-scoped duplicate and existing-transaction matches before inclusion
      parameters: [{ $ref: "#/components/parameters/FinancialTransactionId" }]
      responses:
        "200": { description: Match preview, content: { application/json: { schema: { $ref: "#/components/schemas/StatementMatchPreview" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /bank-statement-transactions/{transactionId}/import:
    post:
      operationId: importBankStatementTransaction
      tags: [household-finance]
      summary: Explicitly import one eligible reviewed row as one official transaction
      parameters: [{ $ref: "#/components/parameters/FinancialTransactionId" }, { $ref: "#/components/parameters/IdempotencyKey" }]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/StatementFinancialImportInput" } } }
      responses:
        "200": { description: Existing idempotent import result, content: { application/json: { schema: { $ref: "#/components/schemas/StatementFinancialInclusion" } } } }
        "201": { description: Imported official transaction, content: { application/json: { schema: { $ref: "#/components/schemas/StatementFinancialInclusion" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
  /bank-statement-transactions/{transactionId}/link:
    post:
      operationId: linkBankStatementTransaction
      tags: [household-finance]
      summary: Explicitly link evidence to one existing household transaction without creating another
      parameters: [{ $ref: "#/components/parameters/FinancialTransactionId" }, { $ref: "#/components/parameters/IdempotencyKey" }]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/StatementFinancialLinkInput" } } }
      responses:
        "200": { description: Linked financial inclusion, content: { application/json: { schema: { $ref: "#/components/schemas/StatementFinancialInclusion" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
  /bank-statement-transactions/{transactionId}/unlink:
    post:
      operationId: unlinkBankStatementTransaction
      tags: [household-finance]
      summary: Explicitly unlink a statement row while preserving an append-only reversal record
      parameters: [{ $ref: "#/components/parameters/FinancialTransactionId" }, { $ref: "#/components/parameters/IdempotencyKey" }]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/StatementFinancialReversalInput" } } }
      responses:
        "200": { description: Unlinked inclusion and reversal record, content: { application/json: { schema: { $ref: "#/components/schemas/StatementFinancialReversalResult" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
  /bank-statement-transactions/{transactionId}/reverse:
    post:
      operationId: reverseBankStatementTransactionImport
      tags: [household-finance]
      summary: Explicitly reverse a previous statement import without deleting source evidence
      parameters: [{ $ref: "#/components/parameters/FinancialTransactionId" }, { $ref: "#/components/parameters/IdempotencyKey" }]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/StatementFinancialReversalInput" } } }
      responses:
        "200": { description: Reversed inclusion and reversal record, content: { application/json: { schema: { $ref: "#/components/schemas/StatementFinancialReversalResult" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
components:
  securitySchemes:
    clerkSession:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: Clerk-issued session token resolved to an active Capital OS household membership.
    clerkReverification:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: Clerk provider-supported reverification is required by the server for protected policy, approval, permission, ownership, and Micro-Live boundary writes. The client receives Clerk's reverification hint and retries after provider verification.
  parameters:
    FinancialDocumentId:
      name: documentId
      in: path
      required: true
      schema: { type: string, pattern: "^[0-9a-fA-F-]{36}$" }
    IdempotencyKey:
      name: Idempotency-Key
      in: header
      required: true
      schema:
        type: string
        minLength: 8
        maxLength: 128
    StrategyId:
      name: strategyId
      in: path
      required: true
      schema:
        type: string
        pattern: "^[0-9a-fA-F-]{36}$"
    RecommendationId:
      name: recommendationId
      in: path
      required: true
      schema:
        type: string
        pattern: "^[0-9a-fA-F-]{36}$"
    FinancialAccountId:
      name: accountId
      in: path
      required: true
      schema:
        type: string
        pattern: "^[0-9a-fA-F-]{36}$"
    FinancialTransactionId:
      name: transactionId
      in: path
      required: true
      schema:
        type: string
        pattern: "^[0-9a-fA-F-]{36}$"
    BankingConnectionId:
      name: connectionId
      in: path
      required: true
      schema:
        type: string
        pattern: "^[0-9a-fA-F-]{36}$"
    BillId:
      name: billId
      in: path
      required: true
      schema:
        type: string
        pattern: "^[0-9a-fA-F-]{36}$"
    UpcomingExpenseId:
      name: expenseId
      in: path
      required: true
      schema:
        type: string
        pattern: "^[0-9a-fA-F-]{36}$"
    IncomeSourceId:
      name: incomeId
      in: path
      required: true
      schema:
        type: string
        pattern: "^[0-9a-fA-F-]{36}$"
  responses:
    BadRequest:
      description: Invalid request
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    Forbidden:
      description: Household role or Capital Governor rejected the action
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    Unauthorized:
      description: Authentication is required
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    NotFound:
      description: Requested household record was not found
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    Conflict:
      description: Idempotency or state conflict
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
  schemas:
    FinancialDocument:
      type: object
      required: [id, documentType, status, sourceFileName, mimeType, documentHash, sourceObjectPath, uploadedAt, detectionSignals, businessId]
      properties:
        id: { type: string }
        businessId: { type: [string, "null"] }
        documentType: { type: string }
        originalDocumentType: { type: [string, "null"] }
        detectedDocumentType: { type: [string, "null"] }
        detectionConfidence: { type: [string, "null"] }
        detectionSignals: { type: array, items: { type: string } }
        detectionVersion: { type: [string, "null"] }
        typeMismatchStatus: { type: string }
        canonicalDocumentId: { type: [string, "null"] }
        duplicateOfDocumentId: { type: [string, "null"] }
        supersedesDocumentId: { type: [string, "null"] }
        supersededByDocumentId: { type: [string, "null"] }
        versionLabel: { type: [string, "null"] }
        identityStatus: { type: string }
        status: { type: string }
        sourceInstitution: { type: [string, "null"] }
        sourceFileName: { type: string }
        mimeType: { type: string }
        documentHash: { type: string }
        sourceObjectPath: { type: string }
        sourceMetadata: { type: object, additionalProperties: true }
        periodStart: { type: [string, "null"] }
        periodEnd: { type: [string, "null"] }
        statementDate: { type: [string, "null"] }
        parserVersion: { type: [string, "null"] }
        sourceRecordType: { type: [string, "null"] }
        sourceRecordId: { type: [string, "null"] }
        uploadedAt: { type: string, format: date-time }
        reviewedAt: { type: [string, "null"], format: date-time }
        reviewDecision: { type: [string, "null"] }
        reviewReason: { type: [string, "null"] }
        bankStatement:
          oneOf:
            - $ref: "#/components/schemas/BankStatementEvidence"
            - type: "null"
        transactions: { type: array, items: { $ref: "#/components/schemas/BankStatementTransactionEvidence" } }
    FinancialDocumentList:
      type: object
      required: [documents]
      properties: { documents: { type: array, items: { $ref: "#/components/schemas/FinancialDocument" } } }
    FinancialDocumentUploadInput:
      type: object
      required: [name, size, contentType, documentType]
      properties:
        name: { type: string, minLength: 1, maxLength: 255 }
        size: { type: number, minimum: 1, maximum: 52428800 }
        contentType: { type: string, enum: [application/pdf, text/csv, application/vnd.openxmlformats-officedocument.spreadsheetml.sheet] }
        documentType: { type: string, enum: [STEVENS_SETTLEMENT, BUSINESS_PROFIT_AND_LOSS, BANK_STATEMENT, "1099", INCOME_VERIFICATION, INSURANCE_DOCUMENT, AUTO_LOAN_DOCUMENT, BUSINESS_LEASE_DOCUMENT, OTHER_FINANCIAL_DOCUMENT] }
    FinancialDocumentUploadTarget:
      type: object
      required: [name, size, contentType, documentType, uploadURL, objectPath, uploadGrant]
      properties:
        name: { type: string }
        size: { type: number }
        contentType: { type: string }
        documentType: { type: string }
        uploadURL: { type: string }
        objectPath: { type: string }
        uploadGrant: { type: string }
    FinancialDocumentIngestInput:
      type: object
      required: [documentType, sourceFileName, sourceObjectPath, contentType, sourceSizeBytes, uploadGrant]
      properties:
        documentType: { type: string }
        sourceFileName: { type: string, minLength: 1, maxLength: 255 }
        sourceObjectPath: { type: string, pattern: "^/objects/uploads/[0-9a-fA-F-]{36}$" }
        contentType: { type: string }
        sourceSizeBytes: { type: number, minimum: 1, maximum: 52428800 }
        uploadGrant: { type: string, minLength: 40, maxLength: 2048 }
        businessId: { type: string }
        sourceInstitution: { type: string }
        accountId: { type: string }
        statementStart: { type: string }
        statementEnd: { type: string }
        accountDisplayName: { type: string }
        accountMask: { type: string }
    FinancialDocumentReviewInput:
      type: object
      required: [decision, reason]
      properties:
        decision: { type: string, enum: [VERIFIED, REJECTED, NEEDS_REVIEW] }
        reason: { type: string, minLength: 1, maxLength: 1000 }
    FinancialDocumentBusinessLinkInput:
      type: object
      required: [businessId, reason, idempotencyKey]
      properties:
        businessId: { type: string, pattern: "^[0-9a-fA-F-]{36}$" }
        reason: { type: string, minLength: 1, maxLength: 1000 }
        idempotencyKey: { type: string, minLength: 8, maxLength: 128 }
    FinancialDocumentTypeDecisionInput:
      type: object
      required: [action, reason, idempotencyKey]
      properties:
        action: { type: string, enum: [USE_DETECTED_TYPE, KEEP_SELECTED_TYPE] }
        reason: { type: string, minLength: 1, maxLength: 1000 }
        idempotencyKey: { type: string, minLength: 8, maxLength: 128 }
    FinancialDocumentTypeDetectionInput:
      type: object
      required: [reason, idempotencyKey]
      properties:
        reason: { type: string, minLength: 1, maxLength: 1000 }
        idempotencyKey: { type: string, minLength: 8, maxLength: 128 }
    FinancialDocumentIdentityReviewInput:
      type: object
      required: [comparedDocumentId, classification, reason]
      properties:
        comparedDocumentId: { type: string, pattern: "^[0-9a-fA-F-]{36}$" }
        classification: { type: string, enum: [EXACT_DUPLICATE, PROBABLE_DUPLICATE, DISTINCT_PERIOD, DISTINCT_VERSION, CORRECTED_VERSION, UNKNOWN_REVIEW_REQUIRED] }
        reason: { type: string, minLength: 1, maxLength: 1000 }
        canonicalDocumentId: { type: string, pattern: "^[0-9a-fA-F-]{36}$" }
    CompatibleBusinessSetupInput:
      type: object
      required: [businessKind, idempotencyKey]
      properties:
        businessKind: { type: string, enum: [INDEPENDENT_CONTRACTOR_TRUCKING] }
        idempotencyKey: { type: string, minLength: 8, maxLength: 128 }
    CompatibleBusinessSetupResult:
      type: object
      required: [business, outcome, disclaimer]
      properties:
        business: { $ref: "#/components/schemas/BusinessEntity" }
        outcome: { type: string, enum: [REUSED, CREATED] }
        disclaimer: { type: string }
    FinancialReviewQueue:
      type: object
      required: [items]
      properties: { items: { type: array, items: { type: object, additionalProperties: true } } }
    BankStatementTransactionReviewInput:
      type: object
      required: [action, reason, idempotencyKey]
      properties:
        action: { type: string, enum: [APPROVE, REJECT, RECLASSIFY, LINK_SETTLEMENT, MARK_TRANSFER] }
        reason: { type: string, minLength: 1, maxLength: 1000 }
        idempotencyKey: { type: string, minLength: 8, maxLength: 128 }
        correctedValue: { type: object, additionalProperties: true }
        settlementDocumentId: { type: string, pattern: "^[0-9a-fA-F-]{36}$" }
    BankStatementTransactionEvidence:
      type: object
      required: [id, bankStatementDocumentId, description, amount, direction, confidence, sourceLine, sourceRegion, parserVersion, evidenceFingerprint, reviewStatus, lastReviewAction, originalValue, correctionHistory, createdAt]
      properties:
        id: { type: string }
        bankStatementDocumentId: { type: string }
        postedDate: { type: [string, "null"] }
        description: { type: string }
        amount: { type: string }
        direction: { type: [string, "null"] }
        runningBalance: { type: [string, "null"] }
        reference: { type: [string, "null"] }
        confidence: { type: [string, "null"] }
        sourcePage: { type: [number, "null"], multipleOf: 1 }
        sourceLine: { type: [number, "null"], multipleOf: 1 }
        sourceRegion: { type: [string, "null"] }
        parserVersion: { type: string }
        evidenceFingerprint: { type: string }
        reviewStatus: { type: string }
        lastReviewAction: { type: [string, "null"], enum: [APPROVE, REJECT, RECLASSIFY, LINK_SETTLEMENT, MARK_TRANSFER, null] }
        originalValue: { type: object, additionalProperties: true }
        correctedValue: { type: [object, "null"], additionalProperties: true }
        correctionReason: { type: [string, "null"] }
        reviewedAt: { type: [string, "null"], format: date-time }
        linkedSettlementDocumentId: { type: [string, "null"] }
        suggestedCategoryId: { type: [string, "null"] }
        suggestedCategoryConfidence: { type: [string, "null"], enum: [HIGH, MEDIUM, LOW, UNKNOWN, null] }
        suggestedCategoryReason: { type: [string, "null"] }
        suggestedCategorySource: { type: [string, "null"] }
        selectedCategoryId: { type: [string, "null"] }
        categoryDecisionStatus: { type: string, enum: [UNCLASSIFIED, SUGGESTED, USER_CONFIRMED, USER_CORRECTED, NOT_APPLICABLE_TRANSFER, NOT_APPLICABLE_SETTLEMENT, REJECTED] }
        categoryDecidedBy: { type: [string, "null"] }
        categoryDecidedAt: { type: [string, "null"], format: date-time }
        categoryCorrectionVersion: { type: number, minimum: 0, multipleOf: 1 }
        economicClassification: { type: string, enum: [HOUSEHOLD, BUSINESS, TRANSFER, SETTLEMENT_LINK, UNKNOWN] }
        createdAt: { type: string, format: date-time }
        correctionHistory:
          type: array
          items: { $ref: "#/components/schemas/BankStatementTransactionCorrection" }
    BankStatementTransactionCorrection:
      type: object
      required: [id, transactionId, revision, correctedValue, reason, correctedAt]
      properties:
        id: { type: string }
        transactionId: { type: string }
        revision: { type: number, multipleOf: 1 }
        previousValue: { type: [object, "null"], additionalProperties: true }
        correctedValue: { type: object, additionalProperties: true }
        reason: { type: string }
        correctedAt: { type: string, format: date-time }
    StatementCategoryDecisionInput:
      type: object
      required: [status, economicClassification, reason, idempotencyKey]
      properties:
        status: { type: string, enum: [USER_CONFIRMED, USER_CORRECTED, NOT_APPLICABLE_TRANSFER, NOT_APPLICABLE_SETTLEMENT, REJECTED] }
        categoryId: { type: [string, "null"], pattern: "^[0-9a-fA-F-]{36}$" }
        economicClassification: { type: string, enum: [HOUSEHOLD, BUSINESS, TRANSFER, SETTLEMENT_LINK, UNKNOWN] }
        reason: { type: string, minLength: 1, maxLength: 1000 }
        idempotencyKey: { type: string, minLength: 8, maxLength: 128 }
    StatementMatchCandidate:
      type: object
      required: [financeTransactionId, transactionDate, amount, description, confidence, reasons]
      properties:
        financeTransactionId: { type: string, pattern: "^[0-9a-fA-F-]{36}$" }
        accountId: { type: string, pattern: "^[0-9a-fA-F-]{36}$" }
        transactionDate: { type: string, format: date }
        amount: { type: string, pattern: "^-?[0-9]+\\.[0-9]{2}$" }
        description: { type: string }
        merchant: { type: [string, "null"] }
        categoryId: { type: [string, "null"] }
        confidence: { type: string, enum: [HIGH, MEDIUM, LOW] }
        reasons: { type: array, items: { type: string } }
    StatementMatchPreview:
      type: object
      required: [statementRowId, outcome, candidates, canImportAsNew, requiresHumanDecision]
      properties:
        statementRowId: { type: string, pattern: "^[0-9a-fA-F-]{36}$" }
        outcome: { type: string, enum: [NO_MATCH, ONE_HIGH_CONFIDENCE_MATCH, MULTIPLE_CANDIDATES, EXACT_ALREADY_LINKED, KNOWN_DUPLICATE] }
        candidates: { type: array, items: { $ref: "#/components/schemas/StatementMatchCandidate" } }
        canImportAsNew: { type: boolean }
        requiresHumanDecision: { type: boolean }
        existingInclusionId: { type: [string, "null"] }
    StatementFinancialImportInput:
      type: object
      required: [idempotencyKey]
      properties:
        idempotencyKey: { type: string, minLength: 8, maxLength: 128 }
        expectedMatchOutcome: { type: string, enum: [NO_MATCH, MULTIPLE_CANDIDATES] }
    StatementFinancialLinkInput:
      type: object
      required: [financeTransactionId, idempotencyKey]
      properties:
        financeTransactionId: { type: string, pattern: "^[0-9a-fA-F-]{36}$" }
        idempotencyKey: { type: string, minLength: 8, maxLength: 128 }
    StatementFinancialReversalInput:
      type: object
      required: [reason, idempotencyKey]
      properties:
        reason: { type: string, minLength: 1, maxLength: 1000 }
        idempotencyKey: { type: string, minLength: 8, maxLength: 128 }
    StatementFinancialReconciliationInput:
      type: object
      required: [reason, idempotencyKey]
      properties:
        reason: { type: string, minLength: 1, maxLength: 1000 }
        idempotencyKey: { type: string, minLength: 8, maxLength: 128 }
    StatementFinancialInclusion:
      type: object
      required: [id, statementDocumentId, statementRowId, statementRowFingerprint, evidenceDecision, inclusionDecision, duplicateStatus, transferStatus, settlementLinkStatus, status, reviewRequired, reconciliationStatus, createdAt, updatedAt]
      properties:
        id: { type: string, pattern: "^[0-9a-fA-F-]{36}$" }
        statementDocumentId: { type: string, pattern: "^[0-9a-fA-F-]{36}$" }
        statementRowId: { type: string, pattern: "^[0-9a-fA-F-]{36}$" }
        statementRowFingerprint: { type: string, minLength: 1 }
        evidenceDecision: { type: string }
        categoryId: { type: [string, "null"] }
        inclusionDecision: { type: string }
        matchedFinanceTransactionId: { type: [string, "null"] }
        createdFinanceTransactionId: { type: [string, "null"] }
        duplicateStatus: { type: string }
        transferStatus: { type: string }
        settlementLinkStatus: { type: string }
        approvedBy: { type: [string, "null"] }
        approvedAt: { type: [string, "null"], format: date-time }
        reversedBy: { type: [string, "null"] }
        reversedAt: { type: [string, "null"], format: date-time }
        status: { type: string, enum: [NOT_REVIEWED, READY_FOR_INCLUSION_REVIEW, MATCH_CANDIDATE, DUPLICATE_REVIEW_REQUIRED, READY_TO_IMPORT, LINKED_EXISTING, IMPORTED_NEW, EXCLUDED_TRANSFER, EXCLUDED_SETTLEMENT, EXCLUDED_DUPLICATE, REVERSED, REJECTED] }
        reviewRequired: { type: boolean }
        mismatchCode: { type: [string, "null"], enum: [SOURCE_OFFICIAL_MISMATCH, null] }
        reconciliationStatus: { type: string, enum: [NOT_REQUIRED, REQUIRED, RESOLVED] }
        reconciledBy: { type: [string, "null"] }
        reconciledAt: { type: [string, "null"], format: date-time }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    StatementFinancialReversal:
      type: object
      required: [id, inclusionId, statementRowId, previousState, newState, reason, actor, timestamp]
      properties:
        id: { type: string, pattern: "^[0-9a-fA-F-]{36}$" }
        inclusionId: { type: string, pattern: "^[0-9a-fA-F-]{36}$" }
        statementRowId: { type: string, pattern: "^[0-9a-fA-F-]{36}$" }
        financeTransactionId: { type: [string, "null"] }
        previousState: { type: object, additionalProperties: true }
        newState: { type: object, additionalProperties: true }
        reason: { type: string }
        actor: { type: string, pattern: "^[0-9a-fA-F-]{36}$" }
        timestamp: { type: string, format: date-time }
    StatementFinancialReversalResult:
      type: object
      required: [inclusion, reversal]
      properties:
        inclusion: { $ref: "#/components/schemas/StatementFinancialInclusion" }
        reversal: { $ref: "#/components/schemas/StatementFinancialReversal" }
    BankStatementEvidence:
      type: object
      required: [id, documentId, openingBalance, closingBalance, totalDeposits, totalWithdrawals, status, createdAt]
      properties:
        id: { type: string }
        documentId: { type: string }
        accountId: { type: [string, "null"] }
        institutionName: { type: [string, "null"] }
        accountDisplayName: { type: [string, "null"] }
        accountMask: { type: [string, "null"] }
        statementStart: { type: [string, "null"] }
        statementEnd: { type: [string, "null"] }
        openingBalance: { type: string }
        closingBalance: { type: string }
        totalDeposits: { type: string }
        totalWithdrawals: { type: string }
        status: { type: string }
        createdAt: { type: string, format: date-time }
    BudgetPlanningVersionInput:
      type: object
      required: [version]
      properties: { version: { type: number, minimum: 1 } }
    BudgetPlanningCategoryInput:
      type: object
      required: [version, name, categoryType, essentialStatus, monthlyTarget]
      properties:
        version: { type: number, minimum: 1 }
        name: { type: string, minLength: 1, maxLength: 160 }
        categoryType: { type: string, enum: [fixed_expense, variable_essential, variable_discretionary, savings, investment, debt_payment, transfer, income, one_time_expense] }
        essentialStatus: { type: string, enum: [essential, discretionary, mixed] }
        monthlyTarget: { type: string }
        warningThreshold: { type: string }
        notes: { type: [string, "null"], maxLength: 2000 }
    BudgetPlanningCategoryUpdateInput:
      type: object
      required: [version]
      properties:
        version: { type: number, minimum: 1 }
        name: { type: string, minLength: 1, maxLength: 160 }
        categoryType: { type: string, enum: [fixed_expense, variable_essential, variable_discretionary, savings, investment, debt_payment, transfer, income, one_time_expense] }
        essentialStatus: { type: string, enum: [essential, discretionary, mixed] }
        monthlyTarget: { type: string }
        warningThreshold: { type: string }
        notes: { type: [string, "null"], maxLength: 2000 }
        archived: { type: boolean }
    BudgetPlanningCategory:
      type: object
      required: [id, name, categoryType, essentialStatus, monthlyTarget, allocationBasisPoints, warningThreshold, sortOrder, archived]
      properties:
        id: { type: string }
        sourceCategoryId: { type: [string, "null"] }
        name: { type: string }
        categoryType: { type: string }
        essentialStatus: { type: string }
        monthlyTarget: { type: string }
        allocationBasisPoints: { type: [number, "null"], minimum: 0, maximum: 10000, multipleOf: 1 }
        warningThreshold: { type: string }
        notes: { type: [string, "null"] }
        sortOrder: { type: number }
        archived: { type: boolean }
    BudgetPlanningPeriod:
      type: object
      required: [id, month, status, version, createdBy, createdAt, updatedAt, categories]
      properties:
        id: { type: string }
        month: { type: string }
        status: { type: string, enum: [draft, approved, closed] }
        version: { type: number }
        copiedFromPeriodId: { type: [string, "null"] }
        supersedesPeriodId: { type: [string, "null"] }
        createdBy: { type: string, pattern: "^[0-9a-fA-F-]{36}$" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        approvedAt: { type: [string, "null"], format: date-time }
        approvedBy: { type: [string, "null"], pattern: "^[0-9a-fA-F-]{36}$" }
        closedAt: { type: [string, "null"], format: date-time }
        closedBy: { type: [string, "null"], pattern: "^[0-9a-fA-F-]{36}$" }
        categories: { type: array, items: { $ref: "#/components/schemas/BudgetPlanningCategory" } }
        advisory:
          type: object
          required: [projectedExpenseTarget, reviewedHouseholdNetActivity, affectsOfficialTotals]
          properties:
            projectedExpenseTarget: { type: string }
            reviewedHouseholdNetActivity: { type: string }
            affectsOfficialTotals: { type: boolean }
    BudgetPlanningCategoryMutation:
      allOf: [{ $ref: "#/components/schemas/BudgetPlanningCategory" }, { type: object, required: [version], properties: { version: { type: number } } }]
    WeeklyBudgetGuidanceCategory:
      type: object
      required: [categoryId, allocationBasisPoints, recommendedMonthly, recommendedWeekly, eligibleActualSpending, remainingRecommendedAmount, status, reason, eligible]
      properties:
        categoryId: { type: string }
        allocationBasisPoints: { type: [number, "null"], minimum: 0, maximum: 10000, multipleOf: 1 }
        recommendedMonthly: { type: [string, "null"], pattern: "^-?[0-9]+\\.[0-9]{2}$" }
        recommendedWeekly: { type: [string, "null"], pattern: "^-?[0-9]+\\.[0-9]{2}$" }
        eligibleActualSpending: { type: string, pattern: "^-?[0-9]+\\.[0-9]{2}$" }
        remainingRecommendedAmount: { type: [string, "null"], pattern: "^-?[0-9]+\\.[0-9]{2}$" }
        status: { type: string, enum: [green, red, neutral] }
        reason: { type: string }
        eligible: { type: boolean }
    WeeklyBudgetGuidance:
      type: object
      required: [periodId, month, calculationDate, basis, verifiedIncome, includedIncomeCount, includedOutflowCount, exclusions, fingerprint, affectsOfficialTotals, categories]
      properties:
        periodId: { type: string }
        month: { type: string }
        calculationDate: { type: string, format: date }
        basis: { type: string }
        verifiedIncome: { type: string, pattern: "^[0-9]+\\.[0-9]{2}$" }
        includedIncomeCount: { type: number, minimum: 0 }
        includedOutflowCount: { type: number, minimum: 0 }
        exclusions: { type: object, additionalProperties: { type: number } }
        fingerprint: { type: string }
        affectsOfficialTotals: { type: boolean, enum: [false] }
        categories: { type: array, items: { $ref: "#/components/schemas/WeeklyBudgetGuidanceCategory" } }
    AcceptWeeklyBudgetGuidanceInput:
      type: object
      required: [version, categoryIds, recommendationFingerprint]
      properties:
        version: { type: number, minimum: 1 }
        categoryIds: { type: array, minItems: 1, items: { type: string } }
        recommendationFingerprint: { type: string, minLength: 1 }
    WeeklyBudgetAllocation:
      type: object
      required: [categoryId, basisPoints]
      properties:
        categoryId: { type: string, minLength: 1 }
        basisPoints: { type: number, minimum: 0, maximum: 10000, multipleOf: 1 }
    WeeklyBudgetAllocationTemplateInput:
      type: object
      required: [version, allocations]
      properties:
        version: { type: number, minimum: 1, multipleOf: 1 }
        allocations: { type: array, minItems: 1, items: { $ref: "#/components/schemas/WeeklyBudgetAllocation" } }
    WeeklyBudgetAllocationTemplateResult:
      type: object
      required: [periodId, version, totalBasisPoints, allocations]
      properties:
        periodId: { type: string }
        version: { type: number, minimum: 1, multipleOf: 1 }
        totalBasisPoints: { type: number, enum: [10000] }
        allocations: { type: object, additionalProperties: { type: number, minimum: 0, maximum: 10000, multipleOf: 1 } }
    AcceptWeeklyBudgetGuidanceResult:
      type: object
      required: [periodId, version, acceptedCategoryIds, targets, affectsOfficialTotals]
      properties:
        periodId: { type: string }
        version: { type: number }
        acceptedCategoryIds: { type: array, items: { type: string } }
        targets: { type: object, additionalProperties: { type: string, pattern: "^[0-9]+\\.[0-9]{2}$" } }
        affectsOfficialTotals: { type: boolean, enum: [false] }
    BudgetPlanningApproval:
      type: object
      required: [id, month, status, version, approvedAt]
      properties:
        id: { type: string }
        month: { type: string }
        status: { type: string, enum: [approved] }
        version: { type: number }
        approvedAt: { type: string, format: date-time }
        supersedesPeriodId: { type: [string, "null"] }
    BudgetPlanningReorderInput:
      type: object
      required: [version, categoryIds]
      properties:
        version: { type: number, minimum: 1 }
        categoryIds: { type: array, minItems: 1, items: { type: string } }
    BudgetPlanningReorderResult:
      type: object
      required: [version, categoryIds]
      properties:
        version: { type: number }
        categoryIds: { type: array, items: { type: string } }
    BudgetPlanningCloseResult:
      type: object
      required: [id, month, status, version, closedAt]
      properties:
        id: { type: string, pattern: "^[0-9a-fA-F-]{36}$" }
        month: { type: string, pattern: "^[0-9]{4}-(0[1-9]|1[0-2])$" }
        status: { type: string, enum: [closed] }
        version: { type: number, minimum: 1 }
        closedAt: { type: string, format: date-time }
    BudgetPlanningHistoryItem:
      type: object
      required: [id, month, status, version, createdBy, createdAt, updatedAt]
      properties:
        id: { type: string, pattern: "^[0-9a-fA-F-]{36}$" }
        month: { type: string, pattern: "^[0-9]{4}-(0[1-9]|1[0-2])$" }
        status: { type: string, enum: [draft, approved, closed] }
        version: { type: number, minimum: 1 }
        copiedFromPeriodId: { type: [string, "null"] }
        supersedesPeriodId: { type: [string, "null"] }
        createdBy: { type: string, pattern: "^[0-9a-fA-F-]{36}$" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        approvedAt: { type: [string, "null"], format: date-time }
        approvedBy: { type: [string, "null"], pattern: "^[0-9a-fA-F-]{36}$" }
        closedAt: { type: [string, "null"], format: date-time }
        closedBy: { type: [string, "null"], pattern: "^[0-9a-fA-F-]{36}$" }
    BudgetPlanningHistoryList:
      type: array
      items: { $ref: "#/components/schemas/BudgetPlanningHistoryItem" }
    BudgetPlanningComparison:
      type: object
      required: [month, monthBudgeted, quarterBudgeted, yearBudgeted, approvedPeriodCount]
      properties:
        month: { type: string, pattern: "^[0-9]{4}-(0[1-9]|1[0-2])$" }
        monthBudgeted: { type: string, pattern: "^-?[0-9]+\\.[0-9]{2}$" }
        quarterBudgeted: { type: string, pattern: "^-?[0-9]+\\.[0-9]{2}$" }
        yearBudgeted: { type: string, pattern: "^-?[0-9]+\\.[0-9]{2}$" }
        approvedPeriodCount: { type: number, minimum: 0 }
    BudgetPlanningContributionTransaction:
      type: object
      required: [id, transactionDate, description, amount, reviewStatus, businessTag, excludedFromBudget, pending]
      properties:
        id: { type: string, pattern: "^[0-9a-fA-F-]{36}$" }
        transactionDate: { type: string, format: date }
        description: { type: string }
        merchant: { type: [string, "null"] }
        amount: { type: string, pattern: "^-?[0-9]+\\.[0-9]{2}$" }
        reviewStatus: { type: string }
        businessTag: { type: string }
        excludedFromBudget: { type: boolean }
        pending: { type: boolean }
        categoryId: { type: [string, "null"] }
        transferGroupId: { type: [string, "null"] }
    BudgetPlanningContributionExclusions:
      type: object
      required: [uncategorized, excluded, business, transfers, unreviewed, rows]
      properties:
        uncategorized: { type: number, minimum: 0 }
        excluded: { type: number, minimum: 0 }
        business: { type: number, minimum: 0 }
        transfers: { type: number, minimum: 0 }
        unreviewed: { type: number, minimum: 0 }
        rows: { type: array, items: { $ref: "#/components/schemas/BudgetPlanningContributionTransaction" } }
    BudgetPlanningContributionDetail:
      type: object
      required: [category, includedReviewedHouseholdTransactions, includedActual, exclusions]
      properties:
        category: { $ref: "#/components/schemas/BudgetPlanningCategory" }
        includedReviewedHouseholdTransactions: { type: array, items: { $ref: "#/components/schemas/BudgetPlanningContributionTransaction" } }
        includedActual: { type: string, pattern: "^-?[0-9]+\\.[0-9]{2}$" }
        exclusions: { $ref: "#/components/schemas/BudgetPlanningContributionExclusions" }
    HealthStatus:
      type: object
      required: [status]
      properties:
        status:
          type: string
    OnboardHouseholdInput:
      type: object
      required: [name]
      properties:
        name: { type: string, minLength: 1, maxLength: 120 }
        timezone: { type: string, default: America/Chicago }
    ErrorResponse:
      type: object
      required: [code, message]
      properties:
        code: { type: string }
        message: { type: string }
        correlationId: { type: string, nullable: true }
        details:
          type: ["object", "null"]
          additionalProperties: true
    HouseholdSummary:
      type: object
      required: [id, name, timezone, role, permissions, privacy]
      properties:
        id: { type: string }
        name: { type: string }
        timezone: { type: string }
        role: { type: string }
        permissions:
          type: array
          items: { type: string }
        privacy:
          type: object
          required: [financeDataPrivate, shareHealthSummary, credentialsStored, bankActionsEnabled]
          properties:
            financeDataPrivate: { type: boolean }
            shareHealthSummary: { type: boolean }
            credentialsStored: { type: boolean }
            bankActionsEnabled: { type: boolean }
    PrivacySettingsInput:
      type: object
      required: [financeDataPrivate, shareHealthSummary]
      properties:
        financeDataPrivate: { type: boolean }
        shareHealthSummary: { type: boolean }
    AccountSummary:
      type: object
      required: [id, name, accountType, balance, protected, riskClass]
      properties:
        id: { type: string }
        name: { type: string }
        accountType: { type: string }
        balance: { type: string }
        protected: { type: boolean }
        riskClass: { type: string }
    GoalSummary:
      type: object
      required: [id, name, targetAmount, currentAmount, protectedAmount, weeklyContribution, progressPercent, amountRemaining, weeksRemaining, requiredWeeklyContribution, projectedCompletionWeeks, status, priority, targetDate]
      properties:
        id: { type: string }
        name: { type: string }
        targetAmount: { type: string }
        currentAmount: { type: string }
        protectedAmount: { type: string }
        weeklyContribution: { type: string }
        progressPercent: { type: number }
        amountRemaining: { type: string }
        weeksRemaining: { type: number }
        requiredWeeklyContribution: { type: string }
        projectedCompletionWeeks:
          type: ["number", "null"]
        status: { type: string }
        priority: { type: string }
        targetDate: { type: string, format: date }
    AllocationSummary:
      type: object
      required: [totalWeekly, duplexReserve, capitalOs, opportunityReserve]
      properties:
        totalWeekly: { type: string }
        duplexReserve: { type: string }
        capitalOs: { type: string }
        opportunityReserve: { type: string }
    ContributionSummary:
      type: object
      required: [id, amount, status, createdAt, metadata]
      properties:
        id: { type: string }
        amount: { type: string }
        status: { type: string }
        createdAt: { type: string, format: date-time }
        idempotencyKey: { type: string }
        metadata:
          type: object
          additionalProperties: true
    ContributionInput:
      type: object
      required: [amount]
      properties:
        amount: { type: string, pattern: "^[0-9]+(\\.[0-9]{1,2})?$" }
        goalId: { type: ["string", "null"] }
        note: { type: ["string", "null"], maxLength: 500 }
    TransferInput:
      type: object
      required: [sourceAccountId, destinationAccountId, amount]
      properties:
        sourceAccountId: { type: string }
        destinationAccountId: { type: string }
        amount: { type: string, pattern: "^[0-9]+(\\.[0-9]{1,2})?$" }
        note: { type: ["string", "null"], maxLength: 500 }
    AllocationInput:
      type: object
      required: [totalWeekly, duplexReserve, capitalOs, opportunityReserve]
      properties:
        totalWeekly: { type: string, pattern: "^[0-9]+(\\.[0-9]{1,2})?$" }
        duplexReserve: { type: string, pattern: "^[0-9]+(\\.[0-9]{1,2})?$" }
        capitalOs: { type: string, pattern: "^[0-9]+(\\.[0-9]{1,2})?$" }
        opportunityReserve: { type: string, pattern: "^[0-9]+(\\.[0-9]{1,2})?$" }
    AllocationImpact:
      type: object
      required: [current, proposed, totalMatches, duplexWeeklyChange, projectedDelayWeeks, protectedSavingsChange, activeCapitalChange, propertyReadinessImpact]
      properties:
        current: { $ref: "#/components/schemas/AllocationSummary" }
        proposed: { $ref: "#/components/schemas/AllocationSummary" }
        totalMatches: { type: boolean }
        duplexWeeklyChange: { type: string }
        projectedDelayWeeks: { type: number }
        protectedSavingsChange: { type: string }
        activeCapitalChange: { type: string }
        propertyReadinessImpact: { type: string }
    ReportDescriptor:
      type: object
      required: [kind, title, status, private, hashable]
      properties:
        kind: { type: string }
        title: { type: string }
        status: { type: string }
        private: { type: boolean }
        hashable: { type: boolean }
    TransactionSummary:
      type: object
      required: [id, amount, category, status, createdAt]
      properties:
        id: { type: string }
        amount: { type: string }
        category: { type: string }
        status: { type: string }
        createdAt: { type: string, format: date-time }
    PortfolioSummary:
      type: object
      required: [totalCapital, protectedCapital, activeCapital, cashReserve, ledgerBalanced, composition]
      properties:
        totalCapital: { type: string }
        protectedCapital: { type: string }
        activeCapital: { type: string }
        cashReserve: { type: string }
        ledgerBalanced: { type: boolean }
        composition:
          type: array
          items:
            type: object
            required: [label, amount, percent]
            properties:
              label: { type: string }
              amount: { type: string }
              percent: { type: number }
    PropertyMilestone:
      type: object
      required: [id, name, status, progress, target, currentState, nextAction]
      properties:
        id: { type: string }
        name: { type: string }
        status: { type: string }
        progress: { type: string }
        target: { type: string }
        currentState: { type: string }
        nextAction: { type: string }
        dueDate: { type: ["string", "null"], format: date }
    BuyBox:
      type: object
      required: [id, householdId, propertyType, ownerOccupied, purchasePriceMinimum, purchasePriceMaximum, targetCashToClose, minimumBedroomsPerUnit, minimumBathroomsPerUnit, minimumEstimatedRent, maximumEstimatedRehabilitation, minimumCashFlow, maximumMonthlyHousingCost, minimumDscrEstimate, minimumPropertyCondition, targetMarkets, excludedMarkets, minimumReadinessScore]
      properties:
        id: { type: string }
        householdId: { type: string }
        propertyType: { type: string }
        ownerOccupied: { type: boolean }
        purchasePriceMinimum: { type: string }
        purchasePriceMaximum: { type: string }
        targetCashToClose: { type: string }
        minimumBedroomsPerUnit: { type: string }
        minimumBathroomsPerUnit: { type: string }
        minimumEstimatedRent: { type: string }
        maximumEstimatedRehabilitation: { type: string }
        minimumCashFlow: { type: string }
        maximumMonthlyHousingCost: { type: string }
        minimumDscrEstimate: { type: string }
        minimumPropertyCondition: { type: string }
        targetMarkets: { type: array, items: { type: string } }
        excludedMarkets: { type: array, items: { type: string } }
        minimumReadinessScore: { type: string }
    UpdateBuyBoxInput:
      type: object
      properties:
        propertyType: { type: string }
        ownerOccupied: { type: boolean }
        purchasePriceMinimum: { type: string }
        purchasePriceMaximum: { type: string }
        targetCashToClose: { type: string }
        minimumBedroomsPerUnit: { type: string }
        minimumBathroomsPerUnit: { type: string }
        minimumEstimatedRent: { type: string }
        maximumEstimatedRehabilitation: { type: string }
        minimumCashFlow: { type: string }
        maximumMonthlyHousingCost: { type: string }
        minimumDscrEstimate: { type: string }
        minimumPropertyCondition: { type: string }
        targetMarkets: { type: array, items: { type: string } }
        excludedMarkets: { type: array, items: { type: string } }
        minimumReadinessScore: { type: string }
    CreatePropertyCandidateInput:
      type: object
      required: [propertyGoalId, addressLabel, propertyType, units, bedrooms, askingPrice, estimatedRent, repairs, market]
      properties:
        propertyGoalId: { type: string }
        addressLabel: { type: string }
        city: { type: string }
        state: { type: string }
        zip: { type: string }
        market: { type: string }
        propertyType: { type: string }
        units: { type: string }
        bedrooms: { type: string }
        bathrooms: { type: string }
        askingPrice: { type: string }
        estimatedMarketValue: { type: string }
        annualPropertyTaxes: { type: string }
        insurance: { type: string }
        hoa: { type: string }
        estimatedRent: { type: string }
        currentRents: { type: string }
        vacancyAssumption: { type: string }
        repairs: { type: string }
        immediateRepairs: { type: string }
        listingSource: { type: string }
        listingUrl: { type: string }
        status: { type: string }
        notes: { type: string }
    PropertyCandidate:
      type: object
      required: [id, propertyGoalId, addressLabel, propertyType, units, bedrooms, askingPrice, estimatedRent, repairs, status, buyBoxScore, dealQualityScore, dataConfidence]
      properties:
        id: { type: string }
        propertyGoalId: { type: string }
        addressLabel: { type: string }
        propertyType: { type: string }
        units: { type: string }
        bedrooms: { type: string }
        askingPrice: { type: string }
        estimatedRent: { type: string }
        repairs: { type: string }
        status: { type: string }
        buyBoxScore: { type: string }
        dealQualityScore: { type: string }
        dataConfidence: { type: string }
        readinessStatus: { type: string }
        nextAction: { type: string }
    PropertyAnalysis:
      type: object
      required: [candidateId, deal, cashToClose, stress, governor]
      properties:
        candidateId: { type: string }
        deal:
          type: object
          properties:
            analysis:
              type: object
              properties:
                monthlyCashFlow: { type: string }
                annualCashFlow: { type: string }
                ownerEffectiveHousingCost: { type: string }
                noi: { type: string }
                capRate: { type: number }
                cashOnCashReturn: { type: number }
                dscr: { type: number }
            buyBox:
              type: object
              properties:
                score: { type: number }
                failures: { type: array, items: { type: string } }
        cashToClose:
          type: object
          properties:
            estimatedCashToClose: { type: string }
        stress:
          type: object
          properties:
            monthlyCashFlowCents: { type: number }
            emergencyReserveRemainingCents: { type: number }
            monthsUntilLiquidityBreach: { type: number }
            result: { type: string }
        governor:
          type: object
          properties:
            status: { type: string }
            reasons: { type: array, items: { type: string } }
    PropertyUnderwriting:
      type: object
      required: [propertyGoal, buyBox, candidates, financingScenarios, cashToClose, stressTests, preapprovals, markets, documents, milestones, nextAction, dataConfidence, household]
      properties:
        propertyGoal:
          type: object
          properties:
            id: { type: string }
            name: { type: string }
            targetMarket: { type: string }
            targetBudget: { type: string }
            targetCashToClose: { type: string }
            targetDate: { type: string }
            readiness:
              type: object
              properties:
                score: { type: number }
                status: { type: string }
                nextAction: { type: string }
        buyBox: { $ref: "#/components/schemas/BuyBox" }
        candidates: { type: array, items: { $ref: "#/components/schemas/PropertyCandidate" } }
        financingScenarios:
          type: array
          items:
            type: object
            properties:
              id: { type: string }
              name: { type: string }
              loanType: { type: string }
              purchasePrice: { type: string }
              loanAmount: { type: string }
              estimatedMonthlyHousingCost: { type: string }
        cashToClose:
          type: array
          items:
            type: object
            properties:
              id: { type: string }
              propertyCandidateId: { type: string }
              estimatedCashToClose: { type: string }
        stressTests:
          type: array
          items:
            type: object
            properties:
              id: { type: string }
              propertyCandidateId: { type: string }
              name: { type: string }
              monthlyCashFlow: { type: string }
              emergencyReserveRemaining: { type: string }
              result: { type: string }
        preapprovals:
          type: array
          items:
            type: object
            properties:
              id: { type: string }
              provider: { type: string }
              status: { type: string }
              estimatedMaximumPurchasePrice: { type: string }
              estimatedRate: { type: ["string", "null"] }
              estimatedCashRequired: { type: string }
              expiration: { type: ["string", "null"] }
              documentsNeeded: { type: string }
              notes: { type: string }
        markets:
          type: array
          items:
            type: object
            properties:
              id: { type: string }
              name: { type: string }
              score: { type: string }
              medianPrice: { type: string }
              rentYield: { type: string }
              notes: { type: string }
        documents:
          type: array
          items:
            type: object
            properties:
              id: { type: string }
              name: { type: string }
              storagePath: { type: ["string", "null"] }
              metadata: { type: object, properties: { status: { type: string }, private: { type: boolean }, description: { type: string } } }
        milestones:
          type: array
          items:
            $ref: "#/components/schemas/PropertyMilestone"
        nextAction: { type: string }
        dataConfidence: { type: number }
        household:
          type: object
          properties:
            emergencyReserve: { type: string }
            emergencyReserveMonths: { type: number }
            freeCashFlow: { type: string }
            safeToDeploy: { type: string }
    PropertySummary:
      type: object
      required: [id, name, targetMarket, targetBudget, estimatedDownPayment, estimatedClosingCosts, readinessScore, milestones]
      properties:
        id: { type: string }
        name: { type: string }
        targetMarket: { type: string }
        targetBudget: { type: string }
        estimatedDownPayment: { type: string }
        estimatedClosingCosts: { type: string }
        readinessScore: { type: number }
        milestones:
          type: array
          items: { $ref: "#/components/schemas/PropertyMilestone" }
    PropertyNoteInput:
      type: object
      required: [propertyGoalId, body]
      properties:
        propertyGoalId: { type: string }
        body: { type: string, minLength: 1, maxLength: 2000 }
    PropertyNote:
      type: object
      required: [id, propertyGoalId, body, createdAt]
      properties:
        id: { type: string }
        propertyGoalId: { type: string }
        body: { type: string }
        createdAt: { type: string, format: date-time }
    StrategySummary:
      type: object
      required: [id, name, strategyType, stage, allocation, confidenceScore, riskLevel, enabled]
      properties:
        id: { type: string }
        name: { type: string }
        strategyType: { type: string }
        stage: { type: string }
        allocation: { type: string }
        confidenceScore: { type: number }
        riskLevel: { type: string }
        enabled: { type: boolean }
        graduationEvidence:
          type: object
          required: [minimumObservations, reconciliationAccurate, noCriticalErrors]
          properties:
            minimumObservations: { type: boolean }
            reconciliationAccurate: { type: boolean }
            noCriticalErrors: { type: boolean }
    CreateResearchStrategyInput:
      type: object
      required: [name, strategyType, description, hypothesis]
      properties:
        name: { type: string, minLength: 1, maxLength: 120 }
        strategyType: { type: string, minLength: 1, maxLength: 80 }
        description: { type: string, minLength: 1, maxLength: 1000 }
        hypothesis: { type: string, minLength: 1, maxLength: 2000 }
        executionModel: { type: string, enum: [optimistic, moderate, conservative, queue_aware] }
    ResearchStrategyCreated:
      type: object
      required: [id, versionId, name, stage, hypothesis]
      properties:
        id: { type: string }
        versionId: { type: string }
        name: { type: string }
        stage: { type: string }
        hypothesis: { type: string }
    CreateStrategyVersionInput:
      type: object
      required: [version, reason, logicChanges]
      properties:
        version: { type: string, minLength: 1, maxLength: 40 }
        reason: { type: string, minLength: 1, maxLength: 500 }
        logicChanges: { type: string, minLength: 1, maxLength: 2000 }
    StrategyVersionCreated:
      type: object
      required: [id, strategyId, version, configuration, createdAt]
      properties:
        id: { type: string }
        strategyId: { type: string }
        version: { type: string }
        configuration:
          type: object
          properties:
            reason: { type: string }
            logicChanges: { type: string }
            immutable: { type: boolean }
        createdAt: { type: string, format: date-time }
    RunStrategyExperimentInput:
      type: object
      required: [strategyId, strategyVersionId, name, mode, datasetVersion, executionModel, randomSeed]
      properties:
        strategyId: { type: string }
        strategyVersionId: { type: string }
        name: { type: string, minLength: 1, maxLength: 160 }
        mode: { type: string, enum: [backtest, walk_forward, shadow, paper] }
        datasetVersion: { type: string, minLength: 1, maxLength: 120 }
        executionModel: { type: string, enum: [optimistic, moderate, conservative, queue_aware] }
        randomSeed: { type: number, minimum: 1 }
    StrategyLabMetrics:
      type: object
      required: [orders, fills, trades, marketDays, totalReturnPct, profitFactor, expectancyBps, maxDrawdownPct, fillRatePct, partialFillRatePct, adverseSelectionBps, netEdgeBps, evidenceScore, criticalModelErrors, warning]
      properties:
        orders: { type: number }
        fills: { type: number }
        trades: { type: number }
        marketDays: { type: number }
        independentEvents: { type: number }
        totalReturnPct: { type: number }
        annualizedReturnPct: { type: number }
        profitFactor: { type: number }
        winRatePct: { type: number }
        averageWinBps: { type: number }
        averageLossBps: { type: number }
        expectancyBps: { type: number }
        maxDrawdownPct: { type: number }
        averageDrawdownPct: { type: number }
        recoveryTimeDays: { type: number }
        sharpeRatio: { type: number }
        sortinoRatio: { type: number }
        calmarRatio: { type: number }
        volatilityPct: { type: number }
        exposurePct: { type: number }
        turnover: { type: number }
        fillRatePct: { type: number }
        partialFillRatePct: { type: number }
        adverseSelectionBps: { type: number }
        grossSpreadCaptureBps: { type: number }
        rebatesBps: { type: number }
        feeBps: { type: number }
        slippageBps: { type: number }
        hedgeCostBps: { type: number }
        inventoryLossBps: { type: number }
        netEdgeBps: { type: number }
        latencyMs: { type: number }
        inventoryPeak: { type: number }
        inventoryLimitBreaches: { type: number }
        averageHoldingMinutes: { type: number }
        evidenceScore: { type: number }
        dataQuality: { type: string }
        criticalModelErrors: { type: number }
        warning: { type: string }
    StrategyGraduationGate:
      type: object
      required: [name, passed]
      properties:
        name: { type: string }
        passed: { type: boolean }
    StrategyGraduationReview:
      type: object
      required: [strategyId, strategyName, eligible, decision, nextStage, gates, liveTradingEnabled, note]
      properties:
        strategyId: { type: string }
        strategyName: { type: string }
        eligible: { type: boolean }
        decision: { type: string }
        nextStage: { type: string }
        gates:
          type: array
          items: { $ref: "#/components/schemas/StrategyGraduationGate" }
        liveTradingEnabled: { type: boolean }
        note: { type: string }
    StrategyLabExperiment:
      type: object
      required: [id, strategyId, strategyName, strategyVersionId, name, mode, datasetVersion, status, randomSeed, softwareVersion, metrics, createdAt]
      properties:
        id: { type: string }
        strategyId: { type: string }
        strategyName: { type: string }
        strategyVersionId: { type: string }
        name: { type: string }
        mode: { type: string }
        datasetVersion: { type: string }
        status: { type: string }
        randomSeed: { type: number }
        softwareVersion: { type: string }
        metrics: { $ref: "#/components/schemas/StrategyLabMetrics" }
        failureReason: { type: ["string", "null"] }
        createdAt: { type: string, format: date-time }
        completedAt: { type: ["string", "null"], format: date-time }
    MicroLiveGate:
      type: object
      required: [name, passed]
      properties:
        name: { type: string }
        passed: { type: boolean }
    MicroLivePositionSnapshot:
      type: object
      required: [id, householdId, marketId, source, quantity, averagePrice, markPrice, notional, capturedAt]
      properties:
        id: { type: string }
        householdId: { type: string }
        sessionId: { type: ["string", "null"] }
        venueId: { type: ["string", "null"] }
        marketId: { type: string }
        source: { type: string }
        quantity: { type: string }
        averagePrice: { type: string }
        markPrice: { type: string }
        notional: { type: string }
        metadata:
          type: object
          additionalProperties: true
        capturedAt: { type: string, format: date-time }
    MicroLiveFillSnapshot:
      type: object
      required: [id, householdId, externalFillId, marketId, source, side, quantity, price, fee, capturedAt]
      properties:
        id: { type: string }
        householdId: { type: string }
        sessionId: { type: ["string", "null"] }
        venueId: { type: ["string", "null"] }
        externalFillId: { type: string }
        marketId: { type: string }
        source: { type: string }
        side: { type: string }
        quantity: { type: string }
        price: { type: string }
        fee: { type: string }
        metadata:
          type: object
          additionalProperties: true
        capturedAt: { type: string, format: date-time }
    MicroLiveReconciliationMismatches:
      type: object
      required: [positionMismatch, orderMismatch, missingInternalFills, orphanedInternalFills]
      properties:
        positionMismatch: { type: boolean }
        orderMismatch: { type: boolean }
        missingInternalFills: { type: array, items: { type: string } }
        orphanedInternalFills: { type: array, items: { type: string } }
    MicroLiveReconciliationRun:
      type: object
      required: [id, householdId, status, mismatches, internalState, venueState, completedAt]
      properties:
        id: { type: string }
        householdId: { type: string }
        sessionId: { type: ["string", "null"] }
        status: { type: string }
        mismatches: { $ref: "#/components/schemas/MicroLiveReconciliationMismatches" }
        internalState:
          type: object
          additionalProperties: true
        venueState:
          type: object
          additionalProperties: true
        completedAt: { type: string, format: date-time }
    MicroLiveIncident:
      type: object
      required: [id, householdId, severity, incidentType, title, timeline, capitalImpact, status, createdAt, resolvedAt, hasReview, openRequirementCount]
      properties:
        id: { type: string }
        householdId: { type: string }
        sessionId: { type: ["string", "null"] }
        severity: { type: string }
        incidentType: { type: string }
        title: { type: string }
        timeline: { type: array, items: { type: string } }
        capitalImpact: { type: string }
        status: { type: string }
        createdAt: { type: string, format: date-time }
        resolvedAt: { type: ["string", "null"], format: date-time }
        hasReview: { type: boolean }
        openRequirementCount: { type: number }
    MicroLiveIncidentReviewInput:
      type: object
      required: [rootCause, capitalImpact, safeguardsWorked, requiredFixes, reactivationRequirements]
      properties:
        rootCause: { type: string, minLength: 1, maxLength: 2000 }
        capitalImpact: { type: string, pattern: "^-?[0-9]+(\\.[0-9]{1,2})?$" }
        safeguardsWorked: { type: array, items: { type: string, minLength: 1 }, maxItems: 20 }
        requiredFixes: { type: array, items: { type: string, minLength: 1 }, maxItems: 20 }
        reactivationRequirements: { type: array, minItems: 1, items: { type: string, minLength: 1 }, maxItems: 20 }
        notes: { type: ["string", "null"], maxLength: 2000 }
    MicroLiveReactivationRequirement:
      type: object
      required: [id, householdId, incidentId, reviewId, requirement, status, createdAt]
      properties:
        id: { type: string }
        householdId: { type: string }
        incidentId: { type: string }
        reviewId: { type: string }
        requirement: { type: string }
        status: { type: string }
        completedBy: { type: ["string", "null"] }
        completedAt: { type: ["string", "null"], format: date-time }
        createdAt: { type: string, format: date-time }
    MicroLiveIncidentReview:
      type: object
      required: [id, householdId, incidentId, reviewedBy, rootCause, capitalImpact, safeguardsWorked, requiredFixes, reactivationRequirements, reviewedAt, requirements]
      properties:
        id: { type: string }
        householdId: { type: string }
        incidentId: { type: string }
        reviewedBy: { type: string }
        rootCause: { type: string }
        capitalImpact: { type: string }
        safeguardsWorked: { type: array, items: { type: string } }
        requiredFixes: { type: array, items: { type: string } }
        reactivationRequirements: { type: array, items: { type: string } }
        notes: { type: ["string", "null"] }
        reviewedAt: { type: string, format: date-time }
        requirements:
          type: array
          items: { $ref: "#/components/schemas/MicroLiveReactivationRequirement" }
    MicroLiveVenueApprovalRequest:
      type: object
      required: [credentialsReference, jurisdictionConfirmed, termsReviewed, marketPermissions, withdrawalReviewed, withdrawalDisabled]
      properties:
        credentialsReference: { type: string, pattern: "^secret://capital-os/venues/[A-Za-z0-9_-]{8,128}$" }
        jurisdictionConfirmed: { type: boolean }
        termsReviewed: { type: boolean }
        marketPermissions: { type: array, minItems: 1, items: { type: string, minLength: 1 } }
        withdrawalReviewed: { type: boolean }
        withdrawalDisabled: { type: boolean }
    MicroLiveVenueReviewRequest:
      type: object
      required: [reviewReference]
      properties:
        reviewReference: { type: string, pattern: "^review://capital-os/(security|jurisdiction)/[A-Za-z0-9_-]{8,128}$" }
    MicroLiveVenueReview:
      type: object
      required: [venueId, reviewType, reviewedBy, reviewedAt, expiresAt, liveExecutionEnabled]
      properties:
        venueId: { type: string }
        reviewType: { type: string, enum: [security, jurisdiction] }
        reviewedBy: { type: string }
        reviewedAt: { type: string, format: date-time }
        expiresAt: { type: string, format: date-time }
        liveExecutionEnabled: { type: boolean }
    MicroLiveVenueApproval:
      type: object
      required: [venue, liveExecutionEnabled, householdCapitalAccessible, protectedCapitalAccessible]
      properties:
        venue:
          type: object
          required: [id, name, adapterType, status, marketPermissions, approval]
          properties:
            id: { type: string }
            name: { type: string }
            adapterType: { type: string }
            status: { type: string }
            marketPermissions: { type: array, items: { type: string } }
            approval:
              type: object
              required: [approved, status, checks, note]
              properties:
                approved: { type: boolean }
                status: { type: string }
                checks:
                  type: array
                  items: { $ref: "#/components/schemas/MicroLiveGate" }
                note: { type: string }
        liveExecutionEnabled: { type: boolean }
        householdCapitalAccessible: { type: boolean }
        protectedCapitalAccessible: { type: boolean }
    MicroLiveArmRequest:
      type: object
      required: [venueId]
      properties:
        venueId: { type: string }
    MicroLiveOrderRequest:
      type: object
      required: [marketId, side, orderType, quantity, quoteCycle, referencePriceBps, priceBps, marketDataAgeMs]
      properties:
        marketId: { type: string, minLength: 1, maxLength: 120 }
        side: { type: string, enum: [buy, sell] }
        orderType: { type: string, enum: [limit, market] }
        quantity: { type: number, exclusiveMinimum: 0 }
        price: { type: number, exclusiveMinimum: 0 }
        strategyId: { type: string, pattern: "^[0-9a-fA-F-]{36}$" }
        strategyVersionId: { type: string, pattern: "^[0-9a-fA-F-]{36}$" }
        quoteCycle: { type: string, minLength: 1, maxLength: 120 }
        referencePriceBps: { type: number, exclusiveMinimum: 0 }
        priceBps: { type: number, exclusiveMinimum: 0 }
        marketDataAgeMs: { type: number, minimum: 0 }
        postOnly: { type: boolean }
    ExecutionControlCommandInput:
      type: object
      required: [reason]
      properties:
        reason: { type: string, minLength: 1, maxLength: 500 }
    ExecutionControlState:
      type: object
      required: [id, householdId, state, version, reason, changedBy, changedAt, correlationId, createdAt, updatedAt, availableTransitions, executionPermitted, idempotent]
      properties:
        id: { type: string }
        householdId: { type: string }
        state: { type: string, enum: [DISABLED, MICRO_LIVE_ELIGIBLE, MICRO_LIVE_ARMED, MICRO_LIVE_ACTIVE, SAFE_MODE, STOP, EVACUATE, LOCKED] }
        version: { type: number }
        reason: { type: string }
        changedBy: { type: ["string", "null"] }
        changedAt: { type: string, format: date-time }
        correlationId: { type: ["string", "null"] }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        availableTransitions: { type: array, items: { type: string } }
        executionPermitted: { type: boolean }
        idempotent: { type: boolean }
    MicroLiveOrderResponse:
      type: object
      required: [orderIntentId, clientOrderId, state, idempotent, venueOrder, reconciliation]
      properties:
        orderIntentId: { type: string }
        clientOrderId: { type: string }
        state: { type: string }
        idempotent: { type: boolean }
        venueOrder:
          type: ["object", "null"]
          additionalProperties: true
        reconciliation:
          type: ["object", "null"]
          additionalProperties: true
    MicroLivePolicy:
      type: object
      required: [version, limits, autoScale, leverageEnabled, marginEnabled, borrowingEnabled]
      properties:
        version: { type: string }
        limits:
          type: object
          additionalProperties: { type: [string, number, boolean] }
        autoScale: { type: boolean }
        leverageEnabled: { type: boolean }
        marginEnabled: { type: boolean }
        borrowingEnabled: { type: boolean }
    MicroLiveReadiness:
      type: object
      required: [score, checks, status, liveExecutionEnabled]
      properties:
        score: { type: number }
        checks:
          type: array
          items: { $ref: "#/components/schemas/MicroLiveGate" }
        status: { type: string }
        liveExecutionEnabled: { type: boolean }
    MicroLiveRehearsal:
      type: object
      required: [mode, status, liveOrderTransmission, sequence, validation, chaosTests, note]
      properties:
        mode: { type: string }
        status: { type: string }
        liveOrderTransmission: { type: boolean }
        sequence: { type: array, items: { type: string } }
        validation:
          type: object
          required: [accepted, failures, state]
          properties:
            accepted: { type: boolean }
            failures: { type: array, items: { type: string } }
            state: { type: string }
        chaosTests:
          type: array
          items:
            type: object
            required: [name, result, expectedState]
            properties:
              name: { type: string }
              result: { type: string }
              expectedState: { type: string }
        note: { type: string }
    MicroLiveSnapshot:
      type: object
      required: [status, policy, session, venues, readiness, enablement, guardian, reconciliation, reconciliationRuns, positionSnapshots, fillSnapshots, rehearsal, timeline, incidents, incidentReviews, reactivationRequirements, events, safety]
      properties:
        status: { type: string }
        policy: { $ref: "#/components/schemas/MicroLivePolicy" }
        session:
          type: object
          required: [id, mode, capitalAllocated, currentPosition, openOrders, netPnl, lossLimit, exposureCap]
          properties:
            id: { type: string }
            mode: { type: string }
            capitalAllocated: { type: string }
            currentPosition: { type: string }
            openOrders: { type: number }
            netPnl: { type: string }
            lossLimit: { type: string }
            exposureCap: { type: string }
            expiresAt: { type: ["string", "null"], format: date-time }
        venues:
          type: array
          items:
            type: object
            required: [id, name, adapterType, status, capabilities, jurisdictionConfirmed, integrationApproved, credentialsConfigured, termsReviewed, marketPermissions, withdrawalReviewed, withdrawalDisabled, approval, health]
            properties:
              id: { type: string }
              name: { type: string }
              adapterType: { type: string }
              status: { type: string }
              capabilities:
                type: object
                additionalProperties: { type: boolean }
              jurisdictionConfirmed: { type: boolean }
              integrationApproved: { type: boolean }
              credentialsConfigured: { type: boolean }
              termsReviewed: { type: boolean }
              marketPermissions: { type: array, items: { type: string } }
              withdrawalReviewed: { type: boolean }
              withdrawalDisabled: { type: boolean }
              approval:
                type: object
                required: [approved, status, checks, note]
                properties:
                  approved: { type: boolean }
                  status: { type: string }
                  checks:
                    type: array
                    items: { $ref: "#/components/schemas/MicroLiveGate" }
                  note: { type: string }
              health: { type: string }
        readiness: { $ref: "#/components/schemas/MicroLiveReadiness" }
        enablement:
          type: object
          required: [enabled, status, gates, note]
          properties:
            enabled: { type: boolean }
            status: { type: string }
            gates:
              type: array
              items: { $ref: "#/components/schemas/MicroLiveGate" }
            note: { type: string }
        guardian:
          type: object
          required: [status, decision, reason, independentDeployment]
          properties:
            status: { type: string }
            lastHeartbeatAt: { type: ["string", "null"], format: date-time }
            decision: { type: string }
            reason: { type: string }
            independentDeployment: { type: string }
        reconciliation:
          type: object
          required: [runId, status, action, mismatches, completedAt]
          properties:
            runId: { type: ["string", "null"] }
            status: { type: string }
            action: { type: string }
            mismatches: { $ref: "#/components/schemas/MicroLiveReconciliationMismatches" }
            completedAt: { type: ["string", "null"], format: date-time }
        reconciliationRuns:
          type: array
          items: { $ref: "#/components/schemas/MicroLiveReconciliationRun" }
        positionSnapshots:
          type: array
          items: { $ref: "#/components/schemas/MicroLivePositionSnapshot" }
        fillSnapshots:
          type: array
          items: { $ref: "#/components/schemas/MicroLiveFillSnapshot" }
        rehearsal: { $ref: "#/components/schemas/MicroLiveRehearsal" }
        timeline: { type: array, items: { type: string } }
        incidents:
          type: array
          items: { $ref: "#/components/schemas/MicroLiveIncident" }
        incidentReviews:
          type: array
          items: { $ref: "#/components/schemas/MicroLiveIncidentReview" }
        reactivationRequirements:
          type: array
          items: { $ref: "#/components/schemas/MicroLiveReactivationRequirement" }
        events:
          type: array
          items:
            type: object
            properties:
              id: { type: string }
              eventType: { type: string }
              fromState: { type: ["string", "null"] }
              toState: { type: ["string", "null"] }
              createdAt: { type: string, format: date-time }
        safety:
          type: object
          required: [liveOrderTransmissionEnabled, householdCapitalAccessible, protectedCapitalAccessible, autoScale, aiCanPlaceOrders, aiCanChangeRisk]
          properties:
            liveOrderTransmissionEnabled: { type: boolean }
            householdCapitalAccessible: { type: boolean }
            protectedCapitalAccessible: { type: boolean }
            autoScale: { type: boolean }
            aiCanPlaceOrders: { type: boolean }
            aiCanChangeRisk: { type: boolean }
    MicroLiveEnablementReview:
      type: object
      required: [status, readiness, enablement, liveExecutionEnabled]
      properties:
        status: { type: string }
        readiness: { $ref: "#/components/schemas/MicroLiveReadiness" }
        enablement:
          type: object
          required: [enabled, status, gates, note]
          properties:
            enabled: { type: boolean }
            status: { type: string }
            gates:
              type: array
              items: { $ref: "#/components/schemas/MicroLiveGate" }
            note: { type: string }
        liveExecutionEnabled: { type: boolean }
    StrategyLabStrategy:
      type: object
      required: [id, name, description, strategyType, stage, status, owner, version, hypothesis, markets, venues, assets, timeHorizon, requiredData, assumptions, knownRisks, confidenceScore, allocation, enabled, latestExperimentId, graduation]
      properties:
        id: { type: string }
        name: { type: string }
        description: { type: string }
        strategyType: { type: string }
        stage: { type: string }
        status: { type: string }
        owner: { type: string }
        version: { type: string }
        versionId: { type: ["string", "null"] }
        hypothesis: { type: string }
        markets: { type: array, items: { type: string } }
        venues: { type: array, items: { type: string } }
        assets: { type: array, items: { type: string } }
        timeHorizon: { type: string }
        requiredData: { type: array, items: { type: string } }
        assumptions: { type: array, items: { type: string } }
        knownRisks: { type: array, items: { type: string } }
        confidenceScore: { type: number }
        allocation: { type: string }
        enabled: { type: boolean }
        latestExperimentId: { type: ["string", "null"] }
        latestMetrics: { $ref: "#/components/schemas/StrategyLabMetrics" }
        graduation: { $ref: "#/components/schemas/StrategyGraduationReview" }
    ResearchJournalEntry:
      type: object
      required: [id, strategyId, entryType, title, body, createdAt]
      properties:
        id: { type: string }
        strategyId: { type: string }
        entryType: { type: string }
        title: { type: string }
        body: { type: string }
        createdAt: { type: string, format: date-time }
    CreateResearchJournalEntryInput:
      type: object
      required: [strategyId, entryType, title, body]
      properties:
        strategyId: { type: string }
        entryType: { type: string, minLength: 1, maxLength: 40 }
        title: { type: string, minLength: 1, maxLength: 160 }
        body: { type: string, minLength: 1, maxLength: 3000 }
    StrategyLabSnapshot:
      type: object
      required: [overview, stageCounts, strategies, experiments, journal, paperAccount, safety]
      properties:
        overview:
          type: object
          required: [totalStrategiesTested, researchStrategies, backtestsRunning, shadowStrategies, paperStrategies, microLiveEligible, approvedStrategies, retiredStrategies, profitableExperiments, unprofitableExperiments, bestRiskAdjustedStrategy, highestConfidenceStrategy, lowestDrawdownStrategy, mostDataCollected, latestFailure]
          properties:
            totalStrategiesTested: { type: number }
            researchStrategies: { type: number }
            backtestsRunning: { type: number }
            shadowStrategies: { type: number }
            paperStrategies: { type: number }
            microLiveEligible: { type: number }
            approvedStrategies: { type: number }
            retiredStrategies: { type: number }
            profitableExperiments: { type: number }
            unprofitableExperiments: { type: number }
            bestRiskAdjustedStrategy: { type: string }
            highestConfidenceStrategy: { type: string }
            lowestDrawdownStrategy: { type: string }
            mostDataCollected: { type: string }
            latestFailure: { type: string }
        stageCounts:
          type: object
          properties:
            research: { type: number }
            backtest: { type: number }
            shadow: { type: number }
            paper: { type: number }
            micro_live: { type: number }
            approved: { type: number }
            production: { type: number }
            paused: { type: number }
            retired: { type: number }
        strategies:
          type: array
          items: { $ref: "#/components/schemas/StrategyLabStrategy" }
        experiments:
          type: array
          items: { $ref: "#/components/schemas/StrategyLabExperiment" }
        journal:
          type: array
          items: { $ref: "#/components/schemas/ResearchJournalEntry" }
        paperAccount:
          type: object
          required: [startingCapital, virtualCash, virtualPositions, virtualOrders, virtualFills, maxOrder, maxMarketExposure, maxStrategyExposure, maxDailyLoss, maxTotalDrawdownPct, killSwitch]
          properties:
            startingCapital: { type: string }
            virtualCash: { type: string }
            virtualPositions: { type: number }
            virtualOrders: { type: number }
            virtualFills: { type: number }
            maxOrder: { type: string }
            maxMarketExposure: { type: string }
            maxStrategyExposure: { type: string }
            maxDailyLoss: { type: string }
            maxTotalDrawdownPct: { type: number }
            killSwitch: { type: string }
        safety:
          type: object
          required: [liveExecutionEnabled, liveCredentialsRequired, realOrderSubmissionEnabled, protectedCapitalAccessible, note]
          properties:
            liveExecutionEnabled: { type: boolean }
            liveCredentialsRequired: { type: boolean }
            realOrderSubmissionEnabled: { type: boolean }
            protectedCapitalAccessible: { type: boolean }
            note: { type: string }
    StrategyPromotionInput:
      type: object
      required: [toStage, evidence]
      properties:
        toStage: { type: string }
        authorizedOverride: { type: boolean, default: false }
        evidence:
          type: object
          required: [minimumObservations, reconciliationAccurate, noCriticalErrors]
          properties:
            minimumObservations: { type: boolean }
            reconciliationAccurate: { type: boolean }
            noCriticalErrors: { type: boolean }
    StrategyAllocationInput:
      type: object
      required: [sourceAccountId, amount]
      properties:
        sourceAccountId: { type: string }
        amount: { type: string, pattern: "^[0-9]+(\\.[0-9]{1,2})?$" }
    RiskSummary:
      type: object
      required: [state, protectedCapitalLocked, emergencyStopActive, maxActiveCapital, maxStrategyAllocation, maxWeeklyRisk, maxDrawdown, minimumCashReserve, safeguards]
      properties:
        state: { type: string }
        protectedCapitalLocked: { type: boolean }
        emergencyStopActive: { type: boolean }
        maxActiveCapital: { type: string }
        maxStrategyAllocation: { type: string }
        maxWeeklyRisk: { type: string }
        maxDrawdown: { type: string }
        minimumCashReserve: { type: string }
        safeguards:
          type: array
          items:
            type: object
            required: [name, status, message]
            properties:
              name: { type: string }
              status: { type: string }
              message: { type: string }
    EmergencyStopInput:
      type: object
      required: [confirmed, reason]
      properties:
        confirmed: { type: boolean }
        reason: { type: string, minLength: 1, maxLength: 500 }
    RecommendationSummary:
      type: object
      required: [id, recommendation, rationale, expectedBenefit, riskImpact, confidence, status, advisoryOnly]
      properties:
        id: { type: string }
        recommendation: { type: string }
        rationale: { type: string }
        expectedBenefit: { type: string }
        riskImpact: { type: string }
        confidence: { type: number }
        status: { type: string }
        advisoryOnly: { type: boolean }
    RecommendationDecisionInput:
      type: object
      required: [decision, reason]
      properties:
        decision: { type: string, enum: [approved, rejected] }
        reason: { type: string, minLength: 1, maxLength: 500 }
    IntelligenceSnapshot:
      type: object
      required: [recommendation, analysts, insights, dailyBrief, weeklyReport, monthlyReview]
      properties:
        recommendation: { $ref: "#/components/schemas/IntelligenceRecommendation" }
        analysts:
          type: array
          items: { $ref: "#/components/schemas/IntelligenceAnalyst" }
        insights:
          type: array
          items: { $ref: "#/components/schemas/IntelligenceInsight" }
        dailyBrief: { $ref: "#/components/schemas/DailyBrief" }
        weeklyReport: { $ref: "#/components/schemas/WeeklyIntelligenceReport" }
        monthlyReview: { $ref: "#/components/schemas/MonthlyFamilyCapitalReview" }
    IntelligenceRecommendation:
      type: object
      required: [id, analyst, priority, recommendation, reason, evidence, expectedBenefit, potentialDownside, impactOnDuplexGoal, impactOnHouseholdLiquidity, riskLevel, confidence, dataQuality, status, requiredApproval, suggestedNextAction, advisoryOnly, createdAt]
      properties:
        id: { type: string }
        analyst: { type: string }
        priority: { type: string }
        recommendation: { type: string }
        reason: { type: string }
        evidence: { type: array, items: { type: string } }
        expectedBenefit: { type: string }
        potentialDownside: { type: string }
        impactOnDuplexGoal: { type: string }
        impactOnHouseholdLiquidity: { type: string }
        riskLevel: { type: string }
        confidence: { type: number }
        dataQuality: { type: string }
        status: { type: string }
        requiredApproval: { type: string }
        suggestedNextAction: { type: string }
        advisoryOnly: { type: boolean }
        createdAt: { type: string, format: date-time }
    IntelligenceAnalyst:
      type: object
      required: [id, analyst, scope, summary, dataQuality, confidence, evidence, createdAt]
      properties:
        id: { type: string }
        analyst: { type: string }
        scope: { type: string }
        summary: { type: string }
        dataQuality: { type: string }
        confidence: { type: number }
        evidence: { type: array, items: { type: string } }
        createdAt: { type: string, format: date-time }
    IntelligenceInsight:
      type: object
      required: [id, scope, title, description, severity, evidence, createdAt]
      properties:
        id: { type: string }
        scope: { type: string }
        title: { type: string }
        description: { type: string }
        severity: { type: string }
        evidence: { type: array, items: { type: string } }
        createdAt: { type: string, format: date-time }
    DailyBrief:
      type: object
      required: [status, safeToDeploy, emergencyReserveMonths, propertyCandidates, topRecommendation]
      properties:
        status: { type: string }
        safeToDeploy: { type: string }
        emergencyReserveMonths: { type: number }
        propertyCandidates: { type: number }
        topRecommendation: { type: string }
    WeeklyIntelligenceReport:
      type: object
      required: [title, summary, sections, generatedAt]
      properties:
        title: { type: string }
        summary: { type: string }
        sections: { type: array, items: { type: string } }
        generatedAt: { type: string, format: date-time }
    MonthlyFamilyCapitalReview:
      type: object
      required: [openingNetWorth, closingNetWorth, change, income, expenses, savings, investments, duplexProgress, portfolioPerformance, propertyProgress, riskReview, topFinancialDecisions, nextMonthPriorities]
      properties:
        openingNetWorth: { type: string }
        closingNetWorth: { type: string }
        change: { type: string }
        income: { type: string }
        expenses: { type: string }
        savings: { type: string }
        investments: { type: string }
        duplexProgress: { type: string }
        portfolioPerformance: { type: string }
        propertyProgress: { type: string }
        riskReview: { type: string }
        topFinancialDecisions: { type: array, items: { type: string } }
        nextMonthPriorities: { type: array, items: { type: string } }
    IntelligenceFeedbackInput:
      type: object
      required: [recommendationId, feedback]
      properties:
        recommendationId: { type: string }
        feedback: { type: string, enum: [helpful, not_helpful, implemented, dismissed] }
        note: { type: string, maxLength: 500 }
    RecommendationFeedback:
      type: object
      required: [id, recommendationId, feedback, createdAt, advisoryOnly]
      properties:
        id: { type: string }
        recommendationId: { type: string }
        feedback: { type: string }
        note: { type: ["string", "null"] }
        createdAt: { type: string, format: date-time }
        advisoryOnly: { type: boolean }
    FamilyOfficeResearchInput:
      type: object
      required: [scope, prompt]
      properties:
        analyst: { type: string, maxLength: 80 }
        scope: { type: string, minLength: 1, maxLength: 120 }
        prompt: { type: string, minLength: 1, maxLength: 4000 }
    FamilyOfficeEvidence:
      type: object
      required: [id, title, excerpt, sourceKind, classification, freshness, confidence, createdAt]
      properties:
        id: { type: string }
        title: { type: string }
        excerpt: { type: string }
        sourceKind: { type: string }
        sourceUrl: { type: ["string", "null"] }
        classification: { type: string }
        freshness: { type: string }
        confidence: { type: number }
        createdAt: { type: string, format: date-time }
    FamilyOfficeSourceMarker:
      type: object
      required: [key, label]
      properties:
        key: { type: string, enum: [accounting, treasury, operations] }
        label: { type: string }
    FamilyOfficeRun:
      type: object
      required: [id, analyst, scope, status, providerStatus, errorCode, outputSummary, sourceMarkers, createdAt, completedAt, advisoryOnly]
      properties:
        id: { type: string }
        analyst: { type: string }
        scope: { type: string }
        status: { type: string }
        providerStatus: { type: string }
        errorCode: { type: ["string", "null"] }
        outputSummary: { type: ["string", "null"] }
        sourceMarkers: { type: array, maxItems: 3, items: { $ref: "#/components/schemas/FamilyOfficeSourceMarker" } }
        createdAt: { type: string, format: date-time }
        completedAt: { type: ["string", "null"], format: date-time }
        advisoryOnly: { type: boolean }
    FamilyOfficeRefreshInput:
      type: object
      required: [trigger, contextFreshness]
      properties:
        trigger: { type: string, enum: [on_demand, hourly, daily] }
        contextFreshness: { type: string, enum: [fresh, stale, unknown] }
    FamilyOfficeRefresh:
      type: object
      required: [id, trigger, status, requestedAt, completedAt, providerStatus, providerModel, contextAsOf, failureClassification, evidenceFreshness, resultFingerprint, skipReason, runId]
      properties:
        id: { type: string }
        trigger: { type: string }
        status: { type: string }
        requestedAt: { type: string, format: date-time }
        completedAt: { type: ["string", "null"], format: date-time }
        providerStatus: { type: string }
        providerModel: { type: ["string", "null"] }
        contextAsOf: { type: ["string", "null"], format: date-time }
        failureClassification: { type: ["string", "null"] }
        evidenceFreshness: { type: string }
        resultFingerprint: { type: ["string", "null"] }
        skipReason: { type: ["string", "null"] }
        runId: { type: ["string", "null"] }
    FamilyOfficeProposal:
      type: object
      required: [id, title, thesis, label, analyticalDirection, confidence, facts, assumptions, risks, evidenceIds, status, createdAt, reviewedAt, reviewReason, advisoryOnly, executionAuthorization]
      properties:
        id: { type: string }
        title: { type: string }
        thesis: { type: string }
        label: { type: string }
        analyticalDirection: { type: string }
        confidence: { type: number }
        facts: { type: array, items: { type: string } }
        assumptions: { type: array, items: { type: string } }
        risks: { type: array, items: { type: string } }
        evidenceIds: { type: array, items: { type: string } }
        status: { type: string }
        createdAt: { type: string, format: date-time }
        reviewedAt: { type: ["string", "null"], format: date-time }
        reviewReason: { type: ["string", "null"] }
        advisoryOnly: { type: boolean }
        executionAuthorization: { type: boolean }
    ShadowPortfolioInput:
      type: object
      required: [name]
      properties:
        name: { type: string, minLength: 1, maxLength: 120 }
        benchmark: { type: string, maxLength: 120 }
        strategy: { type: string, maxLength: 500 }
    ShadowPortfolio:
      type: object
      required: [id, name, benchmark, strategy, capitalModel, riskPolicy, enabled, createdAt, authoritativeHouseholdAsset, liveExecutionEnabled]
      properties:
        id: { type: string }
        name: { type: string }
        benchmark: { type: string }
        strategy: { type: string }
        capitalModel: { type: string }
        riskPolicy: { type: string }
        enabled: { type: boolean }
        createdAt: { type: string, format: date-time }
        authoritativeHouseholdAsset: { type: boolean }
        liveExecutionEnabled: { type: boolean }
    ShadowIntentInput:
      type: object
      required: [proposalId, shadowPortfolioId, symbol, direction, hypotheticalQuantity, hypotheticalNotional, referencePrice, timeHorizon]
      properties:
        proposalId: { type: string }
        shadowPortfolioId: { type: string }
        symbol: { type: string, minLength: 1, maxLength: 32 }
        direction: { type: string, enum: [long, short, neutral] }
        hypotheticalQuantity: { type: number, exclusiveMinimum: 0 }
        hypotheticalNotional: { type: string, pattern: "^[0-9]+(\\.[0-9]{1,2})?$" }
        referencePrice: { type: number, exclusiveMinimum: 0 }
        timeHorizon: { type: string, minLength: 1, maxLength: 120 }
    ShadowIntent:
      type: object
      required: [id, proposalId, shadowPortfolioId, symbol, direction, hypotheticalQuantity, hypotheticalNotional, referencePrice, referenceTimestamp, timeHorizon, status, advisoryOnly, transmitted]
      properties:
        id: { type: string }
        proposalId: { type: string }
        shadowPortfolioId: { type: string }
        symbol: { type: string }
        direction: { type: string }
        hypotheticalQuantity: { type: number }
        hypotheticalNotional: { type: string }
        referencePrice: { type: number }
        referenceTimestamp: { type: string, format: date-time }
        timeHorizon: { type: string }
        status: { type: string }
        advisoryOnly: { type: boolean }
        transmitted: { type: boolean }
    ShadowPortfolioOutcome:
      type: object
      required: [id, shadowPortfolioId, shadowIntentId, periodStart, periodEnd, status, shadowReturnBps, benchmarkReturnBps, attributionBps, maxDrawdownBps, confidence, evidenceIds, asOf, advisoryOnly, householdCapitalIncluded, executionAuthorization]
      properties:
        id: { type: string }
        shadowPortfolioId: { type: string }
        shadowIntentId: { type: ["string", "null"] }
        periodStart: { type: string, format: date-time }
        periodEnd: { type: ["string", "null"], format: date-time }
        status: { type: string, enum: [unknown, pending, observed] }
        shadowReturnBps: { type: ["number", "null"] }
        benchmarkReturnBps: { type: ["number", "null"] }
        attributionBps: { type: ["number", "null"] }
        maxDrawdownBps: { type: ["number", "null"] }
        confidence: { type: number, minimum: 0, maximum: 100 }
        evidenceIds: { type: array, items: { type: string } }
        asOf: { type: string, format: date-time }
        advisoryOnly: { type: boolean }
        householdCapitalIncluded: { type: boolean, enum: [false] }
        executionAuthorization: { type: boolean, enum: [false] }
    FamilyOfficeAnalystScorecard:
      type: object
      required: [id, analyst, specialty, status, assignmentCount, completedCount, retryCount, failureCount, qualityScore, calibrationScore, budgetCents, spentCents, valueCents, authority, budgetRemainingCents, updatedAt, advisoryOnly, executionAuthorization]
      properties:
        id: { type: string }
        analyst: { type: string }
        specialty: { type: string }
        status: { type: string }
        assignmentCount: { type: number, minimum: 0 }
        completedCount: { type: number, minimum: 0 }
        retryCount: { type: number, minimum: 0 }
        failureCount: { type: number, minimum: 0 }
        qualityScore: { type: number, minimum: 0, maximum: 100 }
        calibrationScore: { type: number, minimum: 0, maximum: 100 }
        budgetCents: { type: number, minimum: 0 }
        spentCents: { type: number, minimum: 0 }
        valueCents: { type: number, minimum: 0 }
        authority: { type: string }
        budgetRemainingCents: { type: number, minimum: 0 }
        updatedAt: { type: string, format: date-time }
        advisoryOnly: { type: boolean }
        executionAuthorization: { type: boolean, enum: [false] }
    FamilyOfficeWorkforce:
      type: object
      required: [analysts, authority, budgetGovernor]
      properties:
        analysts: { type: array, items: { $ref: "#/components/schemas/FamilyOfficeAnalystScorecard" } }
        authority: { type: string }
        budgetGovernor: { type: string }
    FamilyOfficeReport:
      type: object
      required: [id, reportType, title, status, freshness, summary, citations, scheduledFor, generatedAt, executionDisabled, updatedAt, advisoryOnly]
      properties:
        id: { type: string }
        reportType: { type: string, enum: [morning, weekly, monthly] }
        title: { type: string }
        status: { type: string, enum: [unknown, scheduled, generated, stale, failed] }
        freshness: { type: string, enum: [fresh, stale, unknown] }
        summary: { type: ["string", "null"] }
        citations:
          type: array
          items:
            type: object
            required: [title, sourceUrl, freshness]
            properties:
              title: { type: string }
              sourceUrl: { type: ["string", "null"] }
              freshness: { type: string, enum: [fresh, stale, unknown] }
        scheduledFor: { type: string, format: date-time }
        generatedAt: { type: ["string", "null"], format: date-time }
        executionDisabled: { type: boolean, enum: [true] }
        updatedAt: { type: string, format: date-time }
        advisoryOnly: { type: boolean }
    FamilyOfficeProposalDecisionInput:
      type: object
      required: [decision, reason]
      properties:
        decision: { type: string, enum: [watch, reject, request_more_research, approve_shadow] }
        reason: { type: string, minLength: 1, maxLength: 500 }
    FamilyOfficeSourceRecord:
      type: object
      required: [title, sourceKind]
      properties:
        title: { type: string }
        sourceKind: { type: string }
        sourceUrl: { type: ["string", "null"] }
        verifiedAt: { type: ["string", "null"], format: date }
    FamilyOfficePropertyCandidate:
      type: object
      required: [id, propertyGoalId, addressLabel, propertyType, units, bedrooms, sourceKind, sourcePriority, dataFreshness, liveAvailability, parcelReconciliation, buyBox, intelligence, status, advisoryOnly, purchaseAuthority]
      properties:
        id: { type: string }
        propertyGoalId: { type: string }
        addressLabel: { type: string }
        city: { type: ["string", "null"] }
        county: { type: ["string", "null"] }
        state: { type: ["string", "null"] }
        zip: { type: ["string", "null"] }
        askingPrice: { type: ["string", "null"] }
        estimatedRent: { type: ["string", "null"] }
        annualPropertyTaxes: { type: ["string", "null"] }
        insurance: { type: ["string", "null"] }
        propertyType: { type: string }
        units: { type: string }
        bedrooms: { type: string }
        bathrooms: { type: string }
        zoning: { type: ["string", "null"] }
        floodZone: { type: ["string", "null"] }
        condition: { type: ["string", "null"] }
        sourceKind: { type: string }
        sourcePriority: { type: [number, "null"] }
        sourceRecords: { type: array, items: { $ref: "#/components/schemas/FamilyOfficeSourceRecord" } }
        dataFreshness: { type: string }
        liveAvailability: { type: string }
        parcelReconciliation: { type: string }
        buyBox:
          type: object
          required: [score, failures]
          properties:
            score: { type: number }
            failures: { type: array, items: { type: string } }
        intelligence:
          type: object
          required: [sourceKind, sourcePriority, dataFreshness, liveAvailability, parcelReconciliation, hardStops, deterministicBuyBoxAuthoritative, advisoryOnly, purchaseAuthority]
          properties:
            sourceKind: { type: string }
            sourcePriority: { type: number }
            dataFreshness: { type: string }
            liveAvailability: { type: string }
            parcelReconciliation: { type: string }
            hardStops: { type: array, items: { type: string } }
            deterministicBuyBoxAuthoritative: { type: boolean }
            advisoryOnly: { type: boolean }
            purchaseAuthority: { type: boolean }
        status: { type: string }
        advisoryOnly: { type: boolean }
        purchaseAuthority: { type: boolean }
    FamilyOfficeTaxLienCandidate:
      type: object
      required: [id, jurisdictionPolicy, county, state, certificateNumber, parcelNumber, taxYear, faceAmount, currentPurchaseAmount, status, homesteadStatus, priorCertificates, openCertificates, redeemedCertificates, taxDeedHistory, access, buildability, flood, wetland, codeStatus, titleRisk, redemptionAssessment, redemptionUncertainty, riskFlags, liveAvailability, parcelReconciliation, certificateReconciliation, sourcePriority, sourceRecords, dataFreshness, score, decision, hardStops, capitalGovernor, advisoryOnly, purchaseAuthority]
      properties:
        id: { type: string }
        jurisdictionPolicy: { type: string }
        county: { type: string }
        state: { type: string }
        certificateNumber: { type: string }
        parcelNumber: { type: string }
        taxYear: { type: string }
        faceAmount: { type: string }
        currentPurchaseAmount: { type: string }
        statedRate: { type: ["string", "null"] }
        status: { type: string }
        owner: { type: ["string", "null"] }
        propertyAddress: { type: ["string", "null"] }
        legalDescription: { type: ["string", "null"] }
        propertyUse: { type: ["string", "null"] }
        acreage: { type: ["string", "null"] }
        assessedValue: { type: ["string", "null"] }
        justValue: { type: ["string", "null"] }
        conservativeValue: { type: ["string", "null"] }
        certToValue: { type: number }
        totalLienExposure: { type: ["string", "null"] }
        totalExposureToValue: { type: number }
        homesteadStatus: { type: string }
        priorCertificates: { type: array, items: { type: string } }
        openCertificates: { type: array, items: { type: string } }
        redeemedCertificates: { type: array, items: { type: string } }
        taxDeedHistory: { type: array, items: { type: string } }
        access: { type: string }
        buildability: { type: string }
        flood: { type: string }
        wetland: { type: string }
        codeStatus: { type: string }
        titleRisk: { type: string }
        redemptionAssessment: { type: string }
        redemptionUncertainty: { type: boolean }
        riskFlags: { type: array, items: { type: string } }
        liveAvailability: { type: string }
        parcelReconciliation: { type: string }
        certificateReconciliation: { type: string }
        sourcePriority: { type: number }
        sourceRecords: { type: array, items: { $ref: "#/components/schemas/FamilyOfficeSourceRecord" } }
        dataFreshness: { type: string }
        score: { type: number }
        decision: { type: string, enum: [WATCH, REVIEW_REQUIRED, REJECT] }
        hardStops: { type: array, items: { type: string } }
        capitalGovernor:
          type: object
          required: [positionLimitCents, opportunityReserveMinimumCents, strategicReserveMinimumCents, opportunityReserveAfterCents, strategicReserveAfterCents, passes]
          properties:
            positionLimitCents: { type: number }
            opportunityReserveMinimumCents: { type: number }
            strategicReserveMinimumCents: { type: number }
            opportunityReserveAfterCents: { type: number }
            strategicReserveAfterCents: { type: number }
            passes: { type: boolean }
        lastVerifiedAt: { type: ["string", "null"], format: date-time }
        advisoryOnly: { type: boolean }
        purchaseAuthority: { type: boolean }
    FamilyOfficeRealEstateSnapshot:
      type: object
      required: [propertyCandidates, taxLienCandidates, sourceHierarchy, policy, advisoryOnly, purchaseAuthority]
      properties:
        propertyCandidates: { type: array, items: { $ref: "#/components/schemas/FamilyOfficePropertyCandidate" } }
        taxLienCandidates: { type: array, items: { $ref: "#/components/schemas/FamilyOfficeTaxLienCandidate" } }
        sourceHierarchy: { type: array, items: { type: string } }
        policy:
          type: object
          required: [jurisdiction, startingBankrollCents, initialDeploymentCapCents, preferredPositionRangeCents, singlePositionHardCapCents, opportunityReserveMinimumCents, strategicReserveMinimumCents, primaryOutcome, autonomousPurchase]
          properties:
            jurisdiction: { type: string }
            startingBankrollCents: { type: number }
            initialDeploymentCapCents: { type: number }
            preferredPositionRangeCents: { type: array, items: { type: number } }
            singlePositionHardCapCents: { type: number }
            opportunityReserveMinimumCents: { type: number }
            strategicReserveMinimumCents: { type: number }
            primaryOutcome: { type: string }
            autonomousPurchase: { type: boolean, enum: [false] }
        advisoryOnly: { type: boolean }
        purchaseAuthority: { type: boolean }
    FamilyOfficeSnapshot:
      type: object
      required: [provider, guardrails, runs, refreshes, refreshCadence, proposals, shadowPortfolios, shadowIntents, shadowOutcomes, workforce, reports, realEstate, summary]
      properties:
        provider:
          type: object
          required: [state, enabled, model, lastCheckedAt, lastResult, lastErrorCode]
          properties:
            state: { type: string, enum: [disabled, configured, verified, unavailable] }
            enabled: { type: boolean }
            model: { type: string }
            lastCheckedAt: { type: ["string", "null"], format: date-time }
            lastResult: { type: string, enum: [never, checking, verified, failed] }
            lastErrorCode: { type: ["string", "null"] }
        guardrails: { type: array, items: { type: string } }
        runs: { type: array, items: { $ref: "#/components/schemas/FamilyOfficeRun" } }
        refreshes: { type: array, items: { $ref: "#/components/schemas/FamilyOfficeRefresh" } }
        refreshCadence:
          type: object
          required: [lastSuccessfulBrief, nextEligibleAt, lastAttempt, blockedReason]
          properties:
            lastSuccessfulBrief:
              type: [object, "null"]
              required: [runId, completedAt, resultFingerprint, providerModel, contextAsOf, outputSummary, sourceMarkers]
              properties:
                runId: { type: string }
                completedAt: { type: ["string", "null"], format: date-time }
                resultFingerprint: { type: ["string", "null"] }
                providerModel: { type: ["string", "null"] }
                contextAsOf: { type: ["string", "null"], format: date-time }
                outputSummary: { type: ["string", "null"] }
                sourceMarkers: { type: array, maxItems: 3, items: { $ref: "#/components/schemas/FamilyOfficeSourceMarker" } }
            nextEligibleAt: { type: ["string", "null"], format: date-time }
            lastAttempt:
              type: [object, "null"]
              allOf:
                - $ref: "#/components/schemas/FamilyOfficeRefresh"
            blockedReason: { type: ["string", "null"] }
        proposals: { type: array, items: { $ref: "#/components/schemas/FamilyOfficeProposal" } }
        shadowPortfolios: { type: array, items: { $ref: "#/components/schemas/ShadowPortfolio" } }
        shadowIntents: { type: array, items: { $ref: "#/components/schemas/ShadowIntent" } }
        shadowOutcomes: { type: array, items: { $ref: "#/components/schemas/ShadowPortfolioOutcome" } }
        workforce: { $ref: "#/components/schemas/FamilyOfficeWorkforce" }
        reports: { type: array, items: { $ref: "#/components/schemas/FamilyOfficeReport" } }
        realEstate: { $ref: "#/components/schemas/FamilyOfficeRealEstateSnapshot" }
        summary:
          type: object
          required: [liveExecutionEnabled, realOrdersSent, moneyMovedCents, shadowOnly, outcomesWithAttribution, confidenceCalibration]
          properties:
            liveExecutionEnabled: { type: boolean }
            realOrdersSent: { type: number }
            moneyMovedCents: { type: number }
            shadowOnly: { type: boolean }
            outcomesWithAttribution: { type: number, minimum: 0 }
            confidenceCalibration: { type: [number, "null"], minimum: 0, maximum: 100 }
    FamilyOfficeResearchResult:
      type: object
      required: [run, proposal, advisoryOnly]
      properties:
        run: { $ref: "#/components/schemas/FamilyOfficeRun" }
        proposal: { $ref: "#/components/schemas/FamilyOfficeProposal" }
        advisoryOnly: { type: boolean }
    FamilyOfficeRefreshResult:
      type: object
      required: [refresh, accepted]
      properties:
        refresh: { $ref: "#/components/schemas/FamilyOfficeRefresh" }
        accepted: { type: boolean }
    FamilyOfficeResearchFailure:
      type: object
      required: [code, message, run, proposal, advisoryOnly]
      properties:
        code:
          type: string
          enum:
            - AI_PROVIDER_DISABLED
            - AI_PROVIDER_TIMEOUT
            - AI_PROVIDER_AUTHENTICATION_FAILED
            - AI_PROVIDER_MODEL_UNAVAILABLE
            - AI_PROVIDER_RATE_LIMITED
            - AI_PROVIDER_UPSTREAM_ERROR
            - AI_PROVIDER_INVALID_RESPONSE
            - AI_RESEARCH_PERSISTENCE_ERROR
        message: { type: string }
        correlationId: { type: ["string", "null"] }
        run: { $ref: "#/components/schemas/FamilyOfficeRun" }
        proposal: { type: "null" }
        advisoryOnly: { type: boolean }
    TaxLienCandidateInput:
      type: object
      required: [jurisdiction, county, parcelId, certificateNumber, propertyAddress, sourceKind, sourceFreshness, redemptionStatus, liveAvailability, faceAmount, estimatedTotalExposure, estimatedPropertyValue, householdSafeToDeploy, requiredReserveFloor]
      properties:
        jurisdiction: { type: string, enum: [Florida] }
        county: { type: string, minLength: 1, maxLength: 120 }
        parcelId: { type: string, minLength: 1, maxLength: 160 }
        certificateNumber: { type: string, minLength: 1, maxLength: 120 }
        propertyAddress: { type: string, minLength: 1, maxLength: 240 }
        sourceKind: { type: string, enum: [official_county, official_state, licensed_provider, user_supplied, model_inference] }
        sourceUrl: { type: string, maxLength: 2000 }
        sourceRetrievedAt: { type: string, format: date-time }
        sourceFreshness: { type: string, enum: [current, stale, unknown] }
        officialParcelId: { type: string, maxLength: 160 }
        officialCertificateNumber: { type: string, maxLength: 120 }
        redemptionStatus: { type: string, enum: [unknown, open, closed, disputed] }
        redemptionDeadline: { type: string, maxLength: 80 }
        liveAvailability: { type: string, enum: [unknown, stale, verified, unavailable] }
        availabilityCheckedAt: { type: string, format: date-time }
        faceAmount: { type: string, pattern: "^[0-9]+(\\.[0-9]{1,2})?$" }
        estimatedTotalExposure: { type: string, pattern: "^[0-9]+(\\.[0-9]{1,2})?$" }
        estimatedPropertyValue: { type: string, pattern: "^[0-9]+(\\.[0-9]{1,2})?$" }
        householdSafeToDeploy: { type: string, pattern: "^[0-9]+(\\.[0-9]{1,2})?$" }
        requiredReserveFloor: { type: string, pattern: "^[0-9]+(\\.[0-9]{1,2})?$" }
        notes: { type: string, maxLength: 2000 }
    TaxLienCandidate:
      type: object
      required: [id, jurisdiction, county, parcelId, certificateNumber, propertyAddress, sourceKind, sourceUrl, sourceRetrievedAt, sourceFreshness, officialParcelId, officialCertificateNumber, redemptionStatus, redemptionDeadline, liveAvailability, availabilityCheckedAt, faceAmount, estimatedTotalExposure, estimatedPropertyValue, householdSafeToDeploy, requiredReserveFloor, reconciliationStatus, reserveStatus, reviewStatus, hardStops, notes, createdAt, updatedAt, advisoryOnly, purchaseAuthorized, bidAuthorized]
      properties:
        id: { type: string }
        jurisdiction: { type: string }
        county: { type: string }
        parcelId: { type: string }
        certificateNumber: { type: string }
        propertyAddress: { type: string }
        sourceKind: { type: string }
        sourceUrl: { type: ["string", "null"] }
        sourceRetrievedAt: { type: ["string", "null"], format: date-time }
        sourceFreshness: { type: string }
        officialParcelId: { type: ["string", "null"] }
        officialCertificateNumber: { type: ["string", "null"] }
        redemptionStatus: { type: string }
        redemptionDeadline: { type: ["string", "null"] }
        liveAvailability: { type: string }
        availabilityCheckedAt: { type: ["string", "null"], format: date-time }
        faceAmount: { type: string }
        estimatedTotalExposure: { type: string }
        estimatedPropertyValue: { type: string }
        householdSafeToDeploy: { type: string }
        requiredReserveFloor: { type: string }
        reconciliationStatus: { type: string }
        reserveStatus: { type: string }
        reviewStatus: { type: string }
        hardStops: { type: array, items: { type: string } }
        notes: { type: ["string", "null"] }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        advisoryOnly: { type: boolean }
        purchaseAuthorized: { type: boolean }
        bidAuthorized: { type: boolean }
    RealEstateIntelligence:
      type: object
      required: [property, taxLiens, guardrails, summary]
      properties:
        property:
          allOf:
            - $ref: "#/components/schemas/PropertyUnderwriting"
          type: object
          properties:
            authority: { type: string }
            advisoryOnly: { type: boolean }
            purchaseAuthorized: { type: boolean }
            capitalCommitmentAuthorized: { type: boolean }
        taxLiens: { type: array, items: { $ref: "#/components/schemas/TaxLienCandidate" } }
        guardrails: { type: array, items: { type: string } }
        summary:
          type: object
          required: [taxLienCount, blockedTaxLienCount, purchaseAuthorized, biddingAuthorized, householdCapitalAccessible]
          properties:
            taxLienCount: { type: number }
            blockedTaxLienCount: { type: number }
            purchaseAuthorized: { type: boolean }
            biddingAuthorized: { type: boolean }
            householdCapitalAccessible: { type: boolean }
    IntelligenceScenarioInput:
      type: object
      required: [proposedWeekly]
      properties:
        proposedWeekly:
          type: string
          pattern: "^[0-9]+(\\.[0-9]{1,2})?$"
    ContributionScenario:
      type: object
      required: [name, proposedWeekly, currentWeeks, proposedWeeks, weeksEarlier, weeklyChangeCents, currentSafeToDeploy, productionDataChanged, householdId]
      properties:
        name: { type: string }
        proposedWeekly: { type: string }
        currentWeeks: { type: ["number", "null"] }
        proposedWeeks: { type: ["number", "null"] }
        weeksEarlier: { type: ["number", "null"] }
        weeklyChangeCents: { type: number }
        currentSafeToDeploy: { type: string }
        productionDataChanged: { type: boolean }
        householdId: { type: string }
    AuditEventSummary:
      type: object
      required: [id, eventType, actor, entity, entityId, timestamp]
      properties:
        id: { type: string }
        eventType: { type: string }
        actor: { type: string }
        entity: { type: string }
        entityId: { type: string }
        timestamp: { type: string, format: date-time }
        reason: { type: ["string", "null"] }
    BlockchainStatus:
      type: object
      required: [enabled, preferredChain, chains, privateDataOffChain, liveTransactionsEnabled]
      properties:
        enabled: { type: boolean }
        preferredChain: { type: string }
        chains:
          type: array
          items:
            type: object
            required: [name, chainId, enabled]
            properties:
              name: { type: string }
              chainId: { type: number }
              enabled: { type: boolean }
        privateDataOffChain: { type: boolean }
        liveTransactionsEnabled: { type: boolean }
    DashboardSnapshot:
      type: object
      required: [household, goal, accounts, allocation, portfolio, property, strategies, risk, recommendation, recentActivity]
      properties:
        household: { $ref: "#/components/schemas/HouseholdSummary" }
        goal: { $ref: "#/components/schemas/GoalSummary" }
        accounts:
          type: array
          items: { $ref: "#/components/schemas/AccountSummary" }
        allocation: { $ref: "#/components/schemas/AllocationSummary" }
        portfolio: { $ref: "#/components/schemas/PortfolioSummary" }
        property: { $ref: "#/components/schemas/PropertySummary" }
        strategies:
          type: array
          items: { $ref: "#/components/schemas/StrategySummary" }
        risk: { $ref: "#/components/schemas/RiskSummary" }
        recommendation: { $ref: "#/components/schemas/RecommendationSummary" }
        recentActivity:
          type: array
          items: { $ref: "#/components/schemas/AuditEventSummary" }
    BudgetSummary:
      type: object
      required: [month, categories, totals, notes]
      properties:
        month: { type: string }
        categories:
          type: array
          items:
            type: object
            required: [id, name, categoryType, essentialStatus, budgeted, actual, pendingEvidence, sourceCoverage, noTarget, variance, percentageUsed, projectedMonthEnd, status]
            properties:
              id: { type: string }
              name: { type: string }
              categoryType: { type: string }
              essentialStatus: { type: string }
              budgeted: { type: string }
              actual: { type: string }
              pendingEvidence: { type: string }
              sourceCoverage: { type: string }
              noTarget: { type: boolean }
              variance: { type: string }
              percentageUsed: { type: number }
              projectedMonthEnd: { type: string }
              status: { type: string }
        totals:
          type: object
          required: [budgeted, actual, pendingEvidence, remaining, percentageUsed]
          properties:
            budgeted: { type: string }
            actual: { type: string }
            pendingEvidence: { type: string }
            remaining: { type: string }
            percentageUsed: { type: number }
        notes:
          type: array
          items: { type: string }
    CashFlowSummary:
      type: object
      required: [month, metrics, reserve, financialHealth, forecast]
      properties:
        month: { type: string }
        metrics:
          type: object
          required: [grossInflow, essentialOutflow, discretionaryOutflow, debtService, savingsContributions, investmentContributions, propertyContributions, netCashFlow, freeCashFlow, savingsRate, investmentRate]
          properties:
            grossInflow: { type: string }
            essentialOutflow: { type: string }
            discretionaryOutflow: { type: string }
            debtService: { type: string }
            savingsContributions: { type: string }
            investmentContributions: { type: string }
            propertyContributions: { type: string }
            netCashFlow: { type: string }
            freeCashFlow: { type: string }
            savingsRate: { type: number }
            investmentRate: { type: number }
        reserve:
          type: object
          required: [target, current, gap, monthsCovered, targetMonths]
          properties:
            target: { type: string }
            current: { type: string }
            gap: { type: string }
            monthsCovered: { type: number }
            targetMonths: { type: number }
        financialHealth:
          type: object
          required: [score, label, disclaimer]
          properties:
            score: { type: number }
            label: { type: string }
            disclaimer: { type: string }
        forecast:
          type: object
          required: [nextMonthInflow, nextMonthEssentialOutflow, nextMonthNet, confidence, nextIncomeDate]
          properties:
            nextMonthInflow: { type: string }
            nextMonthEssentialOutflow: { type: string }
            nextMonthNet: { type: string }
            confidence: { type: number }
            nextIncomeDate: { type: ["string", "null"], format: date }
    FinancialAccount:
      type: object
      required: [id, institution, nickname, accountType, connectionStatus, dataSource, includedInNetWorth, includedInBudget, protected, restricted]
      properties:
        id: { type: string }
        institution: { type: string }
        nickname: { type: string }
        accountType: { type: string }
        currentBalance: { type: ["string", "null"] }
        availableBalance: { type: ["string", "null"] }
        connectionStatus: { type: string }
        dataSource: { type: string }
        dataMode: { type: string, enum: [MANUAL, STATEMENT_SUPPORTED, LIVE_CONNECTED, SIMULATED_TEST_ONLY] }
        lastSync: { type: ["string", "null"], format: date-time }
        includedInNetWorth: { type: boolean }
        includedInBudget: { type: boolean }
        protected: { type: boolean }
        restricted: { type: boolean }
    FinancialAccountsSummary:
      type: object
      required: [readOnly, accounts, connections, totals]
      properties:
        readOnly: { type: boolean }
        accounts:
          type: array
          items: { $ref: "#/components/schemas/FinancialAccount" }
        connections:
          type: array
          items: { $ref: "#/components/schemas/BankConnection" }
        totals:
          type: object
          required: [visibleBalance, accountCount]
          properties:
            visibleBalance: { type: string }
            accountCount: { type: number }
    ManualFinancialAccountInput:
      type: object
      required: [institution, nickname, accountType]
      properties:
        institution: { type: string, minLength: 1, maxLength: 120 }
        nickname: { type: string, minLength: 1, maxLength: 120 }
        accountType: { type: string }
        currentBalance: { type: string, pattern: "^-?[0-9]+(\\.[0-9]{1,2})?$" }
    CsvImportInput:
      type: object
      required: [csv]
      properties:
        csv: { type: string, minLength: 1, maxLength: 100000 }
    ManualFinanceTransactionInput:
      type: object
      required: [transactionDate, description, amount, direction]
      properties:
        transactionDate: { type: string, format: date }
        description: { type: string, minLength: 1, maxLength: 240 }
        merchant: { type: ["string", "null"], maxLength: 160 }
        amount: { type: string, pattern: "^(?:0|[1-9][0-9]{0,15})(\\.[0-9]{1,2})?$" }
        direction: { type: string, enum: [inflow, outflow] }
    FinanceTransaction:
      type: object
      required: [id, accountId, transactionDate, description, merchant, originalAmount, amount, dataSource, reviewStatus]
      properties:
        id: { type: string }
        accountId: { type: string }
        transactionDate: { type: string, format: date }
        description: { type: string }
        merchant: { type: ["string", "null"] }
        originalAmount: { type: ["string", "null"] }
        amount: { type: string }
        dataSource: { type: string }
        reviewStatus: { type: string }
    TransactionReviewInput:
      type: object
      required: [status]
      properties:
        status:
          type: string
          enum: [approved, needs_review, excluded, possible_transfer, possible_business]
          description: Use needs_review to save a category without including the row in planning.
        categoryId: { type: ["string", "null"], pattern: "^[0-9a-fA-F-]{36}$" }
        note: { type: ["string", "null"], maxLength: 500 }
    ReviewedFinancialTransaction:
      type: object
      required: [id, accountId, accountName, transactionDate, description, amount, dataSource, reviewStatus, businessTag, excludedFromBudget, pending, categoryId, categoryName, weeklyGuidanceExclusionReason, weeklyGuidanceActionable, reviewReason, reviewNote, reviewedBy, reviewedAt]
      properties:
        id: { type: string, pattern: "^[0-9a-fA-F-]{36}$" }
        accountId: { type: string, pattern: "^[0-9a-fA-F-]{36}$" }
        accountName: { type: string }
        transactionDate: { type: string, format: date }
        description: { type: string }
        merchant: { type: ["string", "null"] }
        amount: { type: string }
        originalAmount: { type: ["string", "null"] }
        categoryId: { type: ["string", "null"], pattern: "^[0-9a-fA-F-]{36}$" }
        categoryName: { type: ["string", "null"] }
        dataSource: { type: string }
        reviewStatus: { type: string }
        businessTag: { type: string }
        excludedFromBudget: { type: boolean }
        pending: { type: boolean }
        weeklyGuidanceExclusionReason:
          type: ["string", "null"]
          enum: [uncategorized, pending, excluded, unreviewed, nonHousehold, transfer, nonIncome, null]
        weeklyGuidanceActionable:
          type: boolean
          description: True only when categorizing or approving the row can make it eligible for weekly guidance without removing a fail-closed classification.
        reviewReason: { type: string }
        reviewNote: { type: ["string", "null"] }
        reviewedBy: { type: ["string", "null"] }
        reviewedAt: { type: ["string", "null"], format: date-time }
    TransactionReviewQueue:
      type: object
      required: [transactions, categories]
      properties:
        transactions:
          type: array
          items: { $ref: "#/components/schemas/ReviewedFinancialTransaction" }
        categories:
          type: array
          items:
            type: object
            required: [id, name]
            properties:
              id: { type: string, pattern: "^[0-9a-fA-F-]{36}$" }
              name: { type: string }
    Bill:
      type: object
      required: [id, householdId, billName, dueDate, expectedAmount, status, essential, autoPay, active]
      properties:
        id: { type: string }
        householdId: { type: string }
        billName: { type: string }
        dueDate: { type: string, format: date }
        expectedAmount: { type: string }
        status: { type: string, enum: [upcoming, due_soon, paid, overdue, estimated, skipped] }
        essential: { type: boolean }
        autoPay: { type: boolean }
        active: { type: boolean }
    BillInput:
      type: object
      required: [billName, dueDate, expectedAmount]
      properties:
        billName: { type: string, minLength: 1, maxLength: 160 }
        dueDate: { type: string, format: date }
        expectedAmount: { type: string, pattern: "^[0-9]+(\\.[0-9]{1,2})?$" }
        status: { type: string, enum: [upcoming, due_soon, paid, overdue, estimated, skipped] }
        essential: { type: boolean }
        autoPay: { type: boolean }
    BillUpdateInput:
      type: object
      properties:
        billName: { type: string, minLength: 1, maxLength: 160 }
        dueDate: { type: string, format: date }
        expectedAmount: { type: string, pattern: "^[0-9]+(\\.[0-9]{1,2})?$" }
        status: { type: string, enum: [upcoming, due_soon, paid, overdue, estimated, skipped] }
        essential: { type: boolean }
        autoPay: { type: boolean }
    UpcomingExpense:
      type: object
      required: [id, householdId, name, estimatedAmount, expectedDate, priority, required, fundedAmount, active]
      properties:
        id: { type: string }
        householdId: { type: string }
        name: { type: string }
        estimatedAmount: { type: string }
        expectedDate: { type: string, format: date }
        priority: { type: string, enum: [low, normal, high, critical] }
        required: { type: boolean }
        fundedAmount: { type: string }
        active: { type: boolean }
    UpcomingExpenseInput:
      type: object
      required: [name, estimatedAmount, expectedDate]
      properties:
        name: { type: string, minLength: 1, maxLength: 160 }
        estimatedAmount: { type: string, pattern: "^[0-9]+(\\.[0-9]{1,2})?$" }
        expectedDate: { type: string, format: date }
        priority: { type: string, enum: [low, normal, high, critical] }
        required: { type: boolean }
        fundedAmount: { type: string, pattern: "^[0-9]+(\\.[0-9]{1,2})?$" }
    UpcomingExpenseUpdateInput:
      type: object
      properties:
        name: { type: string, minLength: 1, maxLength: 160 }
        estimatedAmount: { type: string, pattern: "^[0-9]+(\\.[0-9]{1,2})?$" }
        expectedDate: { type: string, format: date }
        priority: { type: string, enum: [low, normal, high, critical] }
        required: { type: boolean }
        fundedAmount: { type: string, pattern: "^[0-9]+(\\.[0-9]{1,2})?$" }
    IncomeSource:
      type: object
      required: [id, householdId, name, sourceType, expectedMonthly, cadence, nextPayDate, active]
      properties:
        id: { type: string }
        householdId: { type: string }
        name: { type: string }
        sourceType: { type: string }
        expectedMonthly: { type: string }
        cadence: { type: string }
        nextPayDate: { type: string, format: date }
        active: { type: boolean }
    IncomeSourceInput:
      type: object
      required: [name, sourceType, expectedMonthly, cadence, nextPayDate]
      properties:
        name: { type: string, minLength: 1, maxLength: 160 }
        sourceType: { type: string, enum: [employment, contract, business, rental, investment, interest, other] }
        expectedMonthly: { type: string, pattern: "^[0-9]+(\\.[0-9]{1,2})?$" }
        cadence: { type: string, enum: [weekly, biweekly, monthly, quarterly, annual] }
        nextPayDate: { type: string, format: date }
    IncomeSourceUpdateInput:
      type: object
      properties:
        name: { type: string, minLength: 1, maxLength: 160 }
        sourceType: { type: string, enum: [employment, contract, business, rental, investment, interest, other] }
        expectedMonthly: { type: string, pattern: "^[0-9]+(\\.[0-9]{1,2})?$" }
        cadence: { type: string, enum: [weekly, biweekly, monthly, quarterly, annual] }
        nextPayDate: { type: string, format: date }
    SafeToDeploy:
      type: object
      required: [safeToDeploy, rawSafeToDeploy, confidence, confidenceScore, breakdown, reason]
      properties:
        safeToDeploy: { type: string }
        rawSafeToDeploy: { type: string }
        confidence: { type: string }
        confidenceScore: { type: number }
        reason: { type: string }
        breakdown:
          type: object
          required: [liquidAvailableCash, billsDueBeforeNextIncome, requiredMonthlyExpenses, emergencyReserveShortfall, protectedGoalCommitments, knownUpcomingExpenses, requiredSafetyBuffer, maximumDeployableAmount]
          properties:
            liquidAvailableCash: { type: string }
            billsDueBeforeNextIncome: { type: string }
            requiredMonthlyExpenses: { type: string }
            emergencyReserveShortfall: { type: string }
            protectedGoalCommitments: { type: string }
            knownUpcomingExpenses: { type: string }
            requiredSafetyBuffer: { type: string }
            maximumDeployableAmount: { type: string }
    CapitalWaterfallInput:
      type: object
      properties:
        asOf: { type: string, format: date }
        scenario: { type: string, enum: [floor, base, strong] }
    CapitalGovernorV2:
      type: object
      required: [version, asOf, status, safeToDeploy, rawSafeToDeploy, householdCapitalSurplus, dataReadiness, calculation, reasonCodes, reasons, components, bucketStatus, waterfall, controls]
      properties:
        version: { type: string }
        asOf: { type: string, format: date }
        status: { type: string }
        safeToDeploy: { type: string }
        rawSafeToDeploy: { type: string }
        inputSnapshotId: { type: ["string", "null"] }
        waterfallRunId: { type: ["string", "null"] }
        reasonCodes:
          type: array
          items: { type: string }
        reasons:
          type: array
          items: { type: string }
        householdCapitalSurplus:
          type: object
          required: [floor, base, strong, source]
          properties:
            floor: { type: string }
            base: { type: string }
            strong: { type: string }
            source: { type: string }
        dataReadiness:
          type: object
          required: [status, freshnessDays, failClosed]
          properties:
            status: { type: string }
            freshnessDays: { type: ["number", "null"] }
            failClosed: { type: boolean }
        calculation:
          type: object
          required: [amountCalculated, blockedStatus, requiredComponents, doubleSubtraction]
          properties:
            amountCalculated: { type: boolean }
            blockedStatus: { type: ["string", "null"] }
            requiredComponents: { type: array, items: { type: string } }
            doubleSubtraction:
              type: object
              required: [detected, obligationsAreDisjoint, method]
              properties:
                detected: { type: boolean }
                obligationsAreDisjoint: { type: boolean }
                method: { type: string }
        components:
          type: array
          items:
            type: object
            required: [key, label, amount, sign, provenance]
            properties:
              key: { type: string }
              label: { type: string }
              amount: { type: string, nullable: true }
              sign: { type: string }
              provenance:
                type: array
                items: { type: string }
              sourceReferences:
                type: array
                items:
                  type: object
                  required: [reference, sourceType]
                  properties:
                    reference: { type: string }
                    sourceType: { type: string }
              subtractionGroup: { type: string }
        bucketStatus:
          type: array
          items:
            type: object
            required: [key, label, current, target, gap, protected, liquid, physicalAccountIds, provenance]
            properties:
              key: { type: string }
              label: { type: string }
              current: { type: string }
              target: { type: string }
              gap: { type: string }
              protected: { type: boolean }
              liquid: { type: boolean }
              physicalAccountIds:
                type: array
                items: { type: string }
              provenance:
                type: array
                items: { type: string }
        waterfall:
          type: object
          required: [availableForWaterfall, unallocatedAfterRecommendations, allocations, scenarioBehavior]
          properties:
            availableForWaterfall: { type: string }
            unallocatedAfterRecommendations: { type: string }
            allocations:
              type: array
              items:
                type: object
                required: [bucket, amount, amountCents, recommendedOnly, physicalMovementAuthorized, provenance]
                properties:
                  bucket: { type: string }
                  amount: { type: string }
                  amountCents: { type: number }
                  recommendedOnly: { type: boolean }
                  physicalMovementAuthorized: { type: boolean }
                  provenance:
                    type: array
                    items: { type: string }
            scenarioBehavior:
              type: object
              required: [floor, base, strong]
              properties:
                floor: { type: string }
                base: { type: string }
                strong: { type: string }
        controls:
          type: object
          required: [protectedDuplexReserveLocked, businessCashExcluded, moneyMovementAuthorized, microLiveAuthorized, strategyLabAuthority, manualOverride]
          properties:
            protectedDuplexReserveLocked: { type: boolean }
            businessCashExcluded: { type: boolean }
            moneyMovementAuthorized: { type: boolean }
            microLiveAuthorized: { type: boolean }
            strategyLabAuthority: { type: boolean }
            manualOverride: { type: boolean }
    FinanceInsights:
      type: object
      required: [insights, subscriptions, anomalyCount]
      properties:
        insights:
          type: array
          items:
            type: object
            required: [type, title, description, severity]
            properties:
              type: { type: string }
              title: { type: string }
              description: { type: string }
              severity: { type: string }
        subscriptions:
          type: array
          items:
            type: object
            required: [merchant, monthlyAmount, annualCost, essentialStatus]
            properties:
              merchant: { type: string }
              monthlyAmount: { type: string }
              annualCost: { type: string }
              essentialStatus: { type: string }
        anomalyCount: { type: number }
    ProviderWebhookPayload:
      type: object
      additionalProperties: true
    BankingStatus:
      type: object
      required: [readOnly, credentialsStored, transfersEnabled, billPayEnabled, adapters, message]
      properties:
        readOnly: { type: boolean }
        credentialsStored: { type: boolean }
        transfersEnabled: { type: boolean }
        billPayEnabled: { type: boolean }
        message: { type: string }
        adapters:
          type: array
          items:
            type: object
            required: [provider, enabled, readOnly]
            properties:
              provider: { type: string }
              enabled: { type: boolean }
              readOnly: { type: boolean }
    BankConnection:
      type: object
      required: [id, provider, status, consentStatus, institutionName, credentialStored, reconciliationStatus, reconciliationDifference]
      properties:
        id: { type: string, pattern: "^[0-9a-fA-F-]{36}$" }
        provider: { type: string }
        status: { type: string }
        consentStatus: { type: string, enum: [pending, granted, revoked] }
        institutionName: { type: string }
        credentialStored: { type: boolean }
        lastSuccessfulSync: { type: ["string", "null"], format: date-time }
        lastSyncAttempt: { type: ["string", "null"], format: date-time }
        providerAsOf: { type: ["string", "null"], format: date-time }
        reconciliationStatus: { type: string, enum: [not_run, matched, review, stale, outage, rate_limited, revoked] }
        reconciliationDifference: { type: string }
        errorMessage: { type: ["string", "null"] }
    BankConnectionList:
      type: object
      required: [readOnly, connections]
      properties:
        readOnly: { type: boolean }
        connections:
          type: array
          items: { $ref: "#/components/schemas/BankConnection" }
    ReadOnlyBankConnectionInput:
      type: object
      required: [provider, institutionName, providerConnectionRef, consent]
      properties:
        provider: { type: string, minLength: 1, maxLength: 80 }
        institutionName: { type: string, minLength: 1, maxLength: 160 }
        providerConnectionRef: { type: string, minLength: 1, maxLength: 240 }
        consent: { type: boolean, enum: [true] }
    BankAccountLinkInput:
      type: object
      required: [accountId, providerAccountRef]
      properties:
        accountId: { type: string, pattern: "^[0-9a-fA-F-]{36}$" }
        providerAccountRef: { type: string, minLength: 1, maxLength: 240 }
    BankSyncResult:
      type: object
      required: [connectionId, readOnly, status, applied, inserted, updated, duplicates, reviewCount, removed, reconciliationDifference]
      properties:
        connectionId: { type: string, pattern: "^[0-9a-fA-F-]{36}$" }
        readOnly: { type: boolean }
        status: { type: string, enum: [matched, review, stale, outage, rate_limited, revoked] }
        applied: { type: boolean }
        inserted: { type: number }
        updated: { type: number }
        duplicates: { type: number }
        reviewCount: { type: number }
        removed: { type: number }
        providerAsOf: { type: ["string", "null"], format: date-time }
        reconciliationDifference: { type: string }
        errorMessage: { type: ["string", "null"] }
    BankConnectionExport:
      type: object
      required: [readOnly, connection, accounts, transactions]
      properties:
        readOnly: { type: boolean }
        connection: { $ref: "#/components/schemas/BankConnection" }
        accounts:
          type: array
          items:
            type: object
            required: [id, institution, nickname, accountType, providerAccountRef]
            properties:
              id: { type: string }
              institution: { type: string }
              nickname: { type: string }
              accountType: { type: string }
              providerAccountRef: { type: ["string", "null"] }
        transactions:
          type: array
          items:
            type: object
            required: [id, accountId, externalId, transactionDate, description, merchant, amount, pending, reviewStatus]
            properties:
              id: { type: string }
              accountId: { type: string }
              externalId: { type: ["string", "null"] }
              transactionDate: { type: string, format: date }
              description: { type: string }
              merchant: { type: ["string", "null"] }
              amount: { type: string }
              pending: { type: boolean }
              reviewStatus: { type: string }
    BankConnectionDeleteResult:
      type: object
      required: [deleted, readOnly, connectionId, unlinkedAccounts]
      properties:
        deleted: { type: boolean }
        readOnly: { type: boolean }
        connectionId: { type: string }
        unlinkedAccounts: { type: number }
    FinanceSnapshot:
      type: object
      required: [id, householdId, snapshotDate, grossInflow, essentialOutflow, discretionaryOutflow, debtService, savingsContributions, investmentContributions, netCashFlow, freeCashFlow, safeToDeploy, safeToDeployConfidence, financialHealthScore, budgetPerformance, createdAt]
      properties:
        id: { type: string }
        householdId: { type: string }
        snapshotDate: { type: string, format: date }
        grossInflow: { type: string }
        essentialOutflow: { type: string }
        discretionaryOutflow: { type: string }
        debtService: { type: string }
        savingsContributions: { type: string }
        investmentContributions: { type: string }
        netCashFlow: { type: string }
        freeCashFlow: { type: string }
        safeToDeploy: { type: string }
        safeToDeployConfidence: { type: number }
        financialHealthScore: { type: number }
        budgetPerformance:
          type: object
          required: [month, source]
          properties:
            month: { type: string }
            source: { type: string }
        createdAt: { type: string, format: date-time }
    AccountingAccount:
      type: object
      required: [id, name, institution, accountType, balance, includedInNetWorth, protected, dataSource, lastSync, restricted]
      properties:
        id: { type: string }
        name: { type: string }
        institution: { type: string }
        accountType: { type: string }
        balance: { type: ["string", "null"] }
        includedInNetWorth: { type: boolean }
        protected: { type: boolean }
        dataSource: { type: string }
        lastSync: { type: ["string", "null"], format: date }
        restricted: { type: boolean }
    AccountingAsset:
      type: object
      required: [name, category, institution, amount, valueStatus, valuationDate, protected]
      properties:
        name: { type: string }
        category: { type: string }
        institution: { type: string }
        amount: { type: string }
        valueStatus: { type: string }
        valuationDate: { type: ["string", "null"], format: date }
        protected: { type: boolean }
    AccountingLiability:
      type: object
      required: [name, category, institution, amount, interestRate, monthlyPayment, maturity]
      properties:
        name: { type: string }
        category: { type: string }
        institution: { type: string }
        amount: { type: string }
        interestRate: { type: ["string", "null"] }
        monthlyPayment: { type: string }
        maturity: { type: ["string", "null"], format: date }
    AccountingNetWorth:
      type: object
      required: [totalAssets, totalLiabilities, netWorth, liquidNetWorth, protectedCapital, investedCapital, realEstateEquity, businessEquity, cashTreasury]
      properties:
        totalAssets: { type: string }
        totalLiabilities: { type: string }
        netWorth: { type: string }
        liquidNetWorth: { type: string }
        protectedCapital: { type: string }
        investedCapital: { type: string }
        realEstateEquity: { type: string }
        businessEquity: { type: string }
        cashTreasury: { type: string }
    AccountingChange:
      type: object
      required: [month, yearToDate, capitalContributed, investmentGrowth, debtReduction, other, reconciles]
      properties:
        month: { type: string }
        yearToDate: { type: string }
        capitalContributed: { type: string }
        investmentGrowth: { type: string }
        debtReduction: { type: string }
        other: { type: string }
        reconciles: { type: boolean }
    AccountingBalanceSheet:
      type: object
      required: [assets, liabilities, totalAssets, totalLiabilities, netWorth, comparison]
      properties:
        assets: { type: array, items: { $ref: "#/components/schemas/AccountingAsset" } }
        liabilities: { type: array, items: { $ref: "#/components/schemas/AccountingLiability" } }
        totalAssets: { type: string }
        totalLiabilities: { type: string }
        netWorth: { type: string }
        comparison:
          type: object
          required: [previousPeriod, quarterEnd, yearEnd]
          properties:
            previousPeriod: { type: ["string", "null"] }
            quarterEnd: { type: ["string", "null"] }
            yearEnd: { type: ["string", "null"] }
    AccountingCapitalStatement:
      type: object
      required: [beginningCapital, householdContributions, withdrawals, realizedGainsLosses, unrealizedGainsLosses, income, fees, endingCapital]
      properties:
        beginningCapital: { type: string }
        householdContributions: { type: string }
        withdrawals: { type: string }
        realizedGainsLosses: { type: string }
        unrealizedGainsLosses: { type: string }
        income: { type: string }
        fees: { type: string }
        endingCapital: { type: string }
    AccountingLineItem:
      type: object
      required: [category, amount]
      properties:
        category: { type: string }
        amount: { type: string }
    AccountingIncomeStatement:
      type: object
      required: [income, expenses, totalIncome, totalExpenses, netCashIncome, taxableIncomeDisclaimer]
      properties:
        income: { type: array, items: { $ref: "#/components/schemas/AccountingLineItem" } }
        expenses: { type: array, items: { $ref: "#/components/schemas/AccountingLineItem" } }
        totalIncome: { type: string }
        totalExpenses: { type: string }
        netCashIncome: { type: string }
        taxableIncomeDisclaimer: { type: string }
    AccountingCashFlow:
      type: object
      required: [operating, investing, financing, transfers, netCashFlow]
      properties:
        operating: { type: string }
        investing: { type: string }
        financing: { type: string }
        transfers: { type: string }
        netCashFlow: { type: string }
    AccountingMetrics:
      type: object
      required: [savingsRate, debtToAssetRatio, debtToNetWorth, liquidAssets, essentialMonths, protectedLiquidity, unrestrictedLiquidity, returnOnCapital]
      properties:
        savingsRate: { type: number }
        debtToAssetRatio: { type: number }
        debtToNetWorth: { type: number }
        liquidAssets: { type: string }
        essentialMonths: { type: number }
        protectedLiquidity: { type: string }
        unrestrictedLiquidity: { type: string }
        returnOnCapital: { type: number, nullable: true }
    AccountingReconciliation:
      type: object
      required: [ledgerBalanced, accountsIncluded, uncategorizedTransactions, staleAccounts, status, crossView]
      properties:
        ledgerBalanced: { type: boolean }
        accountsIncluded: { type: number }
        uncategorizedTransactions: { type: number }
        staleAccounts: { type: number }
        status: { type: string }
        crossView:
          type: object
          required: [status, accountingNetWorth, separateScopes, note]
          properties:
            status: { type: string }
            accountingNetWorth: { type: string }
            separateScopes:
              type: array
              items:
                type: object
                required: [scope, amount, status]
                properties:
                  scope: { type: string }
                  amount: { type: string }
                  status: { type: string }
            note: { type: string }
    AccountingConfidence:
      type: object
      required: [score, bankSyncFreshness, ledgerReconciliation, categorization, valuationFreshness, label]
      properties:
        score: { type: number }
        bankSyncFreshness: { type: number }
        ledgerReconciliation: { type: number }
        categorization: { type: number }
        valuationFreshness: { type: number }
        label: { type: string }
    AccountingTaxYear:
      type: object
      required: [year, documentsCollected, missingDocuments, realizedGainsLosses, reviewStatus, disclaimer]
      properties:
        year: { type: number }
        documentsCollected: { type: number }
        missingDocuments: { type: array, items: { type: string } }
        realizedGainsLosses: { type: string }
        reviewStatus: { type: string }
        disclaimer: { type: string }
    AccountingOverview:
      type: object
      required: [asOf, period, disclaimer, netWorth, change, balanceSheet, capitalStatement, incomeStatement, cashFlow, metrics, reconciliation, confidence, taxYear, accounts]
      properties:
        asOf: { type: string, format: date }
        period:
          type: object
          required: [label, start, end]
          properties:
            label: { type: string }
            start: { type: string, format: date }
            end: { type: string, format: date }
        disclaimer: { type: string }
        netWorth: { $ref: "#/components/schemas/AccountingNetWorth" }
        change: { $ref: "#/components/schemas/AccountingChange" }
        balanceSheet: { $ref: "#/components/schemas/AccountingBalanceSheet" }
        capitalStatement: { $ref: "#/components/schemas/AccountingCapitalStatement" }
        incomeStatement: { $ref: "#/components/schemas/AccountingIncomeStatement" }
        cashFlow: { $ref: "#/components/schemas/AccountingCashFlow" }
        metrics: { $ref: "#/components/schemas/AccountingMetrics" }
        reconciliation: { $ref: "#/components/schemas/AccountingReconciliation" }
        confidence: { $ref: "#/components/schemas/AccountingConfidence" }
        taxYear: { $ref: "#/components/schemas/AccountingTaxYear" }
        accounts:
          type: array
          items: { $ref: "#/components/schemas/AccountingAccount" }
    SettlementRevenueLineInput:
      type: object
      required: [description, amount]
      properties:
        description: { type: string, minLength: 1, maxLength: 300 }
        category: { type: string, maxLength: 60 }
        normalizedCategory: { type: string, minLength: 1, maxLength: 80 }
        economicTreatment: { type: string, enum: [OPERATING_REVENUE, OPERATING_EXPENSE, BALANCE_SHEET_MOVEMENT, ADVANCE_RECEIVED, ADVANCE_RECOVERY, REIMBURSEMENT, REIMBURSEMENT_OFFSET, OWNER_PERSONAL_ITEM, UNKNOWN_REVIEW_REQUIRED] }
        amount: { type: string, pattern: "^[0-9]+(\\.[0-9]{1,2})?$" }
        quantity: { type: string, pattern: "^[0-9]+(\\.[0-9]{1,4})?$" }
        unitAmount: { type: string, pattern: "^[0-9]+(\\.[0-9]{1,2})?$" }
        serviceDate: { type: string, format: date }
        sourcePage: { type: number, minimum: 1 }
    SettlementDeductionLineInput:
      type: object
      required: [description, amount]
      properties:
        description: { type: string, minLength: 1, maxLength: 300 }
        category: { type: string, maxLength: 60 }
        normalizedCategory: { type: string, minLength: 1, maxLength: 80 }
        economicTreatment: { type: string, enum: [OPERATING_REVENUE, OPERATING_EXPENSE, BALANCE_SHEET_MOVEMENT, ADVANCE_RECEIVED, ADVANCE_RECOVERY, REIMBURSEMENT, REIMBURSEMENT_OFFSET, OWNER_PERSONAL_ITEM, UNKNOWN_REVIEW_REQUIRED] }
        amount: { type: string, pattern: "^[0-9]+(\\.[0-9]{1,2})?$" }
        taxDeduction: { type: boolean }
        passThrough: { type: boolean }
        ownerDraw: { type: boolean }
        reimbursement: { type: boolean }
        sourcePage: { type: number, minimum: 1 }
    SettlementDocumentInput:
      type: object
      required: [businessId, statementPeriodStart, statementPeriodEnd, revenueLines, deductionLines]
      properties:
        businessId: { type: string }
        statementPeriodStart: { type: string, format: date }
        statementPeriodEnd: { type: string, format: date }
        paidDate: { type: string, format: date }
        provider: { type: string, maxLength: 80 }
        sourceFileName: { type: string, maxLength: 255 }
        sourceObjectPath: { type: string, maxLength: 500 }
        sourceSha256: { type: string, pattern: "^[a-fA-F0-9]{64}$" }
        reportedGross: { type: string, pattern: "^[0-9]+(\\.[0-9]{1,2})?$" }
        reportedDeductions: { type: string, pattern: "^[0-9]+(\\.[0-9]{1,2})?$" }
        reportedNet: { type: string, pattern: "^[0-9]+(\\.[0-9]{1,2})?$" }
        notes: { type: string, maxLength: 1000 }
        revenueLines:
          type: array
          minItems: 1
          items: { $ref: "#/components/schemas/SettlementRevenueLineInput" }
        deductionLines:
          type: array
          items: { $ref: "#/components/schemas/SettlementDeductionLineInput" }
    BusinessSettlementDocument:
      type: object
      required: [id, businessId, sourceKind, documentType, provider, statementPeriodStart, statementPeriodEnd, paidDate, sourceFileName, sourceObjectPath, sourceSha256, extractionStatus, extractionReason, verificationStatus, reviewDecision, reviewReason, reviewedBy, reviewedAt, reportedGross, reportedDeductions, reportedNet, notes, correctedFromId, sourceVersion, createdAt, updatedAt, mathStatus, mathReason, calculatedNet, revenueLines, deductionLines]
      properties:
        id: { type: string }
        businessId: { type: string }
        sourceKind: { type: string }
        documentType: { type: string }
        provider: { type: ["string", "null"] }
        statementPeriodStart: { type: string, format: date }
        statementPeriodEnd: { type: string, format: date }
        paidDate: { type: ["string", "null"], format: date }
        sourceFileName: { type: ["string", "null"] }
        sourceObjectPath: { type: ["string", "null"] }
        sourceSha256: { type: ["string", "null"] }
        sourceContentType: { type: ["string", "null"] }
        sourceSizeBytes: { type: ["number", "null"] }
        sourcePageCount: { type: ["number", "null"] }
        extractionStatus: { type: string }
        extractionReason: { type: ["string", "null"] }
        verificationStatus: { type: string }
        reviewDecision: { type: ["string", "null"], enum: [approved, rejected, null] }
        reviewReason: { type: ["string", "null"] }
        reviewedBy: { type: ["string", "null"] }
        reviewedAt: { type: ["string", "null"], format: date-time }
        reportedGross: { type: string }
        reportedDeductions: { type: string }
        reportedNet: { type: string }
        notes: { type: ["string", "null"] }
        correctedFromId: { type: ["string", "null"] }
        sourceVersion: { type: number }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        mathStatus: { type: string }
        mathReason: { type: string }
        calculatedNet: { type: string }
        revenueLines: { type: array, items: { $ref: "#/components/schemas/BusinessSettlementRevenueLine" } }
        deductionLines: { type: array, items: { $ref: "#/components/schemas/BusinessSettlementDeductionLine" } }
    BusinessSettlementRevenueLine:
      type: object
      required: [id, lineNumber, description, category, normalizedCategory, economicTreatment, quantity, unitAmount, amount, serviceDate, sourcePage, reviewStatus, reviewDecision, reviewReason, reviewedBy, reviewedAt, createdAt]
      properties:
        id: { type: string }
        lineNumber: { type: number }
        description: { type: string }
        category: { type: string }
        normalizedCategory: { type: string }
        economicTreatment: { type: string, enum: [OPERATING_REVENUE, OPERATING_EXPENSE, BALANCE_SHEET_MOVEMENT, ADVANCE_RECEIVED, ADVANCE_RECOVERY, REIMBURSEMENT, REIMBURSEMENT_OFFSET, OWNER_PERSONAL_ITEM, UNKNOWN_REVIEW_REQUIRED] }
        quantity: { type: ["string", "null"] }
        unitAmount: { type: ["string", "null"] }
        amount: { type: string }
        serviceDate: { type: ["string", "null"], format: date }
        sourcePage: { type: ["number", "null"] }
        reviewStatus: { type: string, enum: [needs_review, approved, rejected] }
        reviewDecision: { type: ["string", "null"], enum: [approved, rejected, null] }
        reviewReason: { type: ["string", "null"] }
        reviewedBy: { type: ["string", "null"] }
        reviewedAt: { type: ["string", "null"], format: date-time }
        createdAt: { type: string, format: date-time }
    BusinessSettlementDeductionLine:
      type: object
      required: [id, lineNumber, description, category, normalizedCategory, economicTreatment, amount, taxDeduction, passThrough, ownerDraw, reimbursement, sourcePage, reviewStatus, reviewDecision, reviewReason, reviewedBy, reviewedAt, createdAt]
      properties:
        id: { type: string }
        lineNumber: { type: number }
        description: { type: string }
        category: { type: string }
        normalizedCategory: { type: string }
        economicTreatment: { type: string, enum: [OPERATING_REVENUE, OPERATING_EXPENSE, BALANCE_SHEET_MOVEMENT, ADVANCE_RECEIVED, ADVANCE_RECOVERY, REIMBURSEMENT, REIMBURSEMENT_OFFSET, OWNER_PERSONAL_ITEM, UNKNOWN_REVIEW_REQUIRED] }
        amount: { type: string }
        taxDeduction: { type: boolean }
        passThrough: { type: boolean }
        ownerDraw: { type: boolean }
        reimbursement: { type: boolean }
        sourcePage: { type: ["number", "null"] }
        reviewStatus: { type: string, enum: [needs_review, approved, rejected] }
        reviewDecision: { type: ["string", "null"], enum: [approved, rejected, null] }
        reviewReason: { type: ["string", "null"] }
        reviewedBy: { type: ["string", "null"] }
        reviewedAt: { type: ["string", "null"], format: date-time }
        createdAt: { type: string, format: date-time }
    ProfitLossLineInput:
      type: object
      required: [description, amount]
      properties:
        description: { type: string, minLength: 1, maxLength: 300 }
        category: { type: string, maxLength: 60 }
        lineType: { type: string, maxLength: 30 }
        amount: { type: string, pattern: "^[0-9]+(\\.[0-9]{1,2})?$" }
    ProfitLossDocumentInput:
      type: object
      required: [businessId, statementPeriodStart, statementPeriodEnd, reportedRevenue, reportedExpenses, reportedProfit, lines]
      properties:
        businessId: { type: string }
        statementPeriodStart: { type: string, format: date }
        statementPeriodEnd: { type: string, format: date }
        sourceFileName: { type: string, maxLength: 255 }
        sourceObjectPath: { type: string, maxLength: 500 }
        sourceSha256: { type: string, pattern: "^[a-fA-F0-9]{64}$" }
        reportedRevenue: { type: string, pattern: "^[0-9]+(\\.[0-9]{1,2})?$" }
        reportedExpenses: { type: string, pattern: "^[0-9]+(\\.[0-9]{1,2})?$" }
        reportedProfit: { type: string, pattern: "^-?[0-9]+(\\.[0-9]{1,2})?$" }
        lines:
          type: array
          minItems: 1
          items: { $ref: "#/components/schemas/ProfitLossLineInput" }
    BusinessProfitLossDocument:
      type: object
      required: [id, businessId, sourceKind, statementPeriodStart, statementPeriodEnd, sourceFileName, sourceObjectPath, sourceSha256, sourceContentType, sourceSizeBytes, sourcePageCount, extractionStatus, extractionReason, verificationStatus, reviewDecision, reviewReason, reviewedBy, reviewedAt, reportedRevenue, reportedExpenses, reportedProfit, status, createdAt, lines]
      properties:
        id: { type: string }
        businessId: { type: string }
        sourceKind: { type: string }
        statementPeriodStart: { type: string, format: date }
        statementPeriodEnd: { type: string, format: date }
        sourceFileName: { type: ["string", "null"] }
        sourceObjectPath: { type: ["string", "null"] }
        sourceSha256: { type: ["string", "null"] }
        sourceContentType: { type: ["string", "null"] }
        sourceSizeBytes: { type: ["number", "null"] }
        sourcePageCount: { type: ["number", "null"] }
        extractionStatus: { type: string }
        extractionReason: { type: ["string", "null"] }
        verificationStatus: { type: string }
        reviewDecision: { type: ["string", "null"], enum: [approved, rejected, null] }
        reviewReason: { type: ["string", "null"] }
        reviewedBy: { type: ["string", "null"] }
        reviewedAt: { type: ["string", "null"], format: date-time }
        reportedRevenue: { type: string }
        reportedExpenses: { type: string }
        reportedProfit: { type: string }
        status: { type: string }
        createdAt: { type: string, format: date-time }
        lines: { type: array, items: { $ref: "#/components/schemas/BusinessProfitLossLine" } }
    BusinessProfitLossLine:
      type: object
      required: [id, lineNumber, description, category, lineType, amount, sourcePage, reviewStatus, reviewDecision, reviewReason, reviewedBy, reviewedAt, createdAt]
      properties:
        id: { type: string }
        lineNumber: { type: number }
        description: { type: string }
        category: { type: string }
        lineType: { type: string }
        amount: { type: string }
        sourcePage: { type: ["number", "null"] }
        reviewStatus: { type: string, enum: [needs_review, approved, rejected] }
        reviewDecision: { type: ["string", "null"], enum: [approved, rejected, null] }
        reviewReason: { type: ["string", "null"] }
        reviewedBy: { type: ["string", "null"] }
        reviewedAt: { type: ["string", "null"], format: date-time }
        createdAt: { type: string, format: date-time }
    BusinessIncomeDocumentUploadInput:
      type: object
      required: [name, size, contentType, documentType]
      properties:
        name: { type: string, minLength: 1, maxLength: 255 }
        size: { type: number, minimum: 1, maximum: 10485760 }
        contentType: { type: string, enum: [application/pdf] }
        documentType: { type: string, enum: [settlement, profit_loss] }
    BusinessIncomeDocumentUpload:
      type: object
      required: [uploadURL, objectPath, name, size, contentType, documentType]
      properties:
        uploadURL: { type: string }
        objectPath: { type: string }
        name: { type: string }
        size: { type: number }
        contentType: { type: string }
        documentType: { type: string, enum: [settlement, profit_loss] }
    BusinessIncomeDocumentIngestInput:
      type: object
      required: [businessId, documentType, sourceFileName, sourceObjectPath, contentType, sourceSizeBytes]
      properties:
        businessId: { type: string }
        documentType: { type: string, enum: [settlement, profit_loss] }
        sourceFileName: { type: string, minLength: 1, maxLength: 255 }
        sourceObjectPath: { type: string, pattern: "^/objects/uploads/[0-9a-fA-F-]{36}$" }
        contentType: { type: string, enum: [application/pdf] }
        sourceSizeBytes: { type: number, minimum: 1, maximum: 10485760 }
    BusinessIncomeDocumentIngestResult:
      type: object
      required: [documentType, documentId, extractionStatus, verificationStatus, message]
      properties:
        documentType: { type: string, enum: [settlement, profit_loss] }
        documentId: { type: string }
        extractionStatus: { type: string }
        verificationStatus: { type: string }
        message: { type: string }
    BusinessIncomeDocumentReviewInput:
      type: object
      required: [decision, reason]
      properties:
        decision: { type: string, enum: [approved, rejected] }
        reason: { type: string, minLength: 1, maxLength: 1000 }
    BusinessIncomeLineReviewInput:
      type: object
      required: [decision, reason]
      properties:
        decision: { type: string, enum: [approved, rejected] }
        reason: { type: string, minLength: 1, maxLength: 1000 }
        correctedDescription: { type: string, minLength: 1, maxLength: 300 }
        correctedAmount: { type: string, pattern: "^[0-9]+(\\.[0-9]{1,2})?$" }
        normalizedCategory: { type: string, minLength: 1, maxLength: 80 }
        economicTreatment: { type: string, enum: [OPERATING_REVENUE, OPERATING_EXPENSE, BALANCE_SHEET_MOVEMENT, ADVANCE_RECEIVED, ADVANCE_RECOVERY, REIMBURSEMENT, REIMBURSEMENT_OFFSET, OWNER_PERSONAL_ITEM, UNKNOWN_REVIEW_REQUIRED] }
    BusinessIncomeDocumentReviewParams:
      type: object
      required: [documentId]
      properties:
        documentId: { type: string }
    BusinessIncomeLineReviewParams:
      type: object
      required: [documentId, lineId]
      properties:
        documentId: { type: string }
        lineId: { type: string }
    BusinessIncomeReviewResult:
      type: object
      required: [documentType, documentId, decision, verificationStatus, reviewedLineCount, conflictingLineCount, reason]
      properties:
        documentType: { type: string, enum: [settlement, profit_loss] }
        documentId: { type: string }
        decision: { type: string, enum: [approved, rejected] }
        verificationStatus: { type: string }
        reviewedLineCount: { type: number }
        conflictingLineCount: { type: number }
        reason: { type: string }
    BusinessIncomePeriodReconciliationInput:
      type: object
      required: [businessId, statementPeriodStart, statementPeriodEnd]
      properties:
        businessId: { type: string }
        statementPeriodStart: { type: string, format: date }
        statementPeriodEnd: { type: string, format: date }
        reportedProfit: { type: string, pattern: "^-?[0-9]+(\\.[0-9]{1,2})?$" }
    BusinessIncomePeriodReconciliation:
      type: object
      required: [id, businessId, statementPeriodStart, statementPeriodEnd, settlementGross, settlementDeductions, operatingExpenses, calculatedProfit, reportedProfit, variance, periodCoverage, status, reason, createdAt]
      properties:
        id: { type: string }
        businessId: { type: string }
        statementPeriodStart: { type: string, format: date }
        statementPeriodEnd: { type: string, format: date }
        settlementGross: { type: string }
        settlementDeductions: { type: string }
        operatingExpenses: { type: string }
        calculatedProfit: { type: string }
        reportedProfit: { type: ["string", "null"] }
        variance: { type: string }
        periodCoverage: { type: string }
        status: { type: string }
        reason: { type: string }
        createdAt: { type: string, format: date-time }
    BusinessSettlementCashMatch:
      type: object
      required: [id, businessId, settlementDocumentId, financeTransactionId, matchedAmount, matchStatus, confidence, reason, reviewedBy, reviewedAt, createdAt]
      properties:
        id: { type: string }
        businessId: { type: string }
        settlementDocumentId: { type: string }
        financeTransactionId: { type: ["string", "null"] }
        matchedAmount: { type: string }
        matchStatus: { type: string }
        confidence: { type: string }
        reason: { type: string }
        reviewedBy: { type: ["string", "null"] }
        reviewedAt: { type: ["string", "null"], format: date-time }
        createdAt: { type: string, format: date-time }
    BusinessCashPositionInput:
      type: object
      required: [businessId, asOf]
      properties:
        businessId: { type: string }
        asOf: { type: string, format: date }
    BusinessCashPosition:
      type: object
      required: [id, businessId, asOf, bankCash, pendingDeposits, outstandingAdvances, escrowHeld, reimbursementsDue, reserveFloor, safeToDistribute, status, reason, createdAt]
      properties:
        id: { type: string }
        businessId: { type: string }
        asOf: { type: string, format: date }
        bankCash: { type: string }
        pendingDeposits: { type: string }
        outstandingAdvances: { type: string }
        escrowHeld: { type: string }
        reimbursementsDue: { type: string }
        reserveFloor: { type: string }
        safeToDistribute: { type: string }
        status: { type: string }
        reason: { type: string }
        createdAt: { type: string, format: date-time }
    BusinessOwnerDrawInput:
      type: object
      required: [businessId, proposalDate, amount]
      properties:
        businessId: { type: string }
        proposalDate: { type: string, format: date }
        amount: { type: string, pattern: "^[0-9]+(\\.[0-9]{1,2})?$" }
        notes: { type: string, maxLength: 500 }
    BusinessOwnerDraw:
      type: object
      required: [id, businessId, proposalDate, amount, status, eligibleAmount, blockedReasons, approvedBy, approvedAt, notes, createdAt]
      properties:
        id: { type: string }
        businessId: { type: string }
        proposalDate: { type: string, format: date }
        amount: { type: string }
        status: { type: string }
        eligibleAmount: { type: string }
        blockedReasons: { type: array, items: { type: string } }
        approvedBy: { type: ["string", "null"] }
        approvedAt: { type: ["string", "null"], format: date-time }
        notes: { type: ["string", "null"] }
        createdAt: { type: string, format: date-time }
    BusinessOwnerDrawApprovalInput:
      type: object
      required: [approvedAmount]
      properties:
        approvedAmount: { type: string, pattern: "^[0-9]+(\\.[0-9]{1,2})?$" }
        notes: { type: string, maxLength: 500 }
    BusinessVerifiedHouseholdIncome:
      type: object
      required: [id, businessId, ownerDrawProposalId, incomeDate, amount, sourceType, verificationStatus, createdAt]
      properties:
        id: { type: string }
        businessId: { type: string }
        ownerDrawProposalId: { type: string }
        incomeDate: { type: string, format: date }
        amount: { type: string }
        sourceType: { type: string }
        verificationStatus: { type: string }
        createdAt: { type: string, format: date-time }
    BusinessIncomeAnomaly:
      type: object
      required: [id, businessId, anomalyType, severity, status, relatedEntityType, relatedEntityId, message, createdAt, resolvedAt, resolvedBy]
      properties:
        id: { type: string }
        businessId: { type: string }
        anomalyType: { type: string }
        severity: { type: string }
        status: { type: string }
        relatedEntityType: { type: ["string", "null"] }
        relatedEntityId: { type: ["string", "null"] }
        message: { type: string }
        createdAt: { type: string, format: date-time }
        resolvedAt: { type: ["string", "null"], format: date-time }
        resolvedBy: { type: ["string", "null"] }
    BusinessOwnerDrawApprovalResult:
      type: object
      required: [proposal, verifiedIncome]
      properties:
        proposal: { $ref: "#/components/schemas/BusinessOwnerDraw" }
        verifiedIncome: { $ref: "#/components/schemas/BusinessVerifiedHouseholdIncome" }
    BusinessIncomeIntelligence:
      type: object
      required: [asOf, summary, businesses, settlements, profitLossDocuments, reconciliationRuns, cashPositions, ownerDraws, verifiedIncome, anomalies]
      properties:
        asOf: { type: string, format: date }
        summary:
          type: object
          required: [settlementCount, reconciledSettlementCount, openAnomalyCount, pendingOwnerDrawAmount, verifiedHouseholdIncome, bankSyncMode]
          properties:
            settlementCount: { type: number }
            reconciledSettlementCount: { type: number }
            openAnomalyCount: { type: number }
            pendingOwnerDrawAmount: { type: string }
            verifiedHouseholdIncome: { type: string }
            bankSyncMode: { type: string, enum: [read_only] }
        businesses: { type: array, items: { $ref: "#/components/schemas/BusinessEntity" } }
        settlements: { type: array, items: { $ref: "#/components/schemas/BusinessSettlementDocument" } }
        profitLossDocuments: { type: array, items: { $ref: "#/components/schemas/BusinessProfitLossDocument" } }
        reconciliationRuns: { type: array, items: { $ref: "#/components/schemas/BusinessIncomePeriodReconciliation" } }
        cashPositions: { type: array, items: { $ref: "#/components/schemas/BusinessCashPosition" } }
        ownerDraws: { type: array, items: { $ref: "#/components/schemas/BusinessOwnerDraw" } }
        verifiedIncome: { type: array, items: { $ref: "#/components/schemas/BusinessVerifiedHouseholdIncome" } }
        anomalies: { type: array, items: { $ref: "#/components/schemas/BusinessIncomeAnomaly" } }
    BusinessEntity:
      type: object
      required: [id, legalName, displayName, entityType, ownershipPercentage, status, industry, taxClassification, formationDate, state, notes, createdAt, updatedAt]
      properties:
        id: { type: string }
        legalName: { type: string }
        displayName: { type: string }
        entityType: { type: string }
        ownershipPercentage: { type: string }
        taxClassification: { type: ["string", "null"] }
        industry: { type: ["string", "null"] }
        status: { type: string }
        formationDate: { type: ["string", "null"], format: date }
        state: { type: ["string", "null"] }
        notes: { type: ["string", "null"] }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    BusinessEntityInput:
      type: object
      required: [legalName, displayName, entityType, ownershipPercentage]
      properties:
        legalName: { type: string, minLength: 1, maxLength: 160 }
        displayName: { type: string, minLength: 1, maxLength: 80 }
        entityType: { type: string, minLength: 1, maxLength: 60 }
        ownershipPercentage: { type: string, pattern: "^[0-9]+(\\.[0-9]{1,3})?$" }
        taxClassification: { type: string }
        industry: { type: string }
        status: { type: string }
        formationDate: { type: string, format: date }
        state: { type: string }
        notes: { type: string, maxLength: 1000 }
    BusinessEntityUpdate:
      type: object
      properties:
        displayName: { type: string, minLength: 1, maxLength: 80 }
        entityType: { type: string, minLength: 1, maxLength: 60 }
        ownershipPercentage: { type: string, pattern: "^[0-9]+(\\.[0-9]{1,3})?$" }
        taxClassification: { type: string }
        industry: { type: string }
        status: { type: string }
        formationDate: { type: string, format: date }
        state: { type: string }
        notes: { type: string, maxLength: 1000 }
    BusinessRevenue:
      type: object
      required: [id, businessId, revenueDate, category, amount, customer, description, recurring, createdAt]
      properties:
        id: { type: string }
        businessId: { type: string }
        revenueDate: { type: string, format: date }
        category: { type: string }
        amount: { type: string }
        customer: { type: ["string", "null"] }
        description: { type: string }
        recurring: { type: boolean }
        createdAt: { type: string, format: date-time }
    BusinessRevenueInput:
      type: object
      required: [businessId, revenueDate, category, amount, description]
      properties:
        businessId: { type: string }
        revenueDate: { type: string, format: date }
        category: { type: string, minLength: 1, maxLength: 60 }
        amount: { type: string, pattern: "^[0-9]+(\\.[0-9]{1,2})?$" }
        customer: { type: string }
        description: { type: string, minLength: 1, maxLength: 500 }
        recurring: { type: boolean }
    BusinessExpense:
      type: object
      required: [id, businessId, expenseDate, category, amount, description, expenseType, classification, createdAt]
      properties:
        id: { type: string }
        businessId: { type: string }
        expenseDate: { type: string, format: date }
        category: { type: string }
        amount: { type: string }
        description: { type: string }
        expenseType: { type: string }
        classification: { type: string }
        createdAt: { type: string, format: date-time }
    BusinessExpenseInput:
      type: object
      required: [businessId, expenseDate, category, amount, description]
      properties:
        businessId: { type: string }
        expenseDate: { type: string, format: date }
        category: { type: string, minLength: 1, maxLength: 60 }
        amount: { type: string, pattern: "^[0-9]+(\\.[0-9]{1,2})?$" }
        description: { type: string, minLength: 1, maxLength: 500 }
        expenseType: { type: string }
        classification: { type: string }
    BusinessReserve:
      type: object
      required: [businessId, targetMethod, targetAmount, taxReserve, safetyBuffer, updatedAt]
      properties:
        businessId: { type: string }
        targetMethod: { type: string }
        targetAmount: { type: string }
        taxReserve: { type: string }
        safetyBuffer: { type: string }
        updatedAt: { type: string, format: date-time }
    BusinessReserveUpdate:
      type: object
      properties:
        targetMethod: { type: string }
        targetAmount: { type: string, pattern: "^[0-9]+(\\.[0-9]{1,2})?$" }
        taxReserve: { type: string, pattern: "^[0-9]+(\\.[0-9]{1,2})?$" }
        safetyBuffer: { type: string, pattern: "^[0-9]+(\\.[0-9]{1,2})?$" }
    BusinessDistribution:
      type: object
      required: [id, businessId, distributionDate, amount, status, householdDestination, notes, createdAt]
      properties:
        id: { type: string }
        businessId: { type: string }
        distributionDate: { type: string, format: date }
        amount: { type: string }
        status: { type: string }
        householdDestination: { type: string }
        notes: { type: ["string", "null"] }
        createdAt: { type: string, format: date-time }
    BusinessDistributionInput:
      type: object
      required: [businessId, distributionDate, amount]
      properties:
        businessId: { type: string }
        distributionDate: { type: string, format: date }
        amount: { type: string, pattern: "^[0-9]+(\\.[0-9]{1,2})?$" }
        householdDestination: { type: string }
        notes: { type: string, maxLength: 500 }
    BusinessOverview:
      type: object
      required: [asOf, totals, health, businesses, recentRevenue, recentExpenses, distributions]
      properties:
        asOf: { type: string, format: date }
        totals:
          type: object
          required: [totalRevenue, businessProfit, ownerDistributions, businessCash, businessLiabilities, estimatedBusinessEquity, activeBusinesses, revenueThisMonth, profitMargin, ownerPay, reserveCoverageMonths, householdIncomeContribution, incomeConcentration, safeToDistribute]
          properties:
            totalRevenue: { type: string }
            businessProfit: { type: string }
            ownerDistributions: { type: string }
            businessCash: { type: string }
            businessLiabilities: { type: string }
            estimatedBusinessEquity: { type: string }
            activeBusinesses: { type: number }
            revenueThisMonth: { type: string }
            profitMargin: { type: number }
            ownerPay: { type: string }
            reserveCoverageMonths: { type: number }
            householdIncomeContribution: { type: string }
            incomeConcentration: { type: number }
            safeToDistribute: { type: string }
        health: { type: number, minimum: 0, maximum: 100 }
        businesses: { type: array, items: { $ref: "#/components/schemas/BusinessEntity" } }
        recentRevenue: { type: array, items: { $ref: "#/components/schemas/BusinessRevenue" } }
        recentExpenses: { type: array, items: { $ref: "#/components/schemas/BusinessExpense" } }
        distributions: { type: array, items: { $ref: "#/components/schemas/BusinessDistribution" } }
    TreasuryBucket:
      type: object
      required: [id, name, bucketType, priority, targetAmount, minimumAmount, maximumAmount, currentBalance, protected, liquid, liquidityClass, riskClass, withdrawalPolicy, fundingRule]
      properties:
        id: { type: string }
        name: { type: string }
        bucketType: { type: string }
        priority: { type: number }
        targetAmount: { type: string }
        minimumAmount: { type: string }
        maximumAmount: { type: string }
        currentBalance: { type: string }
        protected: { type: boolean }
        liquid: { type: boolean }
        liquidityClass: { type: string }
        riskClass: { type: string }
        withdrawalPolicy: { type: string }
        fundingRule: { type: string }
    TreasuryPolicy:
      type: object
      required: [minimumOperatingCash, emergencyTargetMonths, minimumWeeklyDuplexContribution, maximumStrategyPercent, maximumSingleStrategyPercent, maximumSingleVenuePercent, maximumIlliquidPercent, maximumActivePercent, autoScale, hierarchy, version, emergencyTarget]
      properties:
        minimumOperatingCash: { type: string }
        emergencyTargetMonths: { type: number }
        minimumWeeklyDuplexContribution: { type: string }
        maximumStrategyPercent: { type: string }
        maximumSingleStrategyPercent: { type: string }
        maximumSingleVenuePercent: { type: string }
        maximumIlliquidPercent: { type: string }
        maximumActivePercent: { type: string }
        autoScale: { type: boolean }
        hierarchy: { type: array, items: { type: string } }
        version: { type: string }
        emergencyTarget: { type: string }
    TreasuryHealth:
      type: object
      required: [score, state, deployability, utilization, activeCapitalPercent, liquidityCoverage, emergencyCoverage, duplexProgressPercent, cashDrag]
      properties:
        score: { type: number }
        state: { type: string }
        deployability: { type: number }
        utilization: { type: number }
        activeCapitalPercent: { type: number }
        liquidityCoverage: { type: number }
        emergencyCoverage: { type: number }
        duplexProgressPercent: { type: number }
        cashDrag: { type: string }
    TreasuryTotals:
      type: object
      required: [totalCapital, protectedCapital, liquidReserve, duplexCapital, opportunityCapital, treasuryCapital, activeStrategyCapital, safeToDeploy]
      properties:
        totalCapital: { type: string }
        protectedCapital: { type: string }
        liquidReserve: { type: string }
        duplexCapital: { type: string }
        opportunityCapital: { type: string }
        treasuryCapital: { type: string }
        activeStrategyCapital: { type: string }
        safeToDeploy: { type: string }
    TreasuryLadderItem:
      type: object
      required: [liquidityClass, amount, percent]
      properties:
        liquidityClass: { type: string }
        amount: { type: string }
        percent: { type: number }
    TreasuryStressTest:
      type: object
      required: [name, remainingLiquid, remainingReserves, liquidityMonths, duplexProtected, safeToDeploy, status]
      properties:
        name: { type: string }
        remainingLiquid: { type: string }
        remainingReserves: { type: string }
        liquidityMonths: { type: number }
        duplexProtected: { type: boolean }
        safeToDeploy: { type: string }
        status: { type: string }
    CapitalRequest:
      type: object
      required: [id, requestingModule, requestedAmount, purpose, expectedDuration, riskClass, expectedReturnAssumption, liquidityRequirement, currentAllocation, requestedNewAllocation, evidence, status, decisionReason, createdAt, reviewedAt]
      properties:
        id: { type: string }
        requestingModule: { type: string }
        strategyId: { type: ["string", "null"] }
        requestedAmount: { type: string }
        purpose: { type: string }
        expectedDuration: { type: string }
        riskClass: { type: string }
        expectedReturnAssumption: { type: string }
        liquidityRequirement: { type: string }
        currentAllocation: { type: string }
        requestedNewAllocation: { type: string }
        evidence: { type: array, items: { type: string } }
        status: { type: string }
        decisionReason: { type: ["string", "null"] }
        createdAt: { type: string, format: date-time }
        reviewedAt: { type: ["string", "null"], format: date-time }
    CapitalRequestInput:
      type: object
      required: [requestingModule, requestedAmount, purpose, expectedDuration, riskClass, expectedReturnAssumption, liquidityRequirement]
      properties:
        requestingModule: { type: string, minLength: 1, maxLength: 120 }
        strategyId: { type: string }
        requestedAmount: { type: string, pattern: "^[0-9]+(\\.[0-9]{1,2})?$" }
        purpose: { type: string, minLength: 1, maxLength: 1000 }
        expectedDuration: { type: string, minLength: 1, maxLength: 120 }
        riskClass: { type: string, enum: [protected, conservative, moderate, experimental] }
        expectedReturnAssumption: { type: string, minLength: 1, maxLength: 500 }
        liquidityRequirement: { type: string, minLength: 1, maxLength: 120 }
        currentAllocation: { type: string, pattern: "^[0-9]+(\\.[0-9]{1,2})?$" }
        requestedNewAllocation: { type: string, pattern: "^[0-9]+(\\.[0-9]{1,2})?$" }
        evidence: { type: array, items: { type: string }, maxItems: 10 }
    CapitalRequestDecisionInput:
      type: object
      required: [decision, reason]
      properties:
        decision: { type: string, enum: [APPROVED, PARTIALLY_APPROVED, REJECTED] }
        approvedAmount: { type: string, pattern: "^[0-9]+(\\.[0-9]{1,2})?$" }
        reason: { type: string, minLength: 1, maxLength: 1000 }
    TreasurySnapshot:
      type: object
      required: [totals, health, policy, liquidityLadder, stressTests, alerts, nextAction, buckets, requests, reservations, lastUpdated]
      properties:
        totals: { $ref: "#/components/schemas/TreasuryTotals" }
        health: { $ref: "#/components/schemas/TreasuryHealth" }
        policy: { $ref: "#/components/schemas/TreasuryPolicy" }
        liquidityLadder:
          type: array
          items: { $ref: "#/components/schemas/TreasuryLadderItem" }
        stressTests:
          type: array
          items: { $ref: "#/components/schemas/TreasuryStressTest" }
        alerts: { type: array, items: { type: string } }
        nextAction: { type: string }
        buckets:
          type: array
          items: { $ref: "#/components/schemas/TreasuryBucket" }
        requests:
          type: array
          items: { $ref: "#/components/schemas/CapitalRequest" }
        reservations:
          type: array
          items:
            type: object
            required: [id, bucketId, reason, reservedAmount, startsAt, expiresAt, status]
            properties:
              id: { type: string }
              bucketId: { type: string }
              reason: { type: string }
              reservedAmount: { type: string }
              startsAt: { type: string, format: date }
              expiresAt: { type: ["string", "null"], format: date }
              status: { type: string }
        lastUpdated: { type: string, format: date-time }
    FinancingPolicy:
      type: object
      required: [mode, executionEnabled, providersEnabled, lenderApprovalClaimsAllowed, disclaimer, prohibitedActions, moneyTreatment]
      properties:
        mode: { type: string }
        executionEnabled: { type: boolean }
        providersEnabled: { type: boolean }
        lenderApprovalClaimsAllowed: { type: boolean }
        disclaimer: { type: string }
        prohibitedActions: { type: array, items: { type: string } }
        moneyTreatment: { type: string }
    FinancingLiability:
      type: object
      required: [id, name, liabilityType, ownership, status, originalBalance, currentBalance, monthlyPayment, interestRate, creditLimit, createdAt, updatedAt]
      properties:
        id: { type: string }
        name: { type: string }
        liabilityType: { type: string }
        ownership: { type: string }
        businessEntityId: { type: ["string", "null"] }
        status: { type: string }
        originalBalance: { type: string }
        currentBalance: { type: string }
        monthlyPayment: { type: string }
        interestRate: { type: string }
        termMonths: { type: ["number", "null"] }
        remainingTermMonths: { type: ["number", "null"] }
        creditLimit: { type: string }
        notes: { type: ["string", "null"] }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    FinancingLiabilityInput:
      type: object
      required: [name, liabilityType, ownership, currentBalance, monthlyPayment]
      properties:
        name: { type: string, minLength: 1 }
        liabilityType: { type: string }
        ownership: { type: string, enum: [household, business] }
        businessEntityId: { type: ["string", "null"] }
        currentBalance: { type: string }
        monthlyPayment: { type: string }
        interestRate: { type: number, minimum: 0, maximum: 100 }
        termMonths: { type: ["number", "null"], minimum: 1 }
        remainingTermMonths: { type: ["number", "null"], minimum: 0 }
        creditLimit: { type: ["string", "null"] }
        notes: { type: ["string", "null"] }
    FinancingCreditProfile:
      type: object
      required: [id, score, scoreSource, scoreAsOf, scoreConfidence, creditworthinessStatus, utilizationPercent, paymentHistoryStatus, scoreIsNotApproval, updatedAt]
      properties:
        id: { type: string }
        score: { type: ["number", "null"], minimum: 300, maximum: 850 }
        scoreSource: { type: string }
        scoreAsOf: { type: ["string", "null"] }
        scoreConfidence: { type: string }
        creditworthinessStatus: { type: string }
        utilizationPercent: { type: string }
        paymentHistoryStatus: { type: string }
        notes: { type: ["string", "null"] }
        scoreIsNotApproval: { type: boolean }
        updatedAt: { type: string, format: date-time }
    FinancingCreditProfileUpdate:
      type: object
      properties:
        score: { type: ["number", "null"], minimum: 300, maximum: 850 }
        scoreSource: { type: string }
        scoreAsOf: { type: ["string", "null"] }
        scoreConfidence: { type: string }
        creditworthinessStatus: { type: string }
        paymentHistoryStatus: { type: string }
        notes: { type: ["string", "null"] }
    FinancingScenario:
      type: object
      required: [id, name, loanType, purchasePrice, downPaymentPercent, interestRate, termYears, loanAmount, monthlyPrincipalInterest, estimatedMonthlyHousingCost, createdAt, updatedAt]
      properties:
        id: { type: string }
        name: { type: string }
        propertyCandidateId: { type: ["string", "null"] }
        loanType: { type: string }
        purchasePrice: { type: string }
        downPaymentPercent: { type: string }
        interestRate: { type: string }
        termYears: { type: string }
        mortgageInsurance: { type: string }
        loanFees: { type: string }
        closingCosts: { type: string }
        initialReserves: { type: string }
        loanAmount: { type: string }
        monthlyPrincipalInterest: { type: string }
        estimatedMonthlyHousingCost: { type: string }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    FinancingScenarioInput:
      type: object
      required: [name, loanType, purchasePrice, downPaymentPercent, interestRate, termYears]
      properties:
        name: { type: string, minLength: 1 }
        propertyCandidateId: { type: ["string", "null"] }
        loanType: { type: string }
        purchasePrice: { type: string }
        downPaymentPercent: { type: number, minimum: 0, maximum: 1 }
        interestRate: { type: number, minimum: 0, maximum: 100 }
        termYears: { type: number, minimum: 1, maximum: 50 }
        mortgageInsurance: { type: string }
        loanFees: { type: string }
        closingCosts: { type: string }
        initialReserves: { type: string }
    FinancingOffer:
      type: object
      required: [id, name, programLabel, loanType, commitmentStatus, offerStatus, loanAmount, interestRate, termYears, estimatedMonthlyPayment, estimatedCashToClose, executionEnabled, createdAt, updatedAt]
      properties:
        id: { type: string }
        name: { type: string }
        propertyCandidateId: { type: ["string", "null"] }
        lenderLabel: { type: ["string", "null"] }
        programLabel: { type: string }
        loanType: { type: string }
        commitmentStatus: { type: string }
        offerStatus: { type: string }
        loanAmount: { type: string }
        interestRate: { type: string }
        termYears: { type: number }
        estimatedMonthlyPayment: { type: string }
        estimatedCashToClose: { type: string }
        expirationDate: { type: ["string", "null"] }
        assumptions: { type: object, additionalProperties: true }
        notes: { type: ["string", "null"] }
        executionEnabled: { type: boolean }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    FinancingOfferInput:
      type: object
      required: [name, programLabel, loanType, commitmentStatus, offerStatus, loanAmount, interestRate, termYears, estimatedMonthlyPayment, estimatedCashToClose]
      properties:
        name: { type: string }
        propertyCandidateId: { type: ["string", "null"] }
        lenderLabel: { type: ["string", "null"] }
        programLabel: { type: string }
        loanType: { type: string }
        commitmentStatus: { type: string, enum: [illustrative, indicative, conditional, verified] }
        offerStatus: { type: string }
        loanAmount: { type: string }
        interestRate: { type: number, minimum: 0, maximum: 100 }
        termYears: { type: number, minimum: 1, maximum: 50 }
        estimatedMonthlyPayment: { type: string }
        estimatedCashToClose: { type: string }
        expirationDate: { type: ["string", "null"] }
        assumptions: { type: object, additionalProperties: true }
        notes: { type: ["string", "null"] }
    FinancingPipelineEvent:
      type: object
      required: [id, pipelineId, toStage, executionEnabled, createdAt]
      properties:
        id: { type: string }
        pipelineId: { type: string }
        fromStage: { type: ["string", "null"] }
        toStage: { type: string }
        note: { type: ["string", "null"] }
        executionEnabled: { type: boolean }
        createdAt: { type: string, format: date-time }
    FinancingPipelineEventInput:
      type: object
      required: [toStage]
      properties:
        toStage: { type: string, enum: [research, readiness, comparison, human_review, paused] }
        note: { type: ["string", "null"] }
    FinancingDocument:
      type: object
      required: [id, category, name, status, sensitivity, metadata, updatedAt]
      properties:
        id: { type: string }
        category: { type: string }
        name: { type: string }
        status: { type: string, enum: [needed, in_review, complete, expired] }
        sensitivity: { type: string }
        dueDate: { type: ["string", "null"] }
        storagePath: { type: ["string", "null"] }
        metadata: { type: object, additionalProperties: true }
        notes: { type: ["string", "null"] }
        updatedAt: { type: string, format: date-time }
    FinancingDocumentUpdate:
      type: object
      required: [status]
      properties:
        status: { type: string, enum: [needed, in_review, complete, expired] }
        notes: { type: ["string", "null"] }
    FinancingSnapshot:
      type: object
      required: [policy, creditProfile, liabilities, scenarios, offers, pipeline, documents, readiness, cashToClose, capitalGovernor]
      properties:
        policy: { $ref: "#/components/schemas/FinancingPolicy" }
        creditProfile: { $ref: "#/components/schemas/FinancingCreditProfile" }
        liabilities: { type: array, items: { $ref: "#/components/schemas/FinancingLiability" } }
        scenarios: { type: array, items: { $ref: "#/components/schemas/FinancingScenario" } }
        offers: { type: array, items: { $ref: "#/components/schemas/FinancingOffer" } }
        pipeline: { type: object, additionalProperties: true }
        documents: { type: array, items: { $ref: "#/components/schemas/FinancingDocument" } }
        readiness: { type: object, additionalProperties: true }
        cashToClose: { type: object, additionalProperties: true }
        capitalGovernor: { type: object, additionalProperties: true }
    OperationsTask:
      type: object
      required: [id, title, description, domain, priority, status, dueDate, assignedTo, source, requiresApproval, createdAt, completedAt, completedBy]
      properties:
        id: { type: string }
        title: { type: string }
        description: { type: string }
        domain: { type: string }
        priority: { type: string, enum: [CRITICAL, HIGH, MEDIUM, LOW] }
        status: { type: string, enum: [OPEN, IN_PROGRESS, WAITING, BLOCKED, COMPLETED, DISMISSED, EXPIRED] }
        dueDate: { type: string, format: date }
        assignedTo: { type: ["string", "null"] }
        source: { type: string }
        relatedEntityType: { type: ["string", "null"] }
        relatedEntityId: { type: ["string", "null"] }
        requiresApproval: { type: boolean }
        createdAt: { type: string, format: date-time }
        completedAt: { type: ["string", "null"], format: date-time }
        completedBy: { type: ["string", "null"] }
    OperationsTaskInput:
      type: object
      required: [title, description, domain, priority, dueDate]
      properties:
        title: { type: string, minLength: 1, maxLength: 160 }
        description: { type: string, minLength: 1, maxLength: 1000 }
        domain: { type: string, minLength: 1, maxLength: 40 }
        priority: { type: string, enum: [CRITICAL, HIGH, MEDIUM, LOW] }
        dueDate: { type: string, format: date }
        assignedTo: { type: string }
        requiresApproval: { type: boolean }
    OperationsTaskUpdate:
      type: object
      properties:
        status: { type: string, enum: [OPEN, IN_PROGRESS, WAITING, BLOCKED, COMPLETED, DISMISSED, EXPIRED] }
        assignedTo: { type: string }
    DailyOpsJournalEntry:
      type: object
      required: [id, actorId, entryType, title, decisionContext, outcome, evidenceLinks, unresolvedBlockers, relatedEntityType, relatedEntityId, createdAt]
      properties:
        id: { type: string }
        actorId: { type: string }
        entryType: { type: string, enum: [DECISION, HANDOFF, CLOSEOUT] }
        title: { type: string }
        decisionContext: { type: string }
        outcome: { type: ["string", "null"] }
        evidenceLinks: { type: array, items: { type: string } }
        unresolvedBlockers: { type: array, items: { type: string } }
        relatedEntityType: { type: ["string", "null"] }
        relatedEntityId: { type: ["string", "null"] }
        createdAt: { type: string, format: date-time }
    DailyOpsJournalEntryInput:
      type: object
      required: [entryType, title, decisionContext]
      properties:
        entryType: { type: string, enum: [DECISION, HANDOFF, CLOSEOUT] }
        title: { type: string, minLength: 1, maxLength: 160 }
        decisionContext: { type: string, minLength: 1, maxLength: 2000 }
        outcome: { type: ["string", "null"], maxLength: 1000 }
        evidenceLinks: { type: array, items: { type: string, maxLength: 1000 }, maxItems: 20 }
        unresolvedBlockers: { type: array, items: { type: string, maxLength: 500 }, maxItems: 20 }
        relatedEntityType: { type: ["string", "null"], maxLength: 80 }
        relatedEntityId: { type: ["string", "null"], maxLength: 160 }
    GuidedRunEvent:
      type: object
      required: [id, action, reason, actorId, snoozedUntil, occurredAt]
      properties:
        id: { type: string }
        action: { type: string, enum: [START, COMPLETE, REOPEN, SNOOZE, BLOCK] }
        reason: { type: string }
        actorId: { type: string }
        snoozedUntil: { type: ["string", "null"], format: date-time }
        occurredAt: { type: string, format: date-time }
    GuidedRun:
      type: object
      required: [id, runDate, cadence, status, latestReason, startedAt, completedAt, snoozedUntil, createdBy, updatedBy, createdAt, updatedAt, events]
      properties:
        id: { type: string }
        runDate: { type: string, format: date }
        cadence: { type: string, enum: [TODAY, WEEK, MONTH] }
        status: { type: string, enum: [NOT_STARTED, IN_PROGRESS, COMPLETED, REOPENED, SNOOZED, BLOCKED] }
        latestReason: { type: ["string", "null"] }
        startedAt: { type: ["string", "null"], format: date-time }
        completedAt: { type: ["string", "null"], format: date-time }
        snoozedUntil: { type: ["string", "null"], format: date-time }
        createdBy: { type: string }
        updatedBy: { type: string }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        events: { type: array, items: { $ref: "#/components/schemas/GuidedRunEvent" } }
    DailyOpsHistory:
      type: object
      required: [journalEntries, guidedRuns]
      properties:
        journalEntries: { type: array, items: { $ref: "#/components/schemas/DailyOpsJournalEntry" } }
        guidedRuns: { type: array, items: { $ref: "#/components/schemas/GuidedRun" } }
    GuidedRunActionInput:
      type: object
      required: [action, cadence, reason]
      properties:
        action: { type: string, enum: [START, COMPLETE, REOPEN, SNOOZE, BLOCK] }
        cadence: { type: string, enum: [TODAY, WEEK, MONTH] }
        runId: { type: string }
        reason: { type: string, minLength: 1, maxLength: 1000 }
        snoozedUntil: { type: ["string", "null"], format: date-time }
    GuidedRunActionResult:
      type: object
      required: [run, event]
      properties:
        run: { $ref: "#/components/schemas/GuidedRun" }
        event: { $ref: "#/components/schemas/GuidedRunEvent" }
    OperationsApproval:
      type: object
      required: [id, requestType, requestedBy, relatedEntity, currentState, proposedState, financialImpact, riskImpact, duplexImpact, reason, evidence, requiredAuthority, expiresAt, status, createdAt]
      properties:
        id: { type: string }
        requestType: { type: string }
        requestedBy: { type: string }
        relatedEntity: { type: string }
        currentState: { type: string }
        proposedState: { type: string }
        financialImpact: { type: string }
        riskImpact: { type: string }
        duplexImpact: { type: string }
        reason: { type: string }
        evidence: { type: array, items: { type: string } }
        requiredAuthority: { type: string }
        expiresAt: { type: ["string", "null"], format: date-time }
        status: { type: string, enum: [PENDING, APPROVED, REJECTED, DEFERRED, EXPIRED, CANCELLED] }
        createdAt: { type: string, format: date-time }
        decidedAt: { type: ["string", "null"], format: date-time }
    OperationsApprovalDecision:
      type: object
      required: [decision, reason]
      properties:
        decision: { type: string, enum: [APPROVED, REJECTED, DEFERRED] }
        reason: { type: string, minLength: 1, maxLength: 1000 }
    OperationsAlert:
      type: object
      required: [id, alertKey, title, message, severity, domain, status, occurrenceCount, firstSeen, lastSeen]
      properties:
        id: { type: string }
        alertKey: { type: string }
        title: { type: string }
        message: { type: string }
        severity: { type: string, enum: [INFO, LOW, MEDIUM, HIGH, CRITICAL] }
        domain: { type: string }
        status: { type: string, enum: [ACTIVE, ACKNOWLEDGED, RESOLVED, DISMISSED] }
        occurrenceCount: { type: number, minimum: 0 }
        firstSeen: { type: string, format: date-time }
        lastSeen: { type: string, format: date-time }
        resolvedAt: { type: ["string", "null"], format: date-time }
    OperationsAlertUpdate:
      type: object
      required: [status]
      properties:
        status: { type: string, enum: [ACKNOWLEDGED, RESOLVED, DISMISSED] }
    OperationsAutomation:
      type: object
      required: [id, name, trigger, action, enabled, protected, priority, lastRun, nextRun]
      properties:
        id: { type: string }
        name: { type: string }
        trigger: { type: string }
        action: { type: string }
        enabled: { type: boolean }
        protected: { type: boolean }
        priority: { type: number, minimum: 0 }
        lastRun: { type: ["string", "null"], format: date-time }
        nextRun: { type: ["string", "null"], format: date-time }
    OperationsAutomationRun:
      type: object
      required: [id, automationId, result, actionsCreated, errors, startedAt, completedAt, safeBoundary]
      properties:
        id: { type: string }
        automationId: { type: string }
        result: { type: string }
        actionsCreated: { type: array, items: { type: string } }
        errors: { type: array, items: { type: string } }
        startedAt: { type: string, format: date-time }
        completedAt: { type: string, format: date-time }
        safeBoundary: { type: string }
    OperationsNotificationPreferences:
      type: object
      required: [criticalAlerts, bills, budget, duplexGoal, property, strategies, accounting, security, weeklyReports, monthlyReports, quietHoursStart, quietHoursEnd]
      properties:
        criticalAlerts: { type: array, items: { type: string } }
        bills: { type: array, items: { type: string } }
        budget: { type: array, items: { type: string } }
        duplexGoal: { type: array, items: { type: string } }
        property: { type: array, items: { type: string } }
        strategies: { type: array, items: { type: string } }
        accounting: { type: array, items: { type: string } }
        security: { type: array, items: { type: string } }
        weeklyReports: { type: array, items: { type: string } }
        monthlyReports: { type: array, items: { type: string } }
        quietHoursStart: { type: ["string", "null"] }
        quietHoursEnd: { type: ["string", "null"] }
    OperationsNotificationPreferencesUpdate:
      type: object
      properties:
        criticalAlerts: { type: array, items: { type: string } }
        bills: { type: array, items: { type: string } }
        budget: { type: array, items: { type: string } }
        duplexGoal: { type: array, items: { type: string } }
        property: { type: array, items: { type: string } }
        strategies: { type: array, items: { type: string } }
        accounting: { type: array, items: { type: string } }
        security: { type: array, items: { type: string } }
        weeklyReports: { type: array, items: { type: string } }
        monthlyReports: { type: array, items: { type: string } }
        quietHoursStart: { type: ["string", "null"] }
        quietHoursEnd: { type: ["string", "null"] }
    OperationsOverview:
      type: object
      required: [today, nextBestAction, noActionRequired, health, tasks, approvals, alerts, automations, notifications]
      properties:
        today:
          type: object
          required: [criticalAlerts, approvalsPending, tasksDue, billsDue, goalActions, propertyActions, securityActions, reviewsScheduled]
          properties:
            criticalAlerts: { type: number, minimum: 0 }
            approvalsPending: { type: number, minimum: 0 }
            tasksDue: { type: number, minimum: 0 }
            billsDue: { type: number, minimum: 0 }
            goalActions: { type: number, minimum: 0 }
            propertyActions: { type: number, minimum: 0 }
            securityActions: { type: number, minimum: 0 }
            reviewsScheduled: { type: number, minimum: 0 }
        nextBestAction: { type: ["string", "null"] }
        noActionRequired: { type: boolean }
        health:
          type: object
          required: [score, overdueTasks, pendingApprovals, criticalAlerts, bills, accounting, security, automationFailures]
          properties:
            score: { type: number, minimum: 0, maximum: 100 }
            overdueTasks: { type: number, minimum: 0 }
            pendingApprovals: { type: number, minimum: 0 }
            criticalAlerts: { type: number, minimum: 0 }
            bills: { type: string }
            accounting: { type: string }
            security: { type: string }
            automationFailures: { type: number, minimum: 0 }
        tasks: { type: array, items: { $ref: "#/components/schemas/OperationsTask" } }
        approvals: { type: array, items: { $ref: "#/components/schemas/OperationsApproval" } }
        alerts: { type: array, items: { $ref: "#/components/schemas/OperationsAlert" } }
        automations: { type: array, items: { $ref: "#/components/schemas/OperationsAutomation" } }
        notifications: { $ref: "#/components/schemas/OperationsNotificationPreferences" }
    OperationsJob:
      type: object
      required: [id, householdId, jobKey, kind, status, priority, attempts, maxAttempts, availableAt, claimedAt, claimedBy, leaseOwner, leaseExpiresAt, startedAt, idempotencyKey, correlationId, payloadReference, lastError, deadLetterReason, createdAt, updatedAt, completedAt]
      properties:
        id: { type: string }
        householdId: { type: string }
        jobKey: { type: string }
        kind: { type: string }
        status: { type: string, enum: [QUEUED, LEASED, RUNNING, RETRY_PENDING, SUCCEEDED, DEAD_LETTER] }
        priority: { type: number }
        attempts: { type: number, minimum: 0 }
        maxAttempts: { type: number, minimum: 1 }
        availableAt: { type: string, format: date-time }
        claimedAt: { type: ["string", "null"], format: date-time }
        claimedBy: { type: ["string", "null"] }
        leaseOwner: { type: ["string", "null"] }
        leaseExpiresAt: { type: ["string", "null"], format: date-time }
        startedAt: { type: ["string", "null"], format: date-time }
        idempotencyKey: { type: ["string", "null"] }
        correlationId: { type: ["string", "null"] }
        payloadReference: { type: ["string", "null"] }
        lastError: { type: ["string", "null"] }
        deadLetterReason: { type: ["string", "null"] }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        completedAt: { type: ["string", "null"], format: date-time }
    OperationsJobMetrics:
      type: object
      required: [queueDepth, oldestPendingJobAgeMs, retryQueueDepth, deadLetterCount, activeWorkerCount, staleWorkerCount, workerHeartbeatAgeMs, jobExecutionDurationMs, jobFailureCount, jobRetryCount, recoveredJobCount, statuses]
      properties:
        queueDepth: { type: number, minimum: 0 }
        oldestPendingJobAgeMs: { type: number, minimum: 0 }
        retryQueueDepth: { type: number, minimum: 0 }
        deadLetterCount: { type: number, minimum: 0 }
        activeWorkerCount: { type: number, minimum: 0 }
        staleWorkerCount: { type: number, minimum: 0 }
        workerHeartbeatAgeMs: { type: number, minimum: 0 }
        jobExecutionDurationMs: { type: number, minimum: 0 }
        jobFailureCount: { type: number, minimum: 0 }
        jobRetryCount: { type: number, minimum: 0 }
        recoveredJobCount: { type: number, minimum: 0 }
        statuses: { type: object, additionalProperties: { type: number } }
    OperationsSchedulerMetrics:
      type: object
      required: [schedulerHeartbeat, schedulerLeader, schedulerLagMs, missedScheduleCount, scheduleRecoveryCount]
      properties:
        schedulerHeartbeat: { type: ["string", "null"], format: date-time }
        schedulerLeader: { type: boolean }
        schedulerLagMs: { type: number, minimum: 0 }
        missedScheduleCount: { type: number, minimum: 0 }
        scheduleRecoveryCount: { type: number, minimum: 0 }
    OperationsWorker:
      type: object
      required: [workerId, status, currentJobId, startedAt, lastHeartbeatAt, version, updatedAt]
      properties:
        workerId: { type: string }
        status: { type: string }
        currentJobId: { type: ["string", "null"] }
        startedAt: { type: string, format: date-time }
        lastHeartbeatAt: { type: string, format: date-time }
        version: { type: string }
        updatedAt: { type: string, format: date-time }
    OperationsScheduler:
      type: object
      required: [id, householdId, name, jobKind, cadence, payload, missedRunPolicy, enabled, nextRunAt, lastRunAt, createdBy, createdAt, updatedAt]
      properties:
        id: { type: string }
        householdId: { type: string }
        name: { type: string }
        jobKind: { type: string }
        cadence: { type: string }
        payload: { type: object, additionalProperties: true }
        missedRunPolicy: { type: string, enum: [CATCH_UP, SKIP] }
        enabled: { type: boolean }
        nextRunAt: { type: string, format: date-time }
        lastRunAt: { type: ["string", "null"], format: date-time }
        createdBy: { type: ["string", "null"] }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    OperationsSchedulerLease:
      type: object
      required: [singleton, ownerId, leaseExpiresAt, heartbeatAt]
      properties:
        singleton: { type: string }
        ownerId: { type: string }
        leaseExpiresAt: { type: string, format: date-time }
        heartbeatAt: { type: string, format: date-time }
    RecalculateVariableIncomeProfileInput:
      type: object
      properties:
        calculationDate: { type: string, pattern: "^[0-9]{4}-[0-9]{2}-[0-9]{2}$" }
        policy:
          type: string
          enum: [CONSERVATIVE_MINIMUM, TRAILING_MEDIAN_DISCOUNTED, USER_APPROVED_FLOOR]
        approvedFloor: { type: string, pattern: "^[0-9]+(\\.[0-9]{1,2})?$" }
    VariableIncomeProfile:
      type: object
      required: [calculationDate, currentMonthVerifiedIncome, trailing4WeekIncome, trailing8WeekIncome, trailing13WeekIncome, trailing3MonthIncome, trailing6MonthIncome, highestRecentMonth, lowestRecentMonth, medianRecentMonth, incomeFloor, baseIncome, strongMonthIncome, incomeVolatility, sourceCount, sourceFreshness, policy, confidenceStatus, recentMonthCount]
      properties:
        id: { type: string }
        calculationDate: { type: string }
        currentMonthVerifiedIncome: { type: string }
        trailing4WeekIncome: { type: string }
        trailing8WeekIncome: { type: string }
        trailing13WeekIncome: { type: string }
        trailing3MonthIncome: { type: string }
        trailing6MonthIncome: { type: string }
        highestRecentMonth: { type: string }
        lowestRecentMonth: { type: string }
        medianRecentMonth: { type: string }
        incomeFloor: { type: string }
        baseIncome: { type: string }
        strongMonthIncome: { type: string }
        incomeVolatility: { type: number }
        sourceCount: { type: number }
        sourceFreshness: { type: ["string", "null"] }
        policy: { type: string }
        confidenceStatus: { type: string }
        recentMonthCount: { type: number }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    VehicleScenarioInput:
      type: object
      required: [name, insurance, fuel, maintenanceReserve, registrationReserve, parkingTolls, otherMonthlyCost]
      properties:
        name: { type: string, minLength: 1, maxLength: 120 }
        vehiclePrice: { type: string, pattern: "^[0-9]+(\\.[0-9]{1,2})?$" }
        downPayment: { type: string, pattern: "^[0-9]+(\\.[0-9]{1,2})?$" }
        loanAmount: { type: string, pattern: "^[0-9]+(\\.[0-9]{1,2})?$" }
        estimatedApr: { type: string, pattern: "^[0-9]+(\\.[0-9]{1,4})?$" }
        loanTermMonths: { type: number, minimum: 1 }
        monthlyPayment: { type: string, pattern: "^[0-9]+(\\.[0-9]{1,2})?$" }
        insurance: { type: string, pattern: "^[0-9]+(\\.[0-9]{1,2})?$" }
        fuel: { type: string, pattern: "^[0-9]+(\\.[0-9]{1,2})?$" }
        maintenanceReserve: { type: string, pattern: "^[0-9]+(\\.[0-9]{1,2})?$" }
        registrationReserve: { type: string, pattern: "^[0-9]+(\\.[0-9]{1,2})?$" }
        parkingTolls: { type: string, pattern: "^[0-9]+(\\.[0-9]{1,2})?$" }
        otherMonthlyCost: { type: string, pattern: "^[0-9]+(\\.[0-9]{1,2})?$" }
        notes: { type: string, maxLength: 1000 }
    VehicleScenario:
      type: object
      required: [id, name, vehiclePrice, downPayment, loanAmount, estimatedApr, loanTermMonths, monthlyPayment, paymentSource, insurance, fuel, maintenanceReserve, registrationReserve, parkingTolls, otherMonthlyCost, totalMonthlyCost, currentOperatingCost, newOperatingCost, affordabilityStatus, newOperatingBudget, newFloorSurplus, capitalSurplusImpact, cashBufferImpact, emergencyReserveImpact, duplexContributionImpact, horizonImpact, missingInputs, explanation, planningOnly, liabilityCreated, status]
      properties:
        id: { type: string }
        name: { type: string }
        vehiclePrice: { type: ["string", "null"] }
        downPayment: { type: ["string", "null"] }
        loanAmount: { type: ["string", "null"] }
        estimatedApr: { type: ["string", "null"] }
        loanTermMonths: { type: ["string", "null"] }
        monthlyPayment: { type: string }
        paymentSource: { type: string, enum: [USER_PROVIDED, DERIVED_FROM_APR_TERM, NOT_CALCULATED] }
        insurance: { type: string }
        fuel: { type: string }
        maintenanceReserve: { type: string }
        registrationReserve: { type: string }
        parkingTolls: { type: string }
        otherMonthlyCost: { type: string }
        totalMonthlyCost: { type: string }
        currentOperatingCost: { type: string }
        newOperatingCost: { type: string }
        affordabilityStatus: { type: string }
        newOperatingBudget: { type: string }
        newFloorSurplus: { type: string }
        capitalSurplusImpact: { type: string }
        cashBufferImpact: { type: string }
        emergencyReserveImpact: { type: string }
        duplexContributionImpact: { type: string }
        horizonImpact:
          type: object
          required: [days30, days60, days90]
          properties:
            days30: { type: string }
            days60: { type: string }
            days90: { type: string }
        planningOnly: { type: boolean }
        liabilityCreated: { type: boolean }
        status: { type: string }
        missingInputs: { type: array, items: { type: string } }
        explanation: { type: string }
        notes: { type: ["string", "null"] }
        active: { type: boolean }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    VariableBudgetConstraintSet:
      type: object
      required: [verifiedIncome, incomeFloor, baseIncome, strongIncome, mandatoryObligations, essentialVariableCosts, reserveRequirements, discretionarySpending, currentCash, cashBuffer, capitalGoals, next30DayObligations, operatingBudgetCap, minimumViableOperatingCost, optionalDiscretionaryAllowance, capitalSurplusAtFloor, capitalSurplusAtBase, capitalSurplusAtStrong, forecastShortfall, obligationCoverage, status]
      properties:
        verifiedIncome: { type: string }
        incomeFloor: { type: string }
        baseIncome: { type: string }
        strongIncome: { type: string }
        mandatoryObligations: { type: string }
        essentialVariableCosts: { type: string }
        reserveRequirements: { type: string }
        discretionarySpending: { type: string }
        currentCash: { type: string }
        cashBuffer: { type: string }
        capitalGoals: { type: string }
        next30DayObligations: { type: string }
        operatingBudgetCap: { type: string }
        minimumViableOperatingCost: { type: string }
        optionalDiscretionaryAllowance: { type: string }
        capitalSurplusAtFloor: { type: string }
        capitalSurplusAtBase: { type: string }
        capitalSurplusAtStrong: { type: string }
        forecastShortfall: { type: string }
        obligationCoverage: { type: number }
        status: { type: string }
    VariableBudgetForecast:
      type: object
      required: [days, scenario, openingCash, income, mandatoryOutflows, essentialAllowance, reserveFunding, discretionaryAllowance, capitalContributions, endingCash, shortfall, pressure, status, calculationRows, missingInputs, explanation]
      properties:
        days: { type: number }
        scenario: { type: string, enum: [FLOOR, BASE, STRONG] }
        scenarioIncome: { type: string }
        openingCash: { type: string }
        income: { type: string }
        mandatoryOutflows: { type: string }
        essentialAllowance: { type: string }
        reserveFunding: { type: string }
        discretionaryAllowance: { type: string }
        capitalContributions: { type: string }
        endingCash: { type: string }
        shortfall: { type: string }
        pressure: { type: string, enum: [LOW, MODERATE, HIGH, CRITICAL, INCOMPLETE] }
        explanation: { type: string }
        obligations: { type: string }
        essentialSpending: { type: string }
        reserveContributions: { type: string }
        approvedCapitalContributions: { type: string }
        endingProjectedCash: { type: string }
        bufferShortfall: { type: string }
        status: { type: string }
        calculationRows:
          type: array
          items:
            type: object
            required: [key, label, operation, amount, source]
            properties:
              key: { type: string }
              label: { type: string }
              operation: { type: string, enum: [ADD, SUBTRACT] }
              amount: { type: string }
              source: { type: string }
        missingInputs: { type: array, items: { type: string } }
    VariableBudgetIntelligence:
      type: object
      required: [asOf, source, incomeProfile, documentEvidence, constraints, obligations, reserve, cash, forecast, forecastExplanation, vehicleScenarios]
      properties:
        asOf: { type: string }
        source:
          type: object
          required: [verifiedIncomeEventCount, approvedBudgetPeriod, incomeAuthority, planningStatus, forecastReadiness, pendingDocumentEvidence]
          properties:
            verifiedIncomeEventCount: { type: number }
            approvedBudgetPeriod: { type: ["string", "null"] }
            incomeAuthority: { type: string }
            planningStatus: { type: string }
            forecastReadiness: { type: string }
            pendingDocumentEvidence: { type: boolean }
        incomeProfile: { $ref: "#/components/schemas/VariableIncomeProfile" }
        documentEvidence:
          type: object
          description: Household-scoped uploaded-document evidence summary. Advisory only; never an accounting, income, balance, ledger, or money-movement authority.
          required: [documentCount, statementCount, reviewedDocumentCount, pendingDocumentCount, reviewedRowCount, pendingRowCount, reviewedDeposits, reviewedWithdrawals, excludedTransferCount, linkedSettlementDepositCount, latestStatementPeriod, latestStatementDate, readinessStatus, sourceDocumentIds, affectsOfficialTotals, explanation]
          properties:
            documentCount: { type: number }
            statementCount: { type: number }
            reviewedDocumentCount: { type: number }
            pendingDocumentCount: { type: number }
            reviewedRowCount: { type: number }
            pendingRowCount: { type: number }
            reviewedDeposits: { type: string, description: Exact decimal advisory total. }
            reviewedWithdrawals: { type: string, description: Exact decimal advisory total. }
            excludedTransferCount: { type: number }
            linkedSettlementDepositCount: { type: number }
            latestStatementPeriod:
              type: ["object", "null"]
              properties:
                start: { type: ["string", "null"] }
                end: { type: ["string", "null"] }
            latestStatementDate: { type: ["string", "null"] }
            readinessStatus: { type: string, enum: [NO_UPLOADED_EVIDENCE, PENDING_REVIEW, REVIEWED_EVIDENCE_AVAILABLE] }
            sourceDocumentIds: { type: array, items: { type: string } }
            affectsOfficialTotals: { type: boolean, enum: [false] }
            explanation: { type: string }
        constraints: { $ref: "#/components/schemas/VariableBudgetConstraintSet" }
        obligations:
          type: object
          required: [next7Days, next14Days, next30Days, next60Days, next90Days, items]
          properties:
            next7Days: { type: string }
            next14Days: { type: string }
            next30Days: { type: string }
            next60Days: { type: string }
            next90Days: { type: string }
            items:
              type: array
              items:
                type: object
                required: [name, dueDate, amount, required, source]
                properties:
                  name: { type: string }
                  dueDate: { type: string }
                  amount: { type: string }
                  required: { type: boolean }
                  source: { type: string }
        reserve:
          type: object
          required: [targetMonths, target, current, fundingGap, monthlyFunding, status]
          properties:
            targetMonths: { type: number }
            target: { type: string }
            current: { type: string }
            fundingGap: { type: string }
            monthlyFunding: { type: string }
            status: { type: string }
        cash:
          type: object
          required: [current, buffer, coverage, pressure]
          properties:
            current: { type: string }
            buffer: { type: string }
            coverage: { type: number }
            pressure: { type: string }
        forecast:
          type: array
          items: { $ref: "#/components/schemas/VariableBudgetForecast" }
        forecastExplanation: { type: string }
        vehicleScenarios:
          type: array
          items: { $ref: "#/components/schemas/VehicleScenario" }
